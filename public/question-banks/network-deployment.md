# 网络、Linux、Nginx 与部署

# 网络分层与传输

## Q1：OSI 七层模型分别是什么？

**短回答：**

物理、数据链路、网络、传输、会话、表示、应用是概念分层；实际互联网常用 TCP/IP 模型，排障重点是每层职责与边界。

**原理：**

先说明输入、内部机制和可观察结果，再补充失败边界。 这道题的关键是把“OSI 七层模型分别是什么？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在公网 Web 服务、反向代理和线上故障排查中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果数据规模、并发量或团队人数扩大一个数量级，原方案的首个瓶颈在哪里，如何用指标验证？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [RFC 9293：TCP](https://datatracker.ietf.org/doc/html/rfc9293)、[RFC 5681：TCP 拥塞控制](https://datatracker.ietf.org/doc/html/rfc5681)（校验日期：2026-07-17）

## Q2：TCP 和 UDP 的核心区别是什么？

**短回答：**

TCP 提供面向连接的可靠有序字节流与拥塞控制，UDP 提供无连接数据报且不保证到达顺序，应用可自行构建可靠性。

**原理：**

回答时区分规范语义、具体实现和工程取舍，不能把经验规则说成绝对结论。 这道题的关键是把“TCP 和 UDP 的核心区别是什么？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在公网 Web 服务、反向代理和线上故障排查中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果面试官加入失败重试、版本冲突或安全攻击条件，哪些保证仍成立，哪些需要额外机制？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [RFC 9293：TCP](https://datatracker.ietf.org/doc/html/rfc9293)、[RFC 5681：TCP 拥塞控制](https://datatracker.ietf.org/doc/html/rfc5681)（校验日期：2026-07-17）

## Q3：TCP 三次握手为什么需要三次？

**短回答：**

双方要确认彼此发送和接收能力并同步初始序列号；第三次确认让服务端知道客户端已收到服务端序列号。

**原理：**

先给结论，再用一条真实调用链解释为什么，最后说明如何测量或验证。 这道题的关键是把“TCP 三次握手为什么需要三次？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在公网 Web 服务、反向代理和线上故障排查中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果数据规模、并发量或团队人数扩大一个数量级，原方案的首个瓶颈在哪里，如何用指标验证？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [RFC 9293：TCP](https://datatracker.ietf.org/doc/html/rfc9293)、[RFC 5681：TCP 拥塞控制](https://datatracker.ietf.org/doc/html/rfc5681)（校验日期：2026-07-17）

## Q4：TCP 四次挥手为什么常见？

**短回答：**

TCP 全双工两方向独立关闭，一方 FIN 只表示不再发送，对端可先 ACK 后处理完剩余数据再发送自己的 FIN。

**原理：**

先说明输入、内部机制和可观察结果，再补充失败边界。 这道题的关键是把“TCP 四次挥手为什么常见？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在公网 Web 服务、反向代理和线上故障排查中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果面试官加入失败重试、版本冲突或安全攻击条件，哪些保证仍成立，哪些需要额外机制？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [RFC 9293：TCP](https://datatracker.ietf.org/doc/html/rfc9293)、[RFC 5681：TCP 拥塞控制](https://datatracker.ietf.org/doc/html/rfc5681)（校验日期：2026-07-17）

## Q5：TIME_WAIT 的作用是什么？

**短回答：**

主动关闭方等待足够时间让旧报文过期，并可重发最后 ACK，避免同四元组新连接收到历史报文。

**原理：**

回答时区分规范语义、具体实现和工程取舍，不能把经验规则说成绝对结论。 这道题的关键是把“TIME_WAIT 的作用是什么？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在公网 Web 服务、反向代理和线上故障排查中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果数据规模、并发量或团队人数扩大一个数量级，原方案的首个瓶颈在哪里，如何用指标验证？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [RFC 9293：TCP](https://datatracker.ietf.org/doc/html/rfc9293)、[RFC 5681：TCP 拥塞控制](https://datatracker.ietf.org/doc/html/rfc5681)（校验日期：2026-07-17）

## Q6：TCP 如何保证可靠传输？

**短回答：**

通过序列号、确认、校验、重传、滑动窗口和拥塞控制；可靠指字节流交付，不表示应用业务一定成功。

**原理：**

先给结论，再用一条真实调用链解释为什么，最后说明如何测量或验证。 这道题的关键是把“TCP 如何保证可靠传输？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在公网 Web 服务、反向代理和线上故障排查中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果面试官加入失败重试、版本冲突或安全攻击条件，哪些保证仍成立，哪些需要额外机制？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [RFC 9293：TCP](https://datatracker.ietf.org/doc/html/rfc9293)、[RFC 5681：TCP 拥塞控制](https://datatracker.ietf.org/doc/html/rfc5681)（校验日期：2026-07-17）

## Q7：拥塞控制和流量控制有什么区别？

**短回答：**

流量控制按接收端窗口防止压垮对端，拥塞控制按网络反馈调整发送速率，保护路径中的共享网络。

**原理：**

先说明输入、内部机制和可观察结果，再补充失败边界。 这道题的关键是把“拥塞控制和流量控制有什么区别？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在公网 Web 服务、反向代理和线上故障排查中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果数据规模、并发量或团队人数扩大一个数量级，原方案的首个瓶颈在哪里，如何用指标验证？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [RFC 9293：TCP](https://datatracker.ietf.org/doc/html/rfc9293)、[RFC 5681：TCP 拥塞控制](https://datatracker.ietf.org/doc/html/rfc5681)（校验日期：2026-07-17）

## Q8：粘包为什么不是 TCP 的错误？

**短回答：**

TCP 只提供连续字节流，不保留应用 write 边界；应用协议必须用长度字段、分隔符或固定长度完成消息帧解析。

**原理：**

回答时区分规范语义、具体实现和工程取舍，不能把经验规则说成绝对结论。 这道题的关键是把“粘包为什么不是 TCP 的错误？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在公网 Web 服务、反向代理和线上故障排查中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果面试官加入失败重试、版本冲突或安全攻击条件，哪些保证仍成立，哪些需要额外机制？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [RFC 9293：TCP](https://datatracker.ietf.org/doc/html/rfc9293)、[RFC 5681：TCP 拥塞控制](https://datatracker.ietf.org/doc/html/rfc5681)（校验日期：2026-07-17）

# HTTP、TLS 与实时通信

## Q9：HTTP 为什么称为无状态协议？

**短回答：**

每个请求本身不要求服务器记住前一次请求，登录状态由 Cookie、令牌或服务器 Session 等应用机制补充。

**原理：**

先给结论，再用一条真实调用链解释为什么，最后说明如何测量或验证。 这道题的关键是把“HTTP 为什么称为无状态协议？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在公网 Web 服务、反向代理和线上故障排查中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果数据规模、并发量或团队人数扩大一个数量级，原方案的首个瓶颈在哪里，如何用指标验证？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [RFC 9110：HTTP Semantics](https://datatracker.ietf.org/doc/html/rfc9110)、[MDN HTTP](https://developer.mozilla.org/en-US/docs/Web/HTTP)（校验日期：2026-07-17）

## Q10：HTTPS 建立连接做了什么？

**短回答：**

TLS 协商版本和密码套件，验证服务器证书，进行密钥交换后用对称密钥加密并校验应用数据。

**原理：**

先说明输入、内部机制和可观察结果，再补充失败边界。 这道题的关键是把“HTTPS 建立连接做了什么？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在公网 Web 服务、反向代理和线上故障排查中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果面试官加入失败重试、版本冲突或安全攻击条件，哪些保证仍成立，哪些需要额外机制？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [RFC 9110：HTTP Semantics](https://datatracker.ietf.org/doc/html/rfc9110)、[MDN HTTP](https://developer.mozilla.org/en-US/docs/Web/HTTP)（校验日期：2026-07-17）

## Q11：HTTP keep-alive 的价值是什么？

**短回答：**

多个请求复用连接，减少 TCP/TLS 握手和慢启动成本；连接池需设置空闲超时和最大连接数。

**原理：**

回答时区分规范语义、具体实现和工程取舍，不能把经验规则说成绝对结论。 这道题的关键是把“HTTP keep-alive 的价值是什么？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在公网 Web 服务、反向代理和线上故障排查中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果数据规模、并发量或团队人数扩大一个数量级，原方案的首个瓶颈在哪里，如何用指标验证？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [RFC 9293：TCP](https://datatracker.ietf.org/doc/html/rfc9293)、[RFC 5681：TCP 拥塞控制](https://datatracker.ietf.org/doc/html/rfc5681)（校验日期：2026-07-17）

## Q12：SSE 断线续传如何实现？

**短回答：**

服务端为事件设置递增 id，客户端重连携带 Last-Event-ID，服务端从可保留的事件位置继续或明确要求重新同步。

**原理：**

先给结论，再用一条真实调用链解释为什么，最后说明如何测量或验证。 这道题的关键是把“SSE 断线续传如何实现？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在公网 Web 服务、反向代理和线上故障排查中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果面试官加入失败重试、版本冲突或安全攻击条件，哪些保证仍成立，哪些需要额外机制？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [WHATWG Server-sent events](https://html.spec.whatwg.org/multipage/server-sent-events.html)、[MDN 使用 SSE](https://developer.mozilla.org/en-US/docs/Web/API/Server-sent_events/Using_server-sent_events)（校验日期：2026-07-17）

## Q13：WebSocket 握手后为何不再是普通 HTTP？

**短回答：**

先通过 HTTP Upgrade 协商，成功后在同一 TCP 连接上传输 WebSocket 帧，消息、心跳和鉴权续期由应用协议处理。

**原理：**

先说明输入、内部机制和可观察结果，再补充失败边界。 这道题的关键是把“WebSocket 握手后为何不再是普通 HTTP？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在公网 Web 服务、反向代理和线上故障排查中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果数据规模、并发量或团队人数扩大一个数量级，原方案的首个瓶颈在哪里，如何用指标验证？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [RFC 6455：WebSocket](https://datatracker.ietf.org/doc/html/rfc6455)、[MDN WebSocket API](https://developer.mozilla.org/en-US/docs/Web/API/WebSocket_API)（校验日期：2026-07-17）

# Linux 排障与进程

## Q14：Linux 进程和线程有什么关系？

**短回答：**

线程共享进程地址空间和资源但有独立执行上下文，Linux 内核以 task 调度；多进程隔离更强、通信成本更高。

**原理：**

回答时区分规范语义、具体实现和工程取舍，不能把经验规则说成绝对结论。 这道题的关键是把“Linux 进程和线程有什么关系？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在公网 Web 服务、反向代理和线上故障排查中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果面试官加入失败重试、版本冲突或安全攻击条件，哪些保证仍成立，哪些需要额外机制？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [RFC 9293：TCP](https://datatracker.ietf.org/doc/html/rfc9293)、[Nginx 官方文档](https://nginx.org/en/docs/)（校验日期：2026-07-17）

## Q15：线上 CPU 飙高如何排查？

**短回答：**

先确认系统与进程 CPU、负载和核数，再定位线程与调用栈或火焰图，区分计算、死循环、GC 和内核等待。

**原理：**

先给结论，再用一条真实调用链解释为什么，最后说明如何测量或验证。 这道题的关键是把“线上 CPU 飙高如何排查？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在公网 Web 服务、反向代理和线上故障排查中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果数据规模、并发量或团队人数扩大一个数量级，原方案的首个瓶颈在哪里，如何用指标验证？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [RFC 9293：TCP](https://datatracker.ietf.org/doc/html/rfc9293)、[Nginx 官方文档](https://nginx.org/en/docs/)（校验日期：2026-07-17）

## Q16：内存持续增长如何判断泄漏？

**短回答：**

观察 RSS、堆、缓存和 swap 趋势，采集堆快照比较保留对象；Linux page cache 增长不应直接等同应用泄漏。

**原理：**

先说明输入、内部机制和可观察结果，再补充失败边界。 这道题的关键是把“内存持续增长如何判断泄漏？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在公网 Web 服务、反向代理和线上故障排查中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果面试官加入失败重试、版本冲突或安全攻击条件，哪些保证仍成立，哪些需要额外机制？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [RFC 9293：TCP](https://datatracker.ietf.org/doc/html/rfc9293)、[Nginx 官方文档](https://nginx.org/en/docs/)（校验日期：2026-07-17）

## Q17：load average 很高一定是 CPU 不够吗？

**短回答：**

Linux 负载还包含不可中断睡眠任务，可能是磁盘或网络存储 I/O；需结合 CPU 利用率、iowait 和进程状态判断。

**原理：**

回答时区分规范语义、具体实现和工程取舍，不能把经验规则说成绝对结论。 这道题的关键是把“load average 很高一定是 CPU 不够吗？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在公网 Web 服务、反向代理和线上故障排查中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果数据规模、并发量或团队人数扩大一个数量级，原方案的首个瓶颈在哪里，如何用指标验证？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [RFC 9293：TCP](https://datatracker.ietf.org/doc/html/rfc9293)、[Nginx 官方文档](https://nginx.org/en/docs/)（校验日期：2026-07-17）

## Q18：端口无法访问如何分层排查？

**短回答：**

确认进程监听地址与端口，再查本机防火墙、安全组、路由、DNS 和反向代理，分别从服务端与客户端测试连接。

**原理：**

先给结论，再用一条真实调用链解释为什么，最后说明如何测量或验证。 这道题的关键是把“端口无法访问如何分层排查？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在公网 Web 服务、反向代理和线上故障排查中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果面试官加入失败重试、版本冲突或安全攻击条件，哪些保证仍成立，哪些需要额外机制？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [Nginx 官方文档](https://nginx.org/en/docs/)、[Nginx 负载均衡](https://nginx.org/en/docs/http/load_balancing.html)（校验日期：2026-07-17）

# Nginx 与生产部署

## Q19：Nginx 反向代理解决什么问题？

**短回答：**

它接收公网连接并转发到应用，可统一 TLS、静态资源、超时、缓冲、限流、日志和负载均衡。

**原理：**

先说明输入、内部机制和可观察结果，再补充失败边界。 这道题的关键是把“Nginx 反向代理解决什么问题？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在公网 Web 服务、反向代理和线上故障排查中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果数据规模、并发量或团队人数扩大一个数量级，原方案的首个瓶颈在哪里，如何用指标验证？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [RFC 9110：HTTP Semantics](https://datatracker.ietf.org/doc/html/rfc9110)、[MDN HTTP](https://developer.mozilla.org/en-US/docs/Web/HTTP)（校验日期：2026-07-17）

## Q20：proxy_buffering 对 SSE 有何影响？

**短回答：**

代理缓冲可能攒满后才发送，破坏实时流；SSE 路由通常关闭缓冲并设置合适读超时，同时发送心跳。

**原理：**

回答时区分规范语义、具体实现和工程取舍，不能把经验规则说成绝对结论。 这道题的关键是把“proxy_buffering 对 SSE 有何影响？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在公网 Web 服务、反向代理和线上故障排查中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果面试官加入失败重试、版本冲突或安全攻击条件，哪些保证仍成立，哪些需要额外机制？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [WHATWG Server-sent events](https://html.spec.whatwg.org/multipage/server-sent-events.html)、[MDN 使用 SSE](https://developer.mozilla.org/en-US/docs/Web/API/Server-sent_events/Using_server-sent_events)（校验日期：2026-07-17）

## Q21：Nginx 负载均衡有哪些常见策略？

**短回答：**

默认轮询，还可最少连接、IP 哈希和权重；健康检查、超时与会话状态决定实际可用性。

**原理：**

先给结论，再用一条真实调用链解释为什么，最后说明如何测量或验证。 这道题的关键是把“Nginx 负载均衡有哪些常见策略？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在公网 Web 服务、反向代理和线上故障排查中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果数据规模、并发量或团队人数扩大一个数量级，原方案的首个瓶颈在哪里，如何用指标验证？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [Nginx 官方文档](https://nginx.org/en/docs/)、[Nginx 负载均衡](https://nginx.org/en/docs/http/load_balancing.html)（校验日期：2026-07-17）

## Q22：静态 SPA 为什么要配置 try_files？

**短回答：**

前端路由路径在服务器没有真实文件，需先尝试文件，失败回退 index.html；API 和静态资源路径应排除回退。

**原理：**

先说明输入、内部机制和可观察结果，再补充失败边界。 这道题的关键是把“静态 SPA 为什么要配置 try_files？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在公网 Web 服务、反向代理和线上故障排查中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果面试官加入失败重试、版本冲突或安全攻击条件，哪些保证仍成立，哪些需要额外机制？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [Nginx 官方文档](https://nginx.org/en/docs/)、[Nginx 负载均衡](https://nginx.org/en/docs/http/load_balancing.html)（校验日期：2026-07-17）

## Q23：Cloudflare Tunnel 的工作原理是什么？

**短回答：**

本机 cloudflared 主动建立到 Cloudflare 的出站隧道，公网主机名流量经边缘转入本地服务，无需开放家庭网络入站端口。

**原理：**

回答时区分规范语义、具体实现和工程取舍，不能把经验规则说成绝对结论。 这道题的关键是把“Cloudflare Tunnel 的工作原理是什么？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在公网 Web 服务、反向代理和线上故障排查中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果数据规模、并发量或团队人数扩大一个数量级，原方案的首个瓶颈在哪里，如何用指标验证？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [RFC 9293：TCP](https://datatracker.ietf.org/doc/html/rfc9293)、[Nginx 官方文档](https://nginx.org/en/docs/)（校验日期：2026-07-17）

## Q24：HTTPS 终止在代理后应用要注意什么？

**短回答：**

正确设置信任代理和 X-Forwarded-Proto/For，生成安全 Cookie 与绝对 URL 时使用原始协议，并限制可信代理来源。

**原理：**

先给结论，再用一条真实调用链解释为什么，最后说明如何测量或验证。 这道题的关键是把“HTTPS 终止在代理后应用要注意什么？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在公网 Web 服务、反向代理和线上故障排查中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果面试官加入失败重试、版本冲突或安全攻击条件，哪些保证仍成立，哪些需要额外机制？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [RFC 9110：HTTP Semantics](https://datatracker.ietf.org/doc/html/rfc9110)、[MDN HTTP](https://developer.mozilla.org/en-US/docs/Web/HTTP)（校验日期：2026-07-17）

## Q25：如何验证一次部署没有影响其他域名？

**短回答：**

分别检查 DNS、证书、状态码、关键页面和 Host 路由；子域改动不应覆盖根域记录，部署前后都做独立冒烟。

**原理：**

先说明输入、内部机制和可观察结果，再补充失败边界。 这道题的关键是把“如何验证一次部署没有影响其他域名？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在公网 Web 服务、反向代理和线上故障排查中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果数据规模、并发量或团队人数扩大一个数量级，原方案的首个瓶颈在哪里，如何用指标验证？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [RFC 9110：HTTP Semantics](https://datatracker.ietf.org/doc/html/rfc9110)、[MDN HTTP](https://developer.mozilla.org/en-US/docs/Web/HTTP)（校验日期：2026-07-17）

# Git 与协作工程

# Git 对象与工作区

## Q1：Git 的工作区、暂存区和仓库分别是什么？

**短回答：**

工作区是实际文件，暂存区保存下一次提交的快照，仓库以对象和引用保存历史；git add 更新索引，git commit 由索引生成提交。

**原理：**

先说明输入、内部机制和可观察结果，再补充失败边界。 这道题的关键是把“Git 的工作区、暂存区和仓库分别是什么？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在多人协作仓库、线上热修复与发布分支中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果数据规模、并发量或团队人数扩大一个数量级，原方案的首个瓶颈在哪里，如何用指标验证？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [Git 官方参考](https://git-scm.com/docs)、[Git 用户手册](https://git-scm.com/docs/user-manual)（校验日期：2026-07-17）

## Q2：Git 为什么说是快照模型而不是差异模型？

**短回答：**

每次提交记录一棵完整目录树，并通过内容寻址复用未变化的 blob；展示 diff 是比较两个快照的结果。

**原理：**

回答时区分规范语义、具体实现和工程取舍，不能把经验规则说成绝对结论。 这道题的关键是把“Git 为什么说是快照模型而不是差异模型？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在多人协作仓库、线上热修复与发布分支中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果面试官加入失败重试、版本冲突或安全攻击条件，哪些保证仍成立，哪些需要额外机制？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [Git 官方参考](https://git-scm.com/docs)、[Git 用户手册](https://git-scm.com/docs/user-manual)（校验日期：2026-07-17）

## Q3：blob、tree、commit 和 tag 对象有什么关系？

**短回答：**

blob 保存文件内容，tree 保存目录项，commit 指向根 tree 和父提交，附注 tag 再指向目标对象并保存签名等元数据。

**原理：**

先给结论，再用一条真实调用链解释为什么，最后说明如何测量或验证。 这道题的关键是把“blob、tree、commit 和 tag 对象有什么关系？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在多人协作仓库、线上热修复与发布分支中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果数据规模、并发量或团队人数扩大一个数量级，原方案的首个瓶颈在哪里，如何用指标验证？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [Git 官方参考](https://git-scm.com/docs)、[Git 用户手册](https://git-scm.com/docs/user-manual)（校验日期：2026-07-17）

## Q4：HEAD、branch 和 tag 有什么区别？

**短回答：**

HEAD 表示当前检出的提交或分支，branch 是可移动的提交引用，tag 通常是指向固定版本的发布标记。

**原理：**

先说明输入、内部机制和可观察结果，再补充失败边界。 这道题的关键是把“HEAD、branch 和 tag 有什么区别？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在多人协作仓库、线上热修复与发布分支中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果面试官加入失败重试、版本冲突或安全攻击条件，哪些保证仍成立，哪些需要额外机制？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [Git 官方参考](https://git-scm.com/docs)、[Git 用户手册](https://git-scm.com/docs/user-manual)（校验日期：2026-07-17）

## Q5：git add 到底做了什么？

**短回答：**

它把指定文件内容写成对象并更新 index 中路径对应的 blob、模式和阶段信息，不是简单把文件“放进缓存”。

**原理：**

回答时区分规范语义、具体实现和工程取舍，不能把经验规则说成绝对结论。 这道题的关键是把“git add 到底做了什么？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在多人协作仓库、线上热修复与发布分支中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果数据规模、并发量或团队人数扩大一个数量级，原方案的首个瓶颈在哪里，如何用指标验证？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [Git 官方参考](https://git-scm.com/docs)、[Git 用户手册](https://git-scm.com/docs/user-manual)（校验日期：2026-07-17）

## Q6：git status 为什么能判断文件状态？

**短回答：**

Git 比较 HEAD 树、index 与工作区三份状态：HEAD 对 index 得到待提交变化，index 对工作区得到未暂存变化。

**原理：**

先给结论，再用一条真实调用链解释为什么，最后说明如何测量或验证。 这道题的关键是把“git status 为什么能判断文件状态？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在多人协作仓库、线上热修复与发布分支中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果面试官加入失败重试、版本冲突或安全攻击条件，哪些保证仍成立，哪些需要额外机制？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [Git 官方参考](https://git-scm.com/docs)、[Git 用户手册](https://git-scm.com/docs/user-manual)（校验日期：2026-07-17）

## Q7：git diff、git diff --cached 和 git show 如何区分？

**短回答：**

前者比较工作区与 index，--cached 比较 index 与 HEAD，git show 默认展示指定对象及提交引入的变化。

**原理：**

先说明输入、内部机制和可观察结果，再补充失败边界。 这道题的关键是把“git diff、git diff --cached 和 git show 如何区分？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在多人协作仓库、线上热修复与发布分支中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果数据规模、并发量或团队人数扩大一个数量级，原方案的首个瓶颈在哪里，如何用指标验证？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [Git 官方参考](https://git-scm.com/docs)、[Git 用户手册](https://git-scm.com/docs/user-manual)（校验日期：2026-07-17）

## Q8：什么是 detached HEAD？

**短回答：**

HEAD 直接指向提交而非分支引用；此时提交可创建但没有分支名称保护，应及时创建分支避免后续不可达。

**原理：**

回答时区分规范语义、具体实现和工程取舍，不能把经验规则说成绝对结论。 这道题的关键是把“什么是 detached HEAD？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在多人协作仓库、线上热修复与发布分支中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果面试官加入失败重试、版本冲突或安全攻击条件，哪些保证仍成立，哪些需要额外机制？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [Git 官方参考](https://git-scm.com/docs)、[Git 用户手册](https://git-scm.com/docs/user-manual)（校验日期：2026-07-17）

## Q9：.gitignore 为什么对已跟踪文件不生效？

**短回答：**

忽略规则只影响未跟踪路径的发现；已进入 index 的路径仍受版本控制，需要先 git rm --cached 再提交。

**原理：**

先给结论，再用一条真实调用链解释为什么，最后说明如何测量或验证。 这道题的关键是把“.gitignore 为什么对已跟踪文件不生效？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在多人协作仓库、线上热修复与发布分支中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果数据规模、并发量或团队人数扩大一个数量级，原方案的首个瓶颈在哪里，如何用指标验证？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [Git 官方参考](https://git-scm.com/docs)、[Git 用户手册](https://git-scm.com/docs/user-manual)（校验日期：2026-07-17）

## Q10：Git 的内容寻址和 SHA 哈希解决什么问题？

**短回答：**

对象名由类型、长度和内容计算，可用于去重与完整性校验；哈希相同表示 Git 按对象模型识别为同一内容。

**原理：**

先说明输入、内部机制和可观察结果，再补充失败边界。 这道题的关键是把“Git 的内容寻址和 SHA 哈希解决什么问题？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在多人协作仓库、线上热修复与发布分支中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果面试官加入失败重试、版本冲突或安全攻击条件，哪些保证仍成立，哪些需要额外机制？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [Git 官方参考](https://git-scm.com/docs)、[Git 用户手册](https://git-scm.com/docs/user-manual)（校验日期：2026-07-17）

# 分支、合并与历史

## Q11：merge 和 rebase 的核心区别是什么？

**短回答：**

merge 保留分叉并产生合并提交，rebase 复制提交到新基线并重写提交身份；公共历史通常避免随意 rebase。

**原理：**

回答时区分规范语义、具体实现和工程取舍，不能把经验规则说成绝对结论。 这道题的关键是把“merge 和 rebase 的核心区别是什么？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在多人协作仓库、线上热修复与发布分支中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果数据规模、并发量或团队人数扩大一个数量级，原方案的首个瓶颈在哪里，如何用指标验证？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [Git 官方参考](https://git-scm.com/docs)、[Git 用户手册](https://git-scm.com/docs/user-manual)（校验日期：2026-07-17）

## Q12：fast-forward 合并是什么？

**短回答：**

目标分支没有额外提交时，只需把分支引用前移到新提交，不必创建双亲合并提交。

**原理：**

先给结论，再用一条真实调用链解释为什么，最后说明如何测量或验证。 这道题的关键是把“fast-forward 合并是什么？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在多人协作仓库、线上热修复与发布分支中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果面试官加入失败重试、版本冲突或安全攻击条件，哪些保证仍成立，哪些需要额外机制？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [Git 官方参考](https://git-scm.com/docs)、[Git 用户手册](https://git-scm.com/docs/user-manual)（校验日期：2026-07-17）

## Q13：三方合并为什么需要共同祖先？

**短回答：**

合并算法比较共同祖先、ours 和 theirs，只有双方相对基线都改动同一区域时才需要人工解决冲突。

**原理：**

先说明输入、内部机制和可观察结果，再补充失败边界。 这道题的关键是把“三方合并为什么需要共同祖先？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在多人协作仓库、线上热修复与发布分支中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果数据规模、并发量或团队人数扩大一个数量级，原方案的首个瓶颈在哪里，如何用指标验证？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [Git 官方参考](https://git-scm.com/docs)、[Git 用户手册](https://git-scm.com/docs/user-manual)（校验日期：2026-07-17）

## Q14：如何安全解决 merge conflict？

**短回答：**

先读取冲突阶段和业务语义，编辑出最终内容，运行测试后 git add 标记已解决，再继续 merge 或 rebase；不要只删除标记。

**原理：**

回答时区分规范语义、具体实现和工程取舍，不能把经验规则说成绝对结论。 这道题的关键是把“如何安全解决 merge conflict？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在多人协作仓库、线上热修复与发布分支中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果面试官加入失败重试、版本冲突或安全攻击条件，哪些保证仍成立，哪些需要额外机制？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [Git 官方参考](https://git-scm.com/docs)、[Git 用户手册](https://git-scm.com/docs/user-manual)（校验日期：2026-07-17）

## Q15：git rebase --onto 适合什么场景？

**短回答：**

它可把一段提交从旧基线搬到指定新基线，适合拆错分支或只迁移某段连续历史。

**原理：**

先给结论，再用一条真实调用链解释为什么，最后说明如何测量或验证。 这道题的关键是把“git rebase --onto 适合什么场景？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在多人协作仓库、线上热修复与发布分支中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果数据规模、并发量或团队人数扩大一个数量级，原方案的首个瓶颈在哪里，如何用指标验证？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [Git 官方参考](https://git-scm.com/docs)、[Git 用户手册](https://git-scm.com/docs/user-manual)（校验日期：2026-07-17）

## Q16：cherry-pick 的作用和风险是什么？

**短回答：**

它把指定提交的补丁应用为新提交；会生成新身份，重复挑选或与后续合并叠加可能产生冲突与重复变更。

**原理：**

先说明输入、内部机制和可观察结果，再补充失败边界。 这道题的关键是把“cherry-pick 的作用和风险是什么？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在多人协作仓库、线上热修复与发布分支中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果面试官加入失败重试、版本冲突或安全攻击条件，哪些保证仍成立，哪些需要额外机制？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [Git 官方参考](https://git-scm.com/docs)、[Git 用户手册](https://git-scm.com/docs/user-manual)（校验日期：2026-07-17）

## Q17：revert 和 reset 有什么区别？

**短回答：**

revert 新建反向提交，适合共享分支；reset 移动当前分支并可改变 index/工作区，更适合尚未共享的本地历史。

**原理：**

回答时区分规范语义、具体实现和工程取舍，不能把经验规则说成绝对结论。 这道题的关键是把“revert 和 reset 有什么区别？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在多人协作仓库、线上热修复与发布分支中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果数据规模、并发量或团队人数扩大一个数量级，原方案的首个瓶颈在哪里，如何用指标验证？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [Git 官方参考](https://git-scm.com/docs)、[Git 用户手册](https://git-scm.com/docs/user-manual)（校验日期：2026-07-17）

## Q18：soft、mixed、hard reset 分别改变什么？

**短回答：**

soft 只移动 HEAD，mixed 还重置 index，hard 进一步覆盖工作区；hard 会丢弃未保存修改，使用前必须确认范围。

**原理：**

先给结论，再用一条真实调用链解释为什么，最后说明如何测量或验证。 这道题的关键是把“soft、mixed、hard reset 分别改变什么？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在多人协作仓库、线上热修复与发布分支中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果面试官加入失败重试、版本冲突或安全攻击条件，哪些保证仍成立，哪些需要额外机制？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [Git 官方参考](https://git-scm.com/docs)、[Git 用户手册](https://git-scm.com/docs/user-manual)（校验日期：2026-07-17）

## Q19：reflog 能恢复哪些误操作？

**短回答：**

它记录本地引用的移动，可找回误 reset、误 rebase 或删除分支前指向的提交；但有过期清理且不随 push 共享。

**原理：**

先说明输入、内部机制和可观察结果，再补充失败边界。 这道题的关键是把“reflog 能恢复哪些误操作？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在多人协作仓库、线上热修复与发布分支中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果数据规模、并发量或团队人数扩大一个数量级，原方案的首个瓶颈在哪里，如何用指标验证？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [Git 官方参考](https://git-scm.com/docs)、[Git 用户手册](https://git-scm.com/docs/user-manual)（校验日期：2026-07-17）

## Q20：什么是 squash，何时不该 squash？

**短回答：**

squash 把多个提交整理为较少提交；需要保留独立回滚点、审计轨迹或有意义的分阶段变更时不应盲目压缩。

**原理：**

回答时区分规范语义、具体实现和工程取舍，不能把经验规则说成绝对结论。 这道题的关键是把“什么是 squash，何时不该 squash？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在多人协作仓库、线上热修复与发布分支中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果面试官加入失败重试、版本冲突或安全攻击条件，哪些保证仍成立，哪些需要额外机制？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [Git 官方参考](https://git-scm.com/docs)、[Git 用户手册](https://git-scm.com/docs/user-manual)（校验日期：2026-07-17）

# 远程协作与发布

## Q21：fetch、pull 和 push 的边界是什么？

**短回答：**

fetch 更新远程跟踪引用但不整合，pull 等于 fetch 后 merge 或 rebase，push 请求更新远端引用并发送缺失对象。

**原理：**

先给结论，再用一条真实调用链解释为什么，最后说明如何测量或验证。 这道题的关键是把“fetch、pull 和 push 的边界是什么？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在多人协作仓库、线上热修复与发布分支中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果数据规模、并发量或团队人数扩大一个数量级，原方案的首个瓶颈在哪里，如何用指标验证？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [Git 官方参考](https://git-scm.com/docs)、[Git 用户手册](https://git-scm.com/docs/user-manual)（校验日期：2026-07-17）

## Q22：origin/main 是什么？

**短回答：**

它是本地保存的远程跟踪引用，表示最近一次 fetch 后远端 main 的位置，不会在离线时自动变化。

**原理：**

先说明输入、内部机制和可观察结果，再补充失败边界。 这道题的关键是把“origin/main 是什么？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在多人协作仓库、线上热修复与发布分支中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果面试官加入失败重试、版本冲突或安全攻击条件，哪些保证仍成立，哪些需要额外机制？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [Git 官方参考](https://git-scm.com/docs)、[Git 用户手册](https://git-scm.com/docs/user-manual)（校验日期：2026-07-17）

## Q23：non-fast-forward push 为什么被拒绝？

**短回答：**

远端引用若不能只前移到本地提交，普通 push 会覆盖别人可达的历史，因此服务器默认拒绝。

**原理：**

回答时区分规范语义、具体实现和工程取舍，不能把经验规则说成绝对结论。 这道题的关键是把“non-fast-forward push 为什么被拒绝？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在多人协作仓库、线上热修复与发布分支中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果数据规模、并发量或团队人数扩大一个数量级，原方案的首个瓶颈在哪里，如何用指标验证？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [Git 官方参考](https://git-scm.com/docs)、[Git 用户手册](https://git-scm.com/docs/user-manual)（校验日期：2026-07-17）

## Q24：force-with-lease 为什么比 force 安全？

**短回答：**

它只在远端引用仍等于本地预期值时强推，能避免无意覆盖他人在最近一次 fetch 后新增的提交。

**原理：**

先给结论，再用一条真实调用链解释为什么，最后说明如何测量或验证。 这道题的关键是把“force-with-lease 为什么比 force 安全？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在多人协作仓库、线上热修复与发布分支中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果面试官加入失败重试、版本冲突或安全攻击条件，哪些保证仍成立，哪些需要额外机制？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [Git 官方参考](https://git-scm.com/docs)、[Git 用户手册](https://git-scm.com/docs/user-manual)（校验日期：2026-07-17）

## Q25：如何设计 feature branch 与主干协作？

**短回答：**

分支应短生命周期、频繁同步主干、通过 PR 和自动检查合并；功能开关用于把部署与功能发布解耦。

**原理：**

先说明输入、内部机制和可观察结果，再补充失败边界。 这道题的关键是把“如何设计 feature branch 与主干协作？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在多人协作仓库、线上热修复与发布分支中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果数据规模、并发量或团队人数扩大一个数量级，原方案的首个瓶颈在哪里，如何用指标验证？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [Git 官方参考](https://git-scm.com/docs)、[Git 用户手册](https://git-scm.com/docs/user-manual)（校验日期：2026-07-17）

## Q26：Git Flow、GitHub Flow 和 trunk-based 如何选择？

**短回答：**

多版本发布可用更重的发布分支；持续部署偏向短分支或主干开发，选择依据是发布节奏、合规和团队规模。

**原理：**

回答时区分规范语义、具体实现和工程取舍，不能把经验规则说成绝对结论。 这道题的关键是把“Git Flow、GitHub Flow 和 trunk-based 如何选择？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在多人协作仓库、线上热修复与发布分支中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果面试官加入失败重试、版本冲突或安全攻击条件，哪些保证仍成立，哪些需要额外机制？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [Git 官方参考](https://git-scm.com/docs)、[Git 用户手册](https://git-scm.com/docs/user-manual)（校验日期：2026-07-17）

## Q27：如何处理线上 hotfix？

**短回答：**

从线上实际版本创建修复分支，最小改动并回归，发布后把修复合并或挑选回所有仍维护的分支，避免分叉遗漏。

**原理：**

先给结论，再用一条真实调用链解释为什么，最后说明如何测量或验证。 这道题的关键是把“如何处理线上 hotfix？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在多人协作仓库、线上热修复与发布分支中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果数据规模、并发量或团队人数扩大一个数量级，原方案的首个瓶颈在哪里，如何用指标验证？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [Git 官方参考](https://git-scm.com/docs)、[Git 用户手册](https://git-scm.com/docs/user-manual)（校验日期：2026-07-17）

## Q28：语义化版本与 Git tag 如何配合？

**短回答：**

通过受保护 tag 标记已构建的不可变提交，流水线从 tag 产出制品；版本号表达不兼容、功能和修复级别。

**原理：**

先说明输入、内部机制和可观察结果，再补充失败边界。 这道题的关键是把“语义化版本与 Git tag 如何配合？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在多人协作仓库、线上热修复与发布分支中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果面试官加入失败重试、版本冲突或安全攻击条件，哪些保证仍成立，哪些需要额外机制？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [Git 官方参考](https://git-scm.com/docs)、[Git 用户手册](https://git-scm.com/docs/user-manual)（校验日期：2026-07-17）

## Q29：submodule 和 monorepo 的权衡是什么？

**短回答：**

submodule 保持仓库与版本边界但协作复杂；monorepo 统一变更和工具链但需要权限、构建缓存与依赖边界治理。

**原理：**

回答时区分规范语义、具体实现和工程取舍，不能把经验规则说成绝对结论。 这道题的关键是把“submodule 和 monorepo 的权衡是什么？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在多人协作仓库、线上热修复与发布分支中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果数据规模、并发量或团队人数扩大一个数量级，原方案的首个瓶颈在哪里，如何用指标验证？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [Git 官方参考](https://git-scm.com/docs)、[Git 用户手册](https://git-scm.com/docs/user-manual)（校验日期：2026-07-17）

## Q30：git worktree 解决什么问题？

**短回答：**

同一仓库可同时拥有多个工作树，各有 HEAD 和 index，适合并行热修复或评审分支而无需反复 stash 和切换。

**原理：**

先给结论，再用一条真实调用链解释为什么，最后说明如何测量或验证。 这道题的关键是把“git worktree 解决什么问题？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在多人协作仓库、线上热修复与发布分支中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果面试官加入失败重试、版本冲突或安全攻击条件，哪些保证仍成立，哪些需要额外机制？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [Git 官方参考](https://git-scm.com/docs)、[Git 用户手册](https://git-scm.com/docs/user-manual)（校验日期：2026-07-17）

# 排障与工程质量

## Q31：git bisect 如何定位回归提交？

**短回答：**

给定一个 good 与 bad，Git 二分检出中间提交；每轮运行测试并标记，最终把搜索缩小到首个坏提交。

**原理：**

先说明输入、内部机制和可观察结果，再补充失败边界。 这道题的关键是把“git bisect 如何定位回归提交？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在多人协作仓库、线上热修复与发布分支中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果数据规模、并发量或团队人数扩大一个数量级，原方案的首个瓶颈在哪里，如何用指标验证？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [Git 官方参考](https://git-scm.com/docs)、[Git 用户手册](https://git-scm.com/docs/user-manual)（校验日期：2026-07-17）

## Q32：git blame 应该如何正确使用？

**短回答：**

它用于找到某行最近一次修改，辅助理解上下文；应结合 log、PR 和业务历史，不应当作责任追究工具。

**原理：**

回答时区分规范语义、具体实现和工程取舍，不能把经验规则说成绝对结论。 这道题的关键是把“git blame 应该如何正确使用？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在多人协作仓库、线上热修复与发布分支中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果面试官加入失败重试、版本冲突或安全攻击条件，哪些保证仍成立，哪些需要额外机制？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [Git 官方参考](https://git-scm.com/docs)、[Git 用户手册](https://git-scm.com/docs/user-manual)（校验日期：2026-07-17）

## Q33：如何从错误提交中只恢复一个文件？

**短回答：**

先用 git restore --source=<commit> -- path 把目标版本写入工作区或 index，检查 diff 后作为新修复提交保存。

**原理：**

先给结论，再用一条真实调用链解释为什么，最后说明如何测量或验证。 这道题的关键是把“如何从错误提交中只恢复一个文件？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在多人协作仓库、线上热修复与发布分支中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果数据规模、并发量或团队人数扩大一个数量级，原方案的首个瓶颈在哪里，如何用指标验证？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [Git 官方参考](https://git-scm.com/docs)、[Git 用户手册](https://git-scm.com/docs/user-manual)（校验日期：2026-07-17）

## Q34：大文件为什么会拖慢 Git？

**短回答：**

每个历史版本都进入对象库，克隆和对象遍历成本持续累积；应使用 Git LFS、制品仓库或对象存储管理二进制资产。

**原理：**

先说明输入、内部机制和可观察结果，再补充失败边界。 这道题的关键是把“大文件为什么会拖慢 Git？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在多人协作仓库、线上热修复与发布分支中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果面试官加入失败重试、版本冲突或安全攻击条件，哪些保证仍成立，哪些需要额外机制？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [Git 官方参考](https://git-scm.com/docs)、[Git 用户手册](https://git-scm.com/docs/user-manual)（校验日期：2026-07-17）

## Q35：什么是 shallow clone 和 partial clone？

**短回答：**

shallow clone 截断提交历史，partial clone 延迟获取部分对象；能降低 CI 初始成本，但某些历史分析命令受限。

**原理：**

回答时区分规范语义、具体实现和工程取舍，不能把经验规则说成绝对结论。 这道题的关键是把“什么是 shallow clone 和 partial clone？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在多人协作仓库、线上热修复与发布分支中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果数据规模、并发量或团队人数扩大一个数量级，原方案的首个瓶颈在哪里，如何用指标验证？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [Git 官方参考](https://git-scm.com/docs)、[Git 用户手册](https://git-scm.com/docs/user-manual)（校验日期：2026-07-17）

## Q36：Git hooks 能做什么，为什么不能只依赖本地 hook？

**短回答：**

hook 可在提交或接收阶段执行校验；本地 hook 容易被跳过且分发不一致，关键规则还应在 CI 或服务端执行。

**原理：**

先给结论，再用一条真实调用链解释为什么，最后说明如何测量或验证。 这道题的关键是把“Git hooks 能做什么，为什么不能只依赖本地 hook？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在多人协作仓库、线上热修复与发布分支中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果面试官加入失败重试、版本冲突或安全攻击条件，哪些保证仍成立，哪些需要额外机制？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [Git 官方参考](https://git-scm.com/docs)、[Git 用户手册](https://git-scm.com/docs/user-manual)（校验日期：2026-07-17）

## Q37：CI 中如何保证构建可复现？

**短回答：**

固定依赖锁、运行时与构建参数，从明确提交构建并保存校验和；不要让流水线隐式依赖开发机或浮动版本。

**原理：**

先说明输入、内部机制和可观察结果，再补充失败边界。 这道题的关键是把“CI 中如何保证构建可复现？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在多人协作仓库、线上热修复与发布分支中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果数据规模、并发量或团队人数扩大一个数量级，原方案的首个瓶颈在哪里，如何用指标验证？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [Git 官方参考](https://git-scm.com/docs)、[Git 用户手册](https://git-scm.com/docs/user-manual)（校验日期：2026-07-17）

## Q38：如何避免敏感信息进入 Git 历史？

**短回答：**

提交前用密钥扫描和环境变量；一旦泄露先吊销轮换，再用历史重写清理，并通知协作者重新同步。

**原理：**

回答时区分规范语义、具体实现和工程取舍，不能把经验规则说成绝对结论。 这道题的关键是把“如何避免敏感信息进入 Git 历史？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在多人协作仓库、线上热修复与发布分支中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果面试官加入失败重试、版本冲突或安全攻击条件，哪些保证仍成立，哪些需要额外机制？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [Git 官方参考](https://git-scm.com/docs)、[Git 用户手册](https://git-scm.com/docs/user-manual)（校验日期：2026-07-17）

## Q39：如何评审一个大型 PR？

**短回答：**

先核对目标和架构边界，再按提交或模块审查，运行测试并关注失败路径；大型改动应提前拆成可独立验证的小批次。

**原理：**

先给结论，再用一条真实调用链解释为什么，最后说明如何测量或验证。 这道题的关键是把“如何评审一个大型 PR？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在多人协作仓库、线上热修复与发布分支中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果数据规模、并发量或团队人数扩大一个数量级，原方案的首个瓶颈在哪里，如何用指标验证？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [Git 官方参考](https://git-scm.com/docs)、[Git 用户手册](https://git-scm.com/docs/user-manual)（校验日期：2026-07-17）

## Q40：如何制定提交信息规范？

**短回答：**

标题说明可观察的变更，正文解释原因、边界和迁移；关联任务与破坏性变更，便于生成日志、回滚和追踪决策。

**原理：**

先说明输入、内部机制和可观察结果，再补充失败边界。 这道题的关键是把“如何制定提交信息规范？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在多人协作仓库、线上热修复与发布分支中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果面试官加入失败重试、版本冲突或安全攻击条件，哪些保证仍成立，哪些需要额外机制？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [Git 官方参考](https://git-scm.com/docs)、[Git 用户手册](https://git-scm.com/docs/user-manual)（校验日期：2026-07-17）

# React 核心与工程实践

# 组件与渲染模型

## Q1：React 组件为什么应当是纯函数？

**短回答：**

相同 props、state 和 context 应返回相同 JSX，渲染阶段不产生外部副作用，React 才能安全重试、暂停或丢弃一次渲染。

**原理：**

先说明输入、内部机制和可观察结果，再补充失败边界。 这道题的关键是把“React 组件为什么应当是纯函数？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在中大型 React 产品、数据流和交互性能优化中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果数据规模、并发量或团队人数扩大一个数量级，原方案的首个瓶颈在哪里，如何用指标验证？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [React 官方文档](https://react.dev/reference/react)、[React 状态管理指南](https://react.dev/learn/managing-state)（校验日期：2026-07-17）

## Q2：JSX 最终是什么？

**短回答：**

JSX 经编译变为创建 React element 的调用，element 是描述类型、props 和 key 的不可变普通对象，不是真实 DOM。

**原理：**

回答时区分规范语义、具体实现和工程取舍，不能把经验规则说成绝对结论。 这道题的关键是把“JSX 最终是什么？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在中大型 React 产品、数据流和交互性能优化中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果面试官加入失败重试、版本冲突或安全攻击条件，哪些保证仍成立，哪些需要额外机制？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [React 官方文档](https://react.dev/reference/react)、[React 状态管理指南](https://react.dev/learn/managing-state)（校验日期：2026-07-17）

## Q3：state 为什么像一次渲染的快照？

**短回答：**

每次渲染中的事件处理器闭包捕获当次 state 值，setState 请求下一次渲染，不会修改当前闭包里的变量。

**原理：**

先给结论，再用一条真实调用链解释为什么，最后说明如何测量或验证。 这道题的关键是把“state 为什么像一次渲染的快照？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在中大型 React 产品、数据流和交互性能优化中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果数据规模、并发量或团队人数扩大一个数量级，原方案的首个瓶颈在哪里，如何用指标验证？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [React 官方文档](https://react.dev/reference/react)、[React 状态管理指南](https://react.dev/learn/managing-state)（校验日期：2026-07-17）

## Q4：React 为什么批处理状态更新？

**短回答：**

同一事件或受支持异步边界内的更新进入队列，事件结束后统一计算和提交，减少中间渲染并保持 UI 一致。

**原理：**

先说明输入、内部机制和可观察结果，再补充失败边界。 这道题的关键是把“React 为什么批处理状态更新？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在中大型 React 产品、数据流和交互性能优化中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果面试官加入失败重试、版本冲突或安全攻击条件，哪些保证仍成立，哪些需要额外机制？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [React 官方文档](https://react.dev/reference/react)、[React 状态管理指南](https://react.dev/learn/managing-state)（校验日期：2026-07-17）

## Q5：函数式 setState 何时必须使用？

**短回答：**

新状态依赖前一个状态或同一批次连续更新时传 updater，React 会按队列依次计算，避免读取旧快照导致丢更新。

**原理：**

回答时区分规范语义、具体实现和工程取舍，不能把经验规则说成绝对结论。 这道题的关键是把“函数式 setState 何时必须使用？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在中大型 React 产品、数据流和交互性能优化中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果数据规模、并发量或团队人数扩大一个数量级，原方案的首个瓶颈在哪里，如何用指标验证？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [React 官方文档](https://react.dev/reference/react)、[React 状态管理指南](https://react.dev/learn/managing-state)（校验日期：2026-07-17）

## Q6：key 如何影响组件状态？

**短回答：**

React 用父级位置、元素类型和 key 判断身份；key 改变会卸载旧实例并创建新实例，可用于有意重置状态。

**原理：**

先给结论，再用一条真实调用链解释为什么，最后说明如何测量或验证。 这道题的关键是把“key 如何影响组件状态？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在中大型 React 产品、数据流和交互性能优化中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果面试官加入失败重试、版本冲突或安全攻击条件，哪些保证仍成立，哪些需要额外机制？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [React 官方文档](https://react.dev/reference/react)、[React 状态管理指南](https://react.dev/learn/managing-state)（校验日期：2026-07-17）

## Q7：为什么不能在 render 中直接修改 state？

**短回答：**

会在渲染过程中再次安排渲染，可能形成循环并破坏纯渲染假设；应在事件、effect 或外部订阅回调中更新。

**原理：**

先说明输入、内部机制和可观察结果，再补充失败边界。 这道题的关键是把“为什么不能在 render 中直接修改 state？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在中大型 React 产品、数据流和交互性能优化中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果数据规模、并发量或团队人数扩大一个数量级，原方案的首个瓶颈在哪里，如何用指标验证？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [React 官方文档](https://react.dev/reference/react)、[React 状态管理指南](https://react.dev/learn/managing-state)（校验日期：2026-07-17）

## Q8：受控组件和非受控组件如何选择？

**短回答：**

受控输入由 React state 驱动，适合联动校验；非受控由 DOM 保存即时值，适合简单表单或文件输入，提交时读取 ref。

**原理：**

回答时区分规范语义、具体实现和工程取舍，不能把经验规则说成绝对结论。 这道题的关键是把“受控组件和非受控组件如何选择？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在中大型 React 产品、数据流和交互性能优化中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果面试官加入失败重试、版本冲突或安全攻击条件，哪些保证仍成立，哪些需要额外机制？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [React 官方文档](https://react.dev/reference/react)、[React 状态管理指南](https://react.dev/learn/managing-state)（校验日期：2026-07-17）

## Q9：Context 的适用边界是什么？

**短回答：**

它适合跨层传递相对稳定的依赖；value 变化会通知所有消费方，高频复杂业务状态需拆分 context 或用外部 store。

**原理：**

先给结论，再用一条真实调用链解释为什么，最后说明如何测量或验证。 这道题的关键是把“Context 的适用边界是什么？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在中大型 React 产品、数据流和交互性能优化中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果数据规模、并发量或团队人数扩大一个数量级，原方案的首个瓶颈在哪里，如何用指标验证？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [React 官方文档](https://react.dev/reference/react)、[React 状态管理指南](https://react.dev/learn/managing-state)（校验日期：2026-07-17）

## Q10：Portal 为什么仍能按 React 树冒泡事件？

**短回答：**

Portal 只改变 DOM 放置位置，组件父子关系仍在 React 树中，因此 context 和合成事件沿 React 树传播。

**原理：**

先说明输入、内部机制和可观察结果，再补充失败边界。 这道题的关键是把“Portal 为什么仍能按 React 树冒泡事件？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在中大型 React 产品、数据流和交互性能优化中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果面试官加入失败重试、版本冲突或安全攻击条件，哪些保证仍成立，哪些需要额外机制？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [React 官方文档](https://react.dev/reference/react)、[React 状态管理指南](https://react.dev/learn/managing-state)（校验日期：2026-07-17）

# Hooks 与副作用

## Q11：Hooks 为什么不能写在条件语句里？

**短回答：**

React 依赖每次渲染相同的 Hook 调用顺序把状态槽与调用对应，条件调用会让后续槽位错位。

**原理：**

回答时区分规范语义、具体实现和工程取舍，不能把经验规则说成绝对结论。 这道题的关键是把“Hooks 为什么不能写在条件语句里？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在中大型 React 产品、数据流和交互性能优化中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果数据规模、并发量或团队人数扩大一个数量级，原方案的首个瓶颈在哪里，如何用指标验证？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [React 官方文档](https://react.dev/reference/react)、[React 状态管理指南](https://react.dev/learn/managing-state)（校验日期：2026-07-17）

## Q12：useEffect 的职责是什么？

**短回答：**

它用于让组件与网络、订阅、DOM 插件等外部系统同步，不应拿来计算可由 props/state 直接推导的数据。

**原理：**

先给结论，再用一条真实调用链解释为什么，最后说明如何测量或验证。 这道题的关键是把“useEffect 的职责是什么？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在中大型 React 产品、数据流和交互性能优化中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果面试官加入失败重试、版本冲突或安全攻击条件，哪些保证仍成立，哪些需要额外机制？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [React 官方文档](https://react.dev/reference/react)、[React 状态管理指南](https://react.dev/learn/managing-state)（校验日期：2026-07-17）

## Q13：依赖数组如何判断？

**短回答：**

effect 读取的所有响应式值都应列为依赖；由组件外常量或稳定 Hook 保证不变的值可不列，不能靠手工删依赖压制 lint。

**原理：**

先说明输入、内部机制和可观察结果，再补充失败边界。 这道题的关键是把“依赖数组如何判断？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在中大型 React 产品、数据流和交互性能优化中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果数据规模、并发量或团队人数扩大一个数量级，原方案的首个瓶颈在哪里，如何用指标验证？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [React 官方文档](https://react.dev/reference/react)、[React 状态管理指南](https://react.dev/learn/managing-state)（校验日期：2026-07-17）

## Q14：effect cleanup 在何时执行？

**短回答：**

依赖变化重新执行前先清理上一次，组件卸载时也清理；开发 Strict Mode 还会额外执行一次 setup-cleanup 用于暴露缺陷。

**原理：**

回答时区分规范语义、具体实现和工程取舍，不能把经验规则说成绝对结论。 这道题的关键是把“effect cleanup 在何时执行？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在中大型 React 产品、数据流和交互性能优化中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果面试官加入失败重试、版本冲突或安全攻击条件，哪些保证仍成立，哪些需要额外机制？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [React 官方文档](https://react.dev/reference/react)、[React 状态管理指南](https://react.dev/learn/managing-state)（校验日期：2026-07-17）

## Q15：useLayoutEffect 和 useEffect 有何区别？

**短回答：**

layout effect 在 DOM 提交后、浏览器绘制前同步执行，适合测量布局；它会阻塞绘制，普通副作用优先 useEffect。

**原理：**

先给结论，再用一条真实调用链解释为什么，最后说明如何测量或验证。 这道题的关键是把“useLayoutEffect 和 useEffect 有何区别？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在中大型 React 产品、数据流和交互性能优化中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果数据规模、并发量或团队人数扩大一个数量级，原方案的首个瓶颈在哪里，如何用指标验证？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [React 官方文档](https://react.dev/reference/react)、[React 状态管理指南](https://react.dev/learn/managing-state)（校验日期：2026-07-17）

## Q16：useMemo 和 useCallback 是语义保证吗？

**短回答：**

它们是性能优化缓存，依赖变化会失效，React 也可能因实现需要丢弃缓存；业务正确性不能依赖缓存永久存在。

**原理：**

先说明输入、内部机制和可观察结果，再补充失败边界。 这道题的关键是把“useMemo 和 useCallback 是语义保证吗？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在中大型 React 产品、数据流和交互性能优化中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果面试官加入失败重试、版本冲突或安全攻击条件，哪些保证仍成立，哪些需要额外机制？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [React 官方文档](https://react.dev/reference/react)、[React 状态管理指南](https://react.dev/learn/managing-state)（校验日期：2026-07-17）

## Q17：useRef 为什么修改后不触发渲染？

**短回答：**

ref 是跨渲染保持身份的可变容器，React 不跟踪 current 变化；适合 DOM、定时器和不参与展示的实例数据。

**原理：**

回答时区分规范语义、具体实现和工程取舍，不能把经验规则说成绝对结论。 这道题的关键是把“useRef 为什么修改后不触发渲染？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在中大型 React 产品、数据流和交互性能优化中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果数据规模、并发量或团队人数扩大一个数量级，原方案的首个瓶颈在哪里，如何用指标验证？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [React 官方文档](https://react.dev/reference/react)、[React 状态管理指南](https://react.dev/learn/managing-state)（校验日期：2026-07-17）

## Q18：闭包导致 stale state 如何处理？

**短回答：**

使用函数式更新、正确 effect 依赖或 ref 保存最新值；不要通过禁用依赖规则掩盖闭包读取旧快照的问题。

**原理：**

先给结论，再用一条真实调用链解释为什么，最后说明如何测量或验证。 这道题的关键是把“闭包导致 stale state 如何处理？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在中大型 React 产品、数据流和交互性能优化中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果面试官加入失败重试、版本冲突或安全攻击条件，哪些保证仍成立，哪些需要额外机制？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [React 官方文档](https://react.dev/reference/react)、[React 状态管理指南](https://react.dev/learn/managing-state)（校验日期：2026-07-17）

## Q19：自定义 Hook 的价值是什么？

**短回答：**

复用有状态逻辑而非共享状态实例，每次调用都有独立 state；名称以 use 开头让 lint 能检查 Hook 规则。

**原理：**

先说明输入、内部机制和可观察结果，再补充失败边界。 这道题的关键是把“自定义 Hook 的价值是什么？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在中大型 React 产品、数据流和交互性能优化中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果数据规模、并发量或团队人数扩大一个数量级，原方案的首个瓶颈在哪里，如何用指标验证？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [React 官方文档](https://react.dev/reference/react)、[React 状态管理指南](https://react.dev/learn/managing-state)（校验日期：2026-07-17）

## Q20：useSyncExternalStore 解决什么问题？

**短回答：**

它规定外部 store 的订阅与快照读取契约，使并发渲染下多个组件看到一致快照，并支持 SSR 的服务端快照。

**原理：**

回答时区分规范语义、具体实现和工程取舍，不能把经验规则说成绝对结论。 这道题的关键是把“useSyncExternalStore 解决什么问题？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在中大型 React 产品、数据流和交互性能优化中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果面试官加入失败重试、版本冲突或安全攻击条件，哪些保证仍成立，哪些需要额外机制？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [React 官方文档](https://react.dev/reference/react)、[React 状态管理指南](https://react.dev/learn/managing-state)（校验日期：2026-07-17）

# 状态架构与数据请求

## Q21：如何判断状态应该放在哪里？

**短回答：**

优先放在需要它的最近共同父组件；只有跨远距离、跨页面或服务端缓存才提升到 context、store 或请求缓存层。

**原理：**

先给结论，再用一条真实调用链解释为什么，最后说明如何测量或验证。 这道题的关键是把“如何判断状态应该放在哪里？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在中大型 React 产品、数据流和交互性能优化中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果数据规模、并发量或团队人数扩大一个数量级，原方案的首个瓶颈在哪里，如何用指标验证？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [React 官方文档](https://react.dev/reference/react)、[React 状态管理指南](https://react.dev/learn/managing-state)（校验日期：2026-07-17）

## Q22：为什么不应把可推导值存进 state？

**短回答：**

源数据变化时容易忘记同步形成双源真相；在渲染中计算，昂贵时再用 memo 优化。

**原理：**

先说明输入、内部机制和可观察结果，再补充失败边界。 这道题的关键是把“为什么不应把可推导值存进 state？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在中大型 React 产品、数据流和交互性能优化中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果面试官加入失败重试、版本冲突或安全攻击条件，哪些保证仍成立，哪些需要额外机制？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [React 官方文档](https://react.dev/reference/react)、[React 状态管理指南](https://react.dev/learn/managing-state)（校验日期：2026-07-17）

## Q23：useReducer 适合什么场景？

**短回答：**

状态转换由多种事件驱动、字段相关或需要可测试的状态机时，reducer 把事件与纯转换集中起来。

**原理：**

回答时区分规范语义、具体实现和工程取舍，不能把经验规则说成绝对结论。 这道题的关键是把“useReducer 适合什么场景？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在中大型 React 产品、数据流和交互性能优化中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果数据规模、并发量或团队人数扩大一个数量级，原方案的首个瓶颈在哪里，如何用指标验证？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [React 官方文档](https://react.dev/reference/react)、[React 状态管理指南](https://react.dev/learn/managing-state)（校验日期：2026-07-17）

## Q24：Context 加 reducer 能替代所有状态库吗？

**短回答：**

可覆盖中等规模全局状态，但缺少细粒度订阅、持久化、DevTools 等能力；高频大状态需评估专用 store。

**原理：**

先给结论，再用一条真实调用链解释为什么，最后说明如何测量或验证。 这道题的关键是把“Context 加 reducer 能替代所有状态库吗？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在中大型 React 产品、数据流和交互性能优化中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果面试官加入失败重试、版本冲突或安全攻击条件，哪些保证仍成立，哪些需要额外机制？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [React 官方文档](https://react.dev/reference/react)、[React 状态管理指南](https://react.dev/learn/managing-state)（校验日期：2026-07-17）

## Q25：客户端请求为什么要处理竞态？

**短回答：**

参数快速变化时旧请求可能晚返回覆盖新结果；cleanup 中取消或用请求序号只接收当前响应。

**原理：**

先说明输入、内部机制和可观察结果，再补充失败边界。 这道题的关键是把“客户端请求为什么要处理竞态？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在中大型 React 产品、数据流和交互性能优化中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果数据规模、并发量或团队人数扩大一个数量级，原方案的首个瓶颈在哪里，如何用指标验证？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [React 官方文档](https://react.dev/reference/react)、[React 状态管理指南](https://react.dev/learn/managing-state)（校验日期：2026-07-17）

## Q26：服务端状态和客户端状态有什么区别？

**短回答：**

服务端状态有缓存、过期、重试和并发一致性，客户端状态描述本地交互；应避免用普通全局 store 手写完整请求缓存。

**原理：**

回答时区分规范语义、具体实现和工程取舍，不能把经验规则说成绝对结论。 这道题的关键是把“服务端状态和客户端状态有什么区别？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在中大型 React 产品、数据流和交互性能优化中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果面试官加入失败重试、版本冲突或安全攻击条件，哪些保证仍成立，哪些需要额外机制？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [React 官方文档](https://react.dev/reference/react)、[React 状态管理指南](https://react.dev/learn/managing-state)（校验日期：2026-07-17）

## Q27：乐观更新如何保证失败可恢复？

**短回答：**

提交前保存可回滚快照或逆操作，先更新 UI，服务器失败时回滚并提示；并发更新需使用版本或请求序号。

**原理：**

先给结论，再用一条真实调用链解释为什么，最后说明如何测量或验证。 这道题的关键是把“乐观更新如何保证失败可恢复？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在中大型 React 产品、数据流和交互性能优化中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果数据规模、并发量或团队人数扩大一个数量级，原方案的首个瓶颈在哪里，如何用指标验证？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [React 官方文档](https://react.dev/reference/react)、[React 状态管理指南](https://react.dev/learn/managing-state)（校验日期：2026-07-17）

## Q28：表单状态如何避免每次输入重渲染整页？

**短回答：**

把状态下沉到字段或表单边界，使用非受控注册、细粒度订阅和 memo，避免顶层对象每次都产生全树新 props。

**原理：**

先说明输入、内部机制和可观察结果，再补充失败边界。 这道题的关键是把“表单状态如何避免每次输入重渲染整页？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在中大型 React 产品、数据流和交互性能优化中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果面试官加入失败重试、版本冲突或安全攻击条件，哪些保证仍成立，哪些需要额外机制？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [React 官方文档](https://react.dev/reference/react)、[React 状态管理指南](https://react.dev/learn/managing-state)（校验日期：2026-07-17）

## Q29：URL 为什么也可以是状态？

**短回答：**

筛选、分页和当前资源若需分享、刷新恢复或前进后退，应编码在 URL；临时输入和敏感信息不适合放 URL。

**原理：**

回答时区分规范语义、具体实现和工程取舍，不能把经验规则说成绝对结论。 这道题的关键是把“URL 为什么也可以是状态？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在中大型 React 产品、数据流和交互性能优化中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果数据规模、并发量或团队人数扩大一个数量级，原方案的首个瓶颈在哪里，如何用指标验证？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [React 官方文档](https://react.dev/reference/react)、[React 状态管理指南](https://react.dev/learn/managing-state)（校验日期：2026-07-17）

## Q30：状态机比多个 boolean 好在哪里？

**短回答：**

显式枚举合法状态与事件，避免 loading、success、error 等布尔组合出现不可能状态，并便于测试转移路径。

**原理：**

先给结论，再用一条真实调用链解释为什么，最后说明如何测量或验证。 这道题的关键是把“状态机比多个 boolean 好在哪里？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在中大型 React 产品、数据流和交互性能优化中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果面试官加入失败重试、版本冲突或安全攻击条件，哪些保证仍成立，哪些需要额外机制？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [React 官方文档](https://react.dev/reference/react)、[React 状态管理指南](https://react.dev/learn/managing-state)（校验日期：2026-07-17）

# 并发、性能与 Fiber

## Q31：Fiber 架构解决什么问题？

**短回答：**

它把渲染工作拆成可暂停和恢复的单元，并为更新赋优先级，使 React 能在提交前调度计算；提交阶段仍保持同步一致。

**原理：**

先说明输入、内部机制和可观察结果，再补充失败边界。 这道题的关键是把“Fiber 架构解决什么问题？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在中大型 React 产品、数据流和交互性能优化中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果数据规模、并发量或团队人数扩大一个数量级，原方案的首个瓶颈在哪里，如何用指标验证？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [React 官方文档](https://react.dev/reference/react)、[React 状态管理指南](https://react.dev/learn/managing-state)（校验日期：2026-07-17）

## Q32：render phase 和 commit phase 有何区别？

**短回答：**

render 计算下一棵树，可被中断或重做；commit 把变更应用到 DOM 并运行布局 effect，不能向用户呈现半成品。

**原理：**

回答时区分规范语义、具体实现和工程取舍，不能把经验规则说成绝对结论。 这道题的关键是把“render phase 和 commit phase 有何区别？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在中大型 React 产品、数据流和交互性能优化中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果面试官加入失败重试、版本冲突或安全攻击条件，哪些保证仍成立，哪些需要额外机制？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [React 官方文档](https://react.dev/reference/react)、[React 状态管理指南](https://react.dev/learn/managing-state)（校验日期：2026-07-17）

## Q33：startTransition 适合什么更新？

**短回答：**

把非紧急且可中断的更新标为 transition，例如大列表筛选；输入框自身值等紧急反馈仍应同步更新。

**原理：**

先给结论，再用一条真实调用链解释为什么，最后说明如何测量或验证。 这道题的关键是把“startTransition 适合什么更新？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在中大型 React 产品、数据流和交互性能优化中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果数据规模、并发量或团队人数扩大一个数量级，原方案的首个瓶颈在哪里，如何用指标验证？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [React 官方文档](https://react.dev/reference/react)、[React 状态管理指南](https://react.dev/learn/managing-state)（校验日期：2026-07-17）

## Q34：useDeferredValue 与防抖有何区别？

**短回答：**

deferred 保留旧值并让新渲染低优先级推进，不固定等待时间；防抖按时间减少触发次数，网络请求常仍需显式防抖。

**原理：**

先说明输入、内部机制和可观察结果，再补充失败边界。 这道题的关键是把“useDeferredValue 与防抖有何区别？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在中大型 React 产品、数据流和交互性能优化中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果面试官加入失败重试、版本冲突或安全攻击条件，哪些保证仍成立，哪些需要额外机制？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [React 官方文档](https://react.dev/reference/react)、[React 状态管理指南](https://react.dev/learn/managing-state)（校验日期：2026-07-17）

## Q35：React.memo 为什么可能无效？

**短回答：**

默认浅比较 props；父组件每次创建新对象、数组或函数会破坏引用相等，且比较成本可能高于重新渲染。

**原理：**

回答时区分规范语义、具体实现和工程取舍，不能把经验规则说成绝对结论。 这道题的关键是把“React.memo 为什么可能无效？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在中大型 React 产品、数据流和交互性能优化中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果数据规模、并发量或团队人数扩大一个数量级，原方案的首个瓶颈在哪里，如何用指标验证？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [React 官方文档](https://react.dev/reference/react)、[React 状态管理指南](https://react.dev/learn/managing-state)（校验日期：2026-07-17）

## Q36：如何用 Profiler 定位 React 性能问题？

**短回答：**

记录交互，查看提交耗时和哪些组件重复渲染，再检查状态位置、props 身份和昂贵计算，用修复前后数据验证。

**原理：**

先给结论，再用一条真实调用链解释为什么，最后说明如何测量或验证。 这道题的关键是把“如何用 Profiler 定位 React 性能问题？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在中大型 React 产品、数据流和交互性能优化中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果面试官加入失败重试、版本冲突或安全攻击条件，哪些保证仍成立，哪些需要额外机制？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [React 官方文档](https://react.dev/reference/react)、[React 状态管理指南](https://react.dev/learn/managing-state)（校验日期：2026-07-17）

## Q37：大型列表如何优化？

**短回答：**

使用稳定 key、窗口化、分页或增量加载，把行组件隔离，并避免每行订阅整个全局状态。

**原理：**

先说明输入、内部机制和可观察结果，再补充失败边界。 这道题的关键是把“大型列表如何优化？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在中大型 React 产品、数据流和交互性能优化中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果数据规模、并发量或团队人数扩大一个数量级，原方案的首个瓶颈在哪里，如何用指标验证？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [React 官方文档](https://react.dev/reference/react)、[React 状态管理指南](https://react.dev/learn/managing-state)（校验日期：2026-07-17）

## Q38：Suspense 是否等于通用数据请求方案？

**短回答：**

Suspense 协调“等待中的渲染”与 fallback，数据源仍需框架或支持缓存与抛出 promise 的集成，不能直接包任意 useEffect 请求。

**原理：**

回答时区分规范语义、具体实现和工程取舍，不能把经验规则说成绝对结论。 这道题的关键是把“Suspense 是否等于通用数据请求方案？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在中大型 React 产品、数据流和交互性能优化中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果面试官加入失败重试、版本冲突或安全攻击条件，哪些保证仍成立，哪些需要额外机制？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [React 官方文档](https://react.dev/reference/react)、[React 状态管理指南](https://react.dev/learn/managing-state)（校验日期：2026-07-17）

## Q39：错误边界能捕获哪些错误？

**短回答：**

它捕获子树渲染、生命周期和构造错误，不捕获事件处理器、异步回调、服务端渲染或边界自身错误。

**原理：**

先给结论，再用一条真实调用链解释为什么，最后说明如何测量或验证。 这道题的关键是把“错误边界能捕获哪些错误？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在中大型 React 产品、数据流和交互性能优化中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果数据规模、并发量或团队人数扩大一个数量级，原方案的首个瓶颈在哪里，如何用指标验证？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [React 官方文档](https://react.dev/reference/react)、[React 状态管理指南](https://react.dev/learn/managing-state)（校验日期：2026-07-17）

## Q40：Strict Mode 为什么会让 effect 开发环境执行两次？

**短回答：**

开发模式用额外 setup-cleanup 周期检查副作用是否可重入且清理完整，生产环境不会做这次诊断执行。

**原理：**

先说明输入、内部机制和可观察结果，再补充失败边界。 这道题的关键是把“Strict Mode 为什么会让 effect 开发环境执行两次？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在中大型 React 产品、数据流和交互性能优化中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果面试官加入失败重试、版本冲突或安全攻击条件，哪些保证仍成立，哪些需要额外机制？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [React 官方文档](https://react.dev/reference/react)、[React 状态管理指南](https://react.dev/learn/managing-state)（校验日期：2026-07-17）

# 工程测试与安全

## Q41：React 测试为什么更关注用户行为？

**短回答：**

通过角色、文本和交互断言可观察结果，比断言内部 state 或实例方法更抗重构，也更接近可访问性使用方式。

**原理：**

回答时区分规范语义、具体实现和工程取舍，不能把经验规则说成绝对结论。 这道题的关键是把“React 测试为什么更关注用户行为？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在中大型 React 产品、数据流和交互性能优化中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果数据规模、并发量或团队人数扩大一个数量级，原方案的首个瓶颈在哪里，如何用指标验证？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [React 官方文档](https://react.dev/reference/react)、[React 状态管理指南](https://react.dev/learn/managing-state)（校验日期：2026-07-17）

## Q42：单元、集成和 E2E 测试如何分工？

**短回答：**

纯函数与边界逻辑做单元测试，组件和 API 协作用集成测试，少量关键业务链路用 E2E 覆盖真实浏览器。

**原理：**

先给结论，再用一条真实调用链解释为什么，最后说明如何测量或验证。 这道题的关键是把“单元、集成和 E2E 测试如何分工？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在中大型 React 产品、数据流和交互性能优化中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果面试官加入失败重试、版本冲突或安全攻击条件，哪些保证仍成立，哪些需要额外机制？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [React 官方文档](https://react.dev/reference/react)、[React 状态管理指南](https://react.dev/learn/managing-state)（校验日期：2026-07-17）

## Q43：SSR hydration mismatch 如何排查？

**短回答：**

比较服务端与客户端首屏输入，排查随机数、时间、浏览器 API、无效 HTML 和数据版本差异，保证首次输出确定。

**原理：**

先说明输入、内部机制和可观察结果，再补充失败边界。 这道题的关键是把“SSR hydration mismatch 如何排查？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在中大型 React 产品、数据流和交互性能优化中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果数据规模、并发量或团队人数扩大一个数量级，原方案的首个瓶颈在哪里，如何用指标验证？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [React 官方文档](https://react.dev/reference/react)、[React 状态管理指南](https://react.dev/learn/managing-state)（校验日期：2026-07-17）

## Q44：React 中如何防 XSS？

**短回答：**

普通 JSX 文本默认转义，dangerouslySetInnerHTML 绕过保护；富文本必须在可信边界前消毒并配合 CSP。

**原理：**

回答时区分规范语义、具体实现和工程取舍，不能把经验规则说成绝对结论。 这道题的关键是把“React 中如何防 XSS？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在中大型 React 产品、数据流和交互性能优化中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果面试官加入失败重试、版本冲突或安全攻击条件，哪些保证仍成立，哪些需要额外机制？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [React 官方文档](https://react.dev/reference/react)、[React 状态管理指南](https://react.dev/learn/managing-state)（校验日期：2026-07-17）

## Q45：代码分割应该以什么边界实施？

**短回答：**

优先路由和低频重功能，用 lazy 与 Suspense 提供加载状态；过度碎片会增加请求与瀑布，需结合打包分析。

**原理：**

先给结论，再用一条真实调用链解释为什么，最后说明如何测量或验证。 这道题的关键是把“代码分割应该以什么边界实施？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在中大型 React 产品、数据流和交互性能优化中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果数据规模、并发量或团队人数扩大一个数量级，原方案的首个瓶颈在哪里，如何用指标验证？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [React 官方文档](https://react.dev/reference/react)、[React 状态管理指南](https://react.dev/learn/managing-state)（校验日期：2026-07-17）

## Q46：如何设计可访问的复合组件？

**短回答：**

先使用语义元素，再实现键盘导航、焦点管理和 ARIA 状态；弹窗打开聚焦、关闭还原焦点并限制背景交互。

**原理：**

先说明输入、内部机制和可观察结果，再补充失败边界。 这道题的关键是把“如何设计可访问的复合组件？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在中大型 React 产品、数据流和交互性能优化中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果面试官加入失败重试、版本冲突或安全攻击条件，哪些保证仍成立，哪些需要额外机制？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [React 官方文档](https://react.dev/reference/react)、[React 状态管理指南](https://react.dev/learn/managing-state)（校验日期：2026-07-17）

## Q47：React 组件库如何避免样式和状态耦合？

**短回答：**

把无样式行为、设计 token 与视觉组件分层，受控/非受控 API 一致，并为组合、主题和可访问性写契约测试。

**原理：**

回答时区分规范语义、具体实现和工程取舍，不能把经验规则说成绝对结论。 这道题的关键是把“React 组件库如何避免样式和状态耦合？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在中大型 React 产品、数据流和交互性能优化中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果数据规模、并发量或团队人数扩大一个数量级，原方案的首个瓶颈在哪里，如何用指标验证？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [React 官方文档](https://react.dev/reference/react)、[React 状态管理指南](https://react.dev/learn/managing-state)（校验日期：2026-07-17）

## Q48：前端错误监控应记录哪些 React 上下文？

**短回答：**

错误堆栈、组件栈、路由、版本、用户动作和请求关联 ID；上报前脱敏并使用 source map 还原。

**原理：**

先给结论，再用一条真实调用链解释为什么，最后说明如何测量或验证。 这道题的关键是把“前端错误监控应记录哪些 React 上下文？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在中大型 React 产品、数据流和交互性能优化中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果面试官加入失败重试、版本冲突或安全攻击条件，哪些保证仍成立，哪些需要额外机制？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [React 官方文档](https://react.dev/reference/react)、[React 状态管理指南](https://react.dev/learn/managing-state)（校验日期：2026-07-17）

## Q49：如何渐进迁移旧 React 项目？

**短回答：**

先建立测试与构建基线，按叶子组件或路由迁移，使用适配层保持数据契约，避免一次重写所有业务。

**原理：**

先说明输入、内部机制和可观察结果，再补充失败边界。 这道题的关键是把“如何渐进迁移旧 React 项目？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在中大型 React 产品、数据流和交互性能优化中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果数据规模、并发量或团队人数扩大一个数量级，原方案的首个瓶颈在哪里，如何用指标验证？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [React 官方文档](https://react.dev/reference/react)、[React 状态管理指南](https://react.dev/learn/managing-state)（校验日期：2026-07-17）

## Q50：面试时如何解释 Vue 与 React 的差异？

**短回答：**

从更新粒度、编译优化、状态模型和生态约束比较，并回到项目场景；不要简化为“一个双向绑定、一个虚拟 DOM”。

**原理：**

回答时区分规范语义、具体实现和工程取舍，不能把经验规则说成绝对结论。 这道题的关键是把“面试时如何解释 Vue 与 React 的差异？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在中大型 React 产品、数据流和交互性能优化中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果面试官加入失败重试、版本冲突或安全攻击条件，哪些保证仍成立，哪些需要额外机制？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [React 官方文档](https://react.dev/reference/react)、[React 状态管理指南](https://react.dev/learn/managing-state)（校验日期：2026-07-17）

# SQL、MySQL、Redis 与缓存

# SQL 与索引设计

## Q1：数据库索引的本质是什么？

**短回答：**

索引是按键组织的辅助数据结构，以额外存储和写放大换取更少的数据页读取；是否有效取决于选择性、查询模式和成本估算。

**原理：**

先说明输入、内部机制和可观察结果，再补充失败边界。 这道题的关键是把“数据库索引的本质是什么？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在订单、库存、题库和高并发缓存的数据设计中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果数据规模、并发量或团队人数扩大一个数量级，原方案的首个瓶颈在哪里，如何用指标验证？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [MySQL 8.4 参考手册](https://dev.mysql.com/doc/refman/8.4/en/)、[MySQL InnoDB 事务模型](https://dev.mysql.com/doc/refman/8.4/en/innodb-transaction-model.html)（校验日期：2026-07-17）

## Q2：B+Tree 为什么适合范围查询？

**短回答：**

内部节点保存有序分隔键，叶子按键有序并相连，等值查找层数稳定，范围扫描可从起点顺序遍历叶子。

**原理：**

回答时区分规范语义、具体实现和工程取舍，不能把经验规则说成绝对结论。 这道题的关键是把“B+Tree 为什么适合范围查询？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在订单、库存、题库和高并发缓存的数据设计中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果面试官加入失败重试、版本冲突或安全攻击条件，哪些保证仍成立，哪些需要额外机制？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [MySQL 8.4 参考手册](https://dev.mysql.com/doc/refman/8.4/en/)、[MySQL InnoDB 事务模型](https://dev.mysql.com/doc/refman/8.4/en/innodb-transaction-model.html)（校验日期：2026-07-17）

## Q3：聚簇索引和二级索引有什么区别？

**短回答：**

InnoDB 聚簇索引叶子保存整行，二级索引叶子保存索引列和主键；二级索引取非覆盖列通常需回表。

**原理：**

先给结论，再用一条真实调用链解释为什么，最后说明如何测量或验证。 这道题的关键是把“聚簇索引和二级索引有什么区别？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在订单、库存、题库和高并发缓存的数据设计中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果数据规模、并发量或团队人数扩大一个数量级，原方案的首个瓶颈在哪里，如何用指标验证？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [MySQL 8.4 参考手册](https://dev.mysql.com/doc/refman/8.4/en/)、[MySQL InnoDB 事务模型](https://dev.mysql.com/doc/refman/8.4/en/innodb-transaction-model.html)（校验日期：2026-07-17）

## Q4：联合索引的最左前缀是什么？

**短回答：**

索引按列定义顺序排序，能使用从第一列开始的连续约束；跳过前导列通常无法直接定位后续列范围。

**原理：**

先说明输入、内部机制和可观察结果，再补充失败边界。 这道题的关键是把“联合索引的最左前缀是什么？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在订单、库存、题库和高并发缓存的数据设计中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果面试官加入失败重试、版本冲突或安全攻击条件，哪些保证仍成立，哪些需要额外机制？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [MySQL 8.4 参考手册](https://dev.mysql.com/doc/refman/8.4/en/)、[MySQL InnoDB 事务模型](https://dev.mysql.com/doc/refman/8.4/en/innodb-transaction-model.html)（校验日期：2026-07-17）

## Q5：联合索引列顺序如何确定？

**短回答：**

先满足常见等值过滤，再考虑范围、排序和覆盖，结合真实查询与选择性；不是机械地把区分度最高列永远放第一。

**原理：**

回答时区分规范语义、具体实现和工程取舍，不能把经验规则说成绝对结论。 这道题的关键是把“联合索引列顺序如何确定？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在订单、库存、题库和高并发缓存的数据设计中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果数据规模、并发量或团队人数扩大一个数量级，原方案的首个瓶颈在哪里，如何用指标验证？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [MySQL 8.4 参考手册](https://dev.mysql.com/doc/refman/8.4/en/)、[MySQL InnoDB 事务模型](https://dev.mysql.com/doc/refman/8.4/en/innodb-transaction-model.html)（校验日期：2026-07-17）

## Q6：什么是覆盖索引？

**短回答：**

查询所需筛选和返回列都可从索引获得，无需回到聚簇索引读取整行，可减少随机 I/O，但索引更宽且写成本增加。

**原理：**

先给结论，再用一条真实调用链解释为什么，最后说明如何测量或验证。 这道题的关键是把“什么是覆盖索引？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在订单、库存、题库和高并发缓存的数据设计中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果面试官加入失败重试、版本冲突或安全攻击条件，哪些保证仍成立，哪些需要额外机制？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [MySQL 8.4 参考手册](https://dev.mysql.com/doc/refman/8.4/en/)、[MySQL InnoDB 事务模型](https://dev.mysql.com/doc/refman/8.4/en/innodb-transaction-model.html)（校验日期：2026-07-17）

## Q7：为什么对索引列使用函数可能失效？

**短回答：**

普通索引保存原值顺序，WHERE 对列计算后无法直接按原键定位；可改写范围条件或使用受支持的函数索引。

**原理：**

先说明输入、内部机制和可观察结果，再补充失败边界。 这道题的关键是把“为什么对索引列使用函数可能失效？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在订单、库存、题库和高并发缓存的数据设计中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果数据规模、并发量或团队人数扩大一个数量级，原方案的首个瓶颈在哪里，如何用指标验证？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [MySQL 8.4 参考手册](https://dev.mysql.com/doc/refman/8.4/en/)、[MySQL InnoDB 事务模型](https://dev.mysql.com/doc/refman/8.4/en/innodb-transaction-model.html)（校验日期：2026-07-17）

## Q8：LIKE 什么时候能使用索引？

**短回答：**

固定前缀如 'abc%' 可转为索引范围，前导通配符 '%abc' 通常无法用普通 B+Tree 定位起点。

**原理：**

回答时区分规范语义、具体实现和工程取舍，不能把经验规则说成绝对结论。 这道题的关键是把“LIKE 什么时候能使用索引？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在订单、库存、题库和高并发缓存的数据设计中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果面试官加入失败重试、版本冲突或安全攻击条件，哪些保证仍成立，哪些需要额外机制？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [MySQL 8.4 参考手册](https://dev.mysql.com/doc/refman/8.4/en/)、[MySQL InnoDB 事务模型](https://dev.mysql.com/doc/refman/8.4/en/innodb-transaction-model.html)（校验日期：2026-07-17）

## Q9：如何为给定 SQL 设计索引？

**短回答：**

先列出 WHERE 等值/范围、JOIN、ORDER BY 和返回列，按访问路径设计候选联合索引，再用 EXPLAIN 与真实数据验证扫描行数。

**原理：**

先给结论，再用一条真实调用链解释为什么，最后说明如何测量或验证。 这道题的关键是把“如何为给定 SQL 设计索引？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在订单、库存、题库和高并发缓存的数据设计中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果数据规模、并发量或团队人数扩大一个数量级，原方案的首个瓶颈在哪里，如何用指标验证？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [MySQL 8.4 参考手册](https://dev.mysql.com/doc/refman/8.4/en/)、[MySQL InnoDB 事务模型](https://dev.mysql.com/doc/refman/8.4/en/innodb-transaction-model.html)（校验日期：2026-07-17）

## Q10：索引为什么会拖慢写入？

**短回答：**

INSERT、UPDATE、DELETE 需维护每棵相关索引，可能发生页分裂、日志和缓存占用；重复或低收益索引应定期清理。

**原理：**

先说明输入、内部机制和可观察结果，再补充失败边界。 这道题的关键是把“索引为什么会拖慢写入？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在订单、库存、题库和高并发缓存的数据设计中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果面试官加入失败重试、版本冲突或安全攻击条件，哪些保证仍成立，哪些需要额外机制？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [MySQL 8.4 参考手册](https://dev.mysql.com/doc/refman/8.4/en/)、[MySQL InnoDB 事务模型](https://dev.mysql.com/doc/refman/8.4/en/innodb-transaction-model.html)（校验日期：2026-07-17）

# 查询优化与建模

## Q11：EXPLAIN 主要看哪些信息？

**短回答：**

关注访问类型、选择的 key、估算 rows、过滤比例、连接顺序和 Extra 中临时表/排序，并用 EXPLAIN ANALYZE 比较实际执行。

**原理：**

回答时区分规范语义、具体实现和工程取舍，不能把经验规则说成绝对结论。 这道题的关键是把“EXPLAIN 主要看哪些信息？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在订单、库存、题库和高并发缓存的数据设计中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果数据规模、并发量或团队人数扩大一个数量级，原方案的首个瓶颈在哪里，如何用指标验证？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [MySQL 8.4 参考手册](https://dev.mysql.com/doc/refman/8.4/en/)、[MySQL InnoDB 事务模型](https://dev.mysql.com/doc/refman/8.4/en/innodb-transaction-model.html)（校验日期：2026-07-17）

## Q12：为什么 SELECT * 可能有问题？

**短回答：**

读取无用列增加网络和页访问，阻碍覆盖索引并让接口隐式依赖表结构；应明确业务需要的列。

**原理：**

先给结论，再用一条真实调用链解释为什么，最后说明如何测量或验证。 这道题的关键是把“为什么 SELECT * 可能有问题？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在订单、库存、题库和高并发缓存的数据设计中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果面试官加入失败重试、版本冲突或安全攻击条件，哪些保证仍成立，哪些需要额外机制？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [MySQL 8.4 参考手册](https://dev.mysql.com/doc/refman/8.4/en/)、[MySQL InnoDB 事务模型](https://dev.mysql.com/doc/refman/8.4/en/innodb-transaction-model.html)（校验日期：2026-07-17）

## Q13：深分页为什么慢？

**短回答：**

LIMIT offset 仍需扫描并丢弃前面大量记录；可使用基于稳定排序键的 keyset pagination 从上次位置继续。

**原理：**

先说明输入、内部机制和可观察结果，再补充失败边界。 这道题的关键是把“深分页为什么慢？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在订单、库存、题库和高并发缓存的数据设计中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果数据规模、并发量或团队人数扩大一个数量级，原方案的首个瓶颈在哪里，如何用指标验证？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [MySQL 8.4 参考手册](https://dev.mysql.com/doc/refman/8.4/en/)、[Redis 官方文档](https://redis.io/docs/latest/)（校验日期：2026-07-17）

## Q14：JOIN 的驱动表如何选择？

**短回答：**

优化器根据统计信息估算成本，通常希望先缩小行数并让被驱动表连接键有索引；应通过执行计划验证而非死记小表驱动大表。

**原理：**

回答时区分规范语义、具体实现和工程取舍，不能把经验规则说成绝对结论。 这道题的关键是把“JOIN 的驱动表如何选择？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在订单、库存、题库和高并发缓存的数据设计中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果面试官加入失败重试、版本冲突或安全攻击条件，哪些保证仍成立，哪些需要额外机制？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [MySQL 8.4 参考手册](https://dev.mysql.com/doc/refman/8.4/en/)、[MySQL InnoDB 事务模型](https://dev.mysql.com/doc/refman/8.4/en/innodb-transaction-model.html)（校验日期：2026-07-17）

## Q15：N+1 查询如何发现和修复？

**短回答：**

列表后为每行再查一次会让请求数线性增长；用 SQL 日志定位，再批量 IN、JOIN、预加载或 DataLoader 合并。

**原理：**

先给结论，再用一条真实调用链解释为什么，最后说明如何测量或验证。 这道题的关键是把“N+1 查询如何发现和修复？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在订单、库存、题库和高并发缓存的数据设计中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果数据规模、并发量或团队人数扩大一个数量级，原方案的首个瓶颈在哪里，如何用指标验证？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [MySQL 8.4 参考手册](https://dev.mysql.com/doc/refman/8.4/en/)、[MySQL InnoDB 事务模型](https://dev.mysql.com/doc/refman/8.4/en/innodb-transaction-model.html)（校验日期：2026-07-17）

## Q16：规范化和反规范化如何权衡？

**短回答：**

规范化减少重复和更新异常，反规范化换取读性能与简单查询；冗余字段必须有明确同步和修复机制。

**原理：**

先说明输入、内部机制和可观察结果，再补充失败边界。 这道题的关键是把“规范化和反规范化如何权衡？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在订单、库存、题库和高并发缓存的数据设计中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果面试官加入失败重试、版本冲突或安全攻击条件，哪些保证仍成立，哪些需要额外机制？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [MySQL 8.4 参考手册](https://dev.mysql.com/doc/refman/8.4/en/)、[Redis 官方文档](https://redis.io/docs/latest/)（校验日期：2026-07-17）

## Q17：软删除字段如何影响索引？

**短回答：**

多数查询都带 deleted_at 条件，应纳入联合索引并评估低选择性；唯一约束和数据增长还需专门设计归档策略。

**原理：**

回答时区分规范语义、具体实现和工程取舍，不能把经验规则说成绝对结论。 这道题的关键是把“软删除字段如何影响索引？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在订单、库存、题库和高并发缓存的数据设计中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果数据规模、并发量或团队人数扩大一个数量级，原方案的首个瓶颈在哪里，如何用指标验证？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [MySQL 8.4 参考手册](https://dev.mysql.com/doc/refman/8.4/en/)、[MySQL InnoDB 事务模型](https://dev.mysql.com/doc/refman/8.4/en/innodb-transaction-model.html)（校验日期：2026-07-17）

## Q18：数据库为什么需要统计信息？

**短回答：**

优化器依赖基数和分布估算各访问路径成本，统计过期或数据倾斜会选错计划，需要更新并用真实执行验证。

**原理：**

先给结论，再用一条真实调用链解释为什么，最后说明如何测量或验证。 这道题的关键是把“数据库为什么需要统计信息？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在订单、库存、题库和高并发缓存的数据设计中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果面试官加入失败重试、版本冲突或安全攻击条件，哪些保证仍成立，哪些需要额外机制？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [MySQL 8.4 参考手册](https://dev.mysql.com/doc/refman/8.4/en/)、[MySQL InnoDB 事务模型](https://dev.mysql.com/doc/refman/8.4/en/innodb-transaction-model.html)（校验日期：2026-07-17）

## Q19：排序与分页如何建立稳定顺序？

**短回答：**

ORDER BY 末尾加入唯一键作为决定项，游标同时携带排序列和唯一键，避免同值记录在翻页时重漏。

**原理：**

先说明输入、内部机制和可观察结果，再补充失败边界。 这道题的关键是把“排序与分页如何建立稳定顺序？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在订单、库存、题库和高并发缓存的数据设计中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果数据规模、并发量或团队人数扩大一个数量级，原方案的首个瓶颈在哪里，如何用指标验证？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [MySQL 8.4 参考手册](https://dev.mysql.com/doc/refman/8.4/en/)、[Redis 官方文档](https://redis.io/docs/latest/)（校验日期：2026-07-17）

## Q20：JSON 字段何时不该使用？

**短回答：**

核心关系、频繁过滤和强约束字段应结构化；JSON 适合变化快的附加属性，但索引、校验和迁移更复杂。

**原理：**

回答时区分规范语义、具体实现和工程取舍，不能把经验规则说成绝对结论。 这道题的关键是把“JSON 字段何时不该使用？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在订单、库存、题库和高并发缓存的数据设计中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果面试官加入失败重试、版本冲突或安全攻击条件，哪些保证仍成立，哪些需要额外机制？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [MySQL 8.4 参考手册](https://dev.mysql.com/doc/refman/8.4/en/)、[MySQL InnoDB 事务模型](https://dev.mysql.com/doc/refman/8.4/en/innodb-transaction-model.html)（校验日期：2026-07-17）

# 事务、锁与一致性

## Q21：ACID 分别表示什么？

**短回答：**

原子性保证事务全成或全败，一致性由约束和业务共同维持，隔离性控制并发可见性，持久性保证提交结果在故障后可恢复。

**原理：**

先给结论，再用一条真实调用链解释为什么，最后说明如何测量或验证。 这道题的关键是把“ACID 分别表示什么？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在订单、库存、题库和高并发缓存的数据设计中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果数据规模、并发量或团队人数扩大一个数量级，原方案的首个瓶颈在哪里，如何用指标验证？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [MySQL 8.4 参考手册](https://dev.mysql.com/doc/refman/8.4/en/)、[MySQL InnoDB 事务模型](https://dev.mysql.com/doc/refman/8.4/en/innodb-transaction-model.html)（校验日期：2026-07-17）

## Q22：脏读、不可重复读和幻读是什么？

**短回答：**

分别是读到未提交数据、同一行两次结果变化、同一条件两次行集合变化；隔离级别和实现决定可见行为。

**原理：**

先说明输入、内部机制和可观察结果，再补充失败边界。 这道题的关键是把“脏读、不可重复读和幻读是什么？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在订单、库存、题库和高并发缓存的数据设计中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果面试官加入失败重试、版本冲突或安全攻击条件，哪些保证仍成立，哪些需要额外机制？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [MySQL 8.4 参考手册](https://dev.mysql.com/doc/refman/8.4/en/)、[Redis 官方文档](https://redis.io/docs/latest/)（校验日期：2026-07-17）

## Q23：MVCC 如何减少读写阻塞？

**短回答：**

行版本与事务可见性规则让普通快照读读取合适历史版本，写入仍需锁和冲突检测；长事务会阻碍旧版本回收。

**原理：**

回答时区分规范语义、具体实现和工程取舍，不能把经验规则说成绝对结论。 这道题的关键是把“MVCC 如何减少读写阻塞？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在订单、库存、题库和高并发缓存的数据设计中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果数据规模、并发量或团队人数扩大一个数量级，原方案的首个瓶颈在哪里，如何用指标验证？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [MySQL 8.4 参考手册](https://dev.mysql.com/doc/refman/8.4/en/)、[MySQL InnoDB 事务模型](https://dev.mysql.com/doc/refman/8.4/en/innodb-transaction-model.html)（校验日期：2026-07-17）

## Q24：MySQL 可重复读下快照读和当前读有何区别？

**短回答：**

普通 SELECT 通常读一致性快照，SELECT FOR UPDATE/UPDATE 读取当前版本并加锁，两者不能混为同一种可见性。

**原理：**

先给结论，再用一条真实调用链解释为什么，最后说明如何测量或验证。 这道题的关键是把“MySQL 可重复读下快照读和当前读有何区别？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在订单、库存、题库和高并发缓存的数据设计中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果面试官加入失败重试、版本冲突或安全攻击条件，哪些保证仍成立，哪些需要额外机制？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [MySQL 8.4 参考手册](https://dev.mysql.com/doc/refman/8.4/en/)、[MySQL InnoDB 事务模型](https://dev.mysql.com/doc/refman/8.4/en/innodb-transaction-model.html)（校验日期：2026-07-17）

## Q25：死锁是如何形成的？

**短回答：**

事务以不同顺序持有资源并等待对方，形成等待环；数据库会回滚一个事务，应用应缩短事务、统一锁顺序并安全重试。

**原理：**

先说明输入、内部机制和可观察结果，再补充失败边界。 这道题的关键是把“死锁是如何形成的？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在订单、库存、题库和高并发缓存的数据设计中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果数据规模、并发量或团队人数扩大一个数量级，原方案的首个瓶颈在哪里，如何用指标验证？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [MySQL 8.4 参考手册](https://dev.mysql.com/doc/refman/8.4/en/)、[MySQL InnoDB 事务模型](https://dev.mysql.com/doc/refman/8.4/en/innodb-transaction-model.html)（校验日期：2026-07-17）

## Q26：乐观锁和悲观锁如何选择？

**短回答：**

冲突少时用版本条件更新并检测 affected_rows，冲突高且临界区短时可用行锁；都需处理失败与超时。

**原理：**

回答时区分规范语义、具体实现和工程取舍，不能把经验规则说成绝对结论。 这道题的关键是把“乐观锁和悲观锁如何选择？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在订单、库存、题库和高并发缓存的数据设计中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果面试官加入失败重试、版本冲突或安全攻击条件，哪些保证仍成立，哪些需要额外机制？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [MySQL 8.4 参考手册](https://dev.mysql.com/doc/refman/8.4/en/)、[Redis 官方文档](https://redis.io/docs/latest/)（校验日期：2026-07-17）

## Q27：库存扣减如何防止超卖？

**短回答：**

让数据库执行带 available > 0 条件的原子 UPDATE，并检查影响行数；Redis 预扣仍需消息、补偿和数据库最终落账。

**原理：**

先给结论，再用一条真实调用链解释为什么，最后说明如何测量或验证。 这道题的关键是把“库存扣减如何防止超卖？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在订单、库存、题库和高并发缓存的数据设计中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果数据规模、并发量或团队人数扩大一个数量级，原方案的首个瓶颈在哪里，如何用指标验证？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [Redis 官方文档](https://redis.io/docs/latest/)、[Redis 数据类型](https://redis.io/docs/latest/develop/data-types/)（校验日期：2026-07-17）

## Q28：事务为什么不应包含远程 HTTP 调用？

**短回答：**

远程延迟和失败会延长锁持有且无法纳入本地原子提交；使用本地事务加 outbox、Saga 或补偿协调。

**原理：**

先说明输入、内部机制和可观察结果，再补充失败边界。 这道题的关键是把“事务为什么不应包含远程 HTTP 调用？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在订单、库存、题库和高并发缓存的数据设计中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果面试官加入失败重试、版本冲突或安全攻击条件，哪些保证仍成立，哪些需要额外机制？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [MySQL 8.4 参考手册](https://dev.mysql.com/doc/refman/8.4/en/)、[MySQL InnoDB 事务模型](https://dev.mysql.com/doc/refman/8.4/en/innodb-transaction-model.html)（校验日期：2026-07-17）

## Q29：什么是 outbox pattern？

**短回答：**

业务变更和待发布事件在同一数据库事务写入，独立发布器可靠投递并标记，避免“数据库已提交但消息未发”的间隙。

**原理：**

回答时区分规范语义、具体实现和工程取舍，不能把经验规则说成绝对结论。 这道题的关键是把“什么是 outbox pattern？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在订单、库存、题库和高并发缓存的数据设计中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果数据规模、并发量或团队人数扩大一个数量级，原方案的首个瓶颈在哪里，如何用指标验证？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [MySQL 8.4 参考手册](https://dev.mysql.com/doc/refman/8.4/en/)、[MySQL InnoDB 事务模型](https://dev.mysql.com/doc/refman/8.4/en/innodb-transaction-model.html)（校验日期：2026-07-17）

## Q30：隔离级别越高越好吗？

**短回答：**

更强隔离减少并发现象但可能增加锁、重试或降低吞吐，应按业务不变量选择并通过约束兜底。

**原理：**

先给结论，再用一条真实调用链解释为什么，最后说明如何测量或验证。 这道题的关键是把“隔离级别越高越好吗？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在订单、库存、题库和高并发缓存的数据设计中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果面试官加入失败重试、版本冲突或安全攻击条件，哪些保证仍成立，哪些需要额外机制？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [MySQL 8.4 参考手册](https://dev.mysql.com/doc/refman/8.4/en/)、[Redis 官方文档](https://redis.io/docs/latest/)（校验日期：2026-07-17）

# Redis 与缓存一致性

## Q31：Redis 为什么快？

**短回答：**

数据主要在内存，核心命令执行路径短，事件驱动处理网络，并有针对数据结构的高效实现；慢命令和大 key 仍会阻塞。

**原理：**

先说明输入、内部机制和可观察结果，再补充失败边界。 这道题的关键是把“Redis 为什么快？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在订单、库存、题库和高并发缓存的数据设计中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果数据规模、并发量或团队人数扩大一个数量级，原方案的首个瓶颈在哪里，如何用指标验证？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [Redis 官方文档](https://redis.io/docs/latest/)、[Redis 数据类型](https://redis.io/docs/latest/develop/data-types/)（校验日期：2026-07-17）

## Q32：String、Hash、List、Set、ZSet 如何选？

**短回答：**

按访问模式选择：值/计数、对象字段、队列序列、去重集合、有分数排序；不要把所有数据都序列化成大字符串。

**原理：**

回答时区分规范语义、具体实现和工程取舍，不能把经验规则说成绝对结论。 这道题的关键是把“String、Hash、List、Set、ZSet 如何选？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在订单、库存、题库和高并发缓存的数据设计中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果面试官加入失败重试、版本冲突或安全攻击条件，哪些保证仍成立，哪些需要额外机制？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [MySQL 8.4 参考手册](https://dev.mysql.com/doc/refman/8.4/en/)、[Redis 官方文档](https://redis.io/docs/latest/)（校验日期：2026-07-17）

## Q33：Redis 过期键如何删除？

**短回答：**

结合惰性删除和周期性抽样清理，不保证到期瞬间物理移除；内存策略还会在达到上限时驱逐键。

**原理：**

先给结论，再用一条真实调用链解释为什么，最后说明如何测量或验证。 这道题的关键是把“Redis 过期键如何删除？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在订单、库存、题库和高并发缓存的数据设计中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果数据规模、并发量或团队人数扩大一个数量级，原方案的首个瓶颈在哪里，如何用指标验证？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [Redis 官方文档](https://redis.io/docs/latest/)、[Redis 数据类型](https://redis.io/docs/latest/develop/data-types/)（校验日期：2026-07-17）

## Q34：RDB 和 AOF 的权衡是什么？

**短回答：**

RDB 是紧凑时间点快照、恢复快但可能丢最近数据；AOF 记录写命令、耐久可调但更大，常按恢复目标组合使用。

**原理：**

先说明输入、内部机制和可观察结果，再补充失败边界。 这道题的关键是把“RDB 和 AOF 的权衡是什么？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在订单、库存、题库和高并发缓存的数据设计中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果面试官加入失败重试、版本冲突或安全攻击条件，哪些保证仍成立，哪些需要额外机制？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [MySQL 8.4 参考手册](https://dev.mysql.com/doc/refman/8.4/en/)、[Redis 官方文档](https://redis.io/docs/latest/)（校验日期：2026-07-17）

## Q35：Redis 事务能回滚吗？

**短回答：**

MULTI/EXEC 保证队列命令连续执行，但运行时错误不会像关系数据库自动回滚已执行命令；WATCH 提供乐观并发检查。

**原理：**

回答时区分规范语义、具体实现和工程取舍，不能把经验规则说成绝对结论。 这道题的关键是把“Redis 事务能回滚吗？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在订单、库存、题库和高并发缓存的数据设计中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果数据规模、并发量或团队人数扩大一个数量级，原方案的首个瓶颈在哪里，如何用指标验证？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [Redis 官方文档](https://redis.io/docs/latest/)、[Redis 数据类型](https://redis.io/docs/latest/develop/data-types/)（校验日期：2026-07-17）

## Q36：Lua 脚本为什么能实现原子操作？

**短回答：**

脚本在 Redis 执行期间不会穿插其他命令，可把读判断写合并；脚本必须短小，长脚本会阻塞所有客户端。

**原理：**

先给结论，再用一条真实调用链解释为什么，最后说明如何测量或验证。 这道题的关键是把“Lua 脚本为什么能实现原子操作？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在订单、库存、题库和高并发缓存的数据设计中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果面试官加入失败重试、版本冲突或安全攻击条件，哪些保证仍成立，哪些需要额外机制？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [Redis 官方文档](https://redis.io/docs/latest/)、[Redis 数据类型](https://redis.io/docs/latest/develop/data-types/)（校验日期：2026-07-17）

## Q37：缓存旁路模式如何更新数据？

**短回答：**

读时 miss 查库再写缓存，写时先更新数据库再删除缓存；并发窗口需用延迟双删、版本或消息失效按一致性要求加强。

**原理：**

先说明输入、内部机制和可观察结果，再补充失败边界。 这道题的关键是把“缓存旁路模式如何更新数据？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在订单、库存、题库和高并发缓存的数据设计中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果数据规模、并发量或团队人数扩大一个数量级，原方案的首个瓶颈在哪里，如何用指标验证？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [MySQL 8.4 参考手册](https://dev.mysql.com/doc/refman/8.4/en/)、[MySQL InnoDB 事务模型](https://dev.mysql.com/doc/refman/8.4/en/innodb-transaction-model.html)（校验日期：2026-07-17）

## Q38：缓存击穿时互斥锁有哪些风险？

**短回答：**

锁过期过短会重复回源，过长会阻塞，持有者崩溃需自动过期；还应使用旧值、逻辑过期和限流降低单点等待。

**原理：**

回答时区分规范语义、具体实现和工程取舍，不能把经验规则说成绝对结论。 这道题的关键是把“缓存击穿时互斥锁有哪些风险？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在订单、库存、题库和高并发缓存的数据设计中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果面试官加入失败重试、版本冲突或安全攻击条件，哪些保证仍成立，哪些需要额外机制？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [Redis 官方文档](https://redis.io/docs/latest/)、[Redis 数据类型](https://redis.io/docs/latest/develop/data-types/)（校验日期：2026-07-17）

## Q39：什么是大 key 和热 key？

**短回答：**

大 key 占内存且删除传输阻塞，热 key 让单节点或单分片负载集中；用扫描与监控发现后拆分、异步删除或多级缓存。

**原理：**

先给结论，再用一条真实调用链解释为什么，最后说明如何测量或验证。 这道题的关键是把“什么是大 key 和热 key？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在订单、库存、题库和高并发缓存的数据设计中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果数据规模、并发量或团队人数扩大一个数量级，原方案的首个瓶颈在哪里，如何用指标验证？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [Redis 官方文档](https://redis.io/docs/latest/)、[Redis 数据类型](https://redis.io/docs/latest/develop/data-types/)（校验日期：2026-07-17）

## Q40：Redis 分布式锁至少要满足什么？

**短回答：**

唯一随机令牌、带过期的原子获取、只由持有者原子释放；业务耗时超期、主从切换和 fencing token 仍需考虑。

**原理：**

先说明输入、内部机制和可观察结果，再补充失败边界。 这道题的关键是把“Redis 分布式锁至少要满足什么？”放回系统边界中：谁维护状态、何时发生转换、失败后如何恢复。

**代码 / 场景：**

在订单、库存、题库和高并发缓存的数据设计中遇到该问题时，先记录可复现输入和关键指标，再做最小实验验证上述机制；修复后用相同数据回归，不能只凭“感觉更快”或“应该没问题”下结论。

**递进追问：**

如果面试官加入失败重试、版本冲突或安全攻击条件，哪些保证仍成立，哪些需要额外机制？

**易错点：**

不要只背术语，也不要把局部优化包装成通用架构。明确默认条件、版本差异与未覆盖边界，面试回答才可继续深挖。

**参考来源：** [Redis 官方文档](https://redis.io/docs/latest/)、[Redis 数据类型](https://redis.io/docs/latest/develop/data-types/)（校验日期：2026-07-17）

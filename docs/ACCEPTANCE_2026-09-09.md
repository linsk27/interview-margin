# 全链路收尾记录（2026-09-09）

本报告承接「面试题库」的全链路检查请求。历史上下文见 [CURRENT_CONTEXT.md](CURRENT_CONTEXT.md)。

结论：本批搜索/AI修复已上线，自动化回归、真实AI和隔离备份恢复通过；完整浏览器、性能和重启/回滚验收仍未闭环，不能宣称全链路100%完成。

## 已验证

- 14项新专项测试通过；新增请求路径兼容、权限/分页、每日额度、预算预占、失败与取消、估算计费、重复请求和管理员用量验证。
- 缺失usage的精确金额测试在修复前失败：预估输入100/输出200时，应为0.0007，却得到0.001；修复后通过。
- `db:check`：14库、50章节、762题、762唯一ID；无缺段和重复示例模板。来源762题、1949引用/1166唯一URL；代码576题、图解53题。
- ECS 应用、Tunnel、备份定时器和健康检查定时器 active 且 enabled。
- 备份隔离恢复演练成功：当前发布代码打开备份副本，首页200、health正常、公开14库762题，关键表行数在启动前后不变。完整性为ok，临时副本随后清理。

## 代码修复

1. `/api/search` 与 `/api/catalog/search` 共用路由处理器。
2. 无usage时按输入/输出各自估算结算，避免把总Token当输出Token重复计费。
3. 已预占、尚未完成的相同请求ID返回409，阻止第二次上游调用。

## 限制和剩余项

- 浏览器早期两次读取超时，延长至60秒后成功读到游客Q1页面和阅读控件；搜索操作仍超时。只确认阅读页可见，真实浏览器E2E与性能验收未完成。
- 全局装饰字体预加载、PNG分享图与移动4G三秒目标仍待验证/修复。
- 内容结构通过不代表所有短答精修完成：376题正文>1800字；637短答>160字，78>240字。全量来源URL未实时复核。
- 隔离备份恢复通过，不代表整机重启或实际发布回滚已经演练。
- 独立外部告警尚无证据；原计划邮件/QQ通知后置。

## 最终回归与发布

- 本地全量测试：61文件、467项通过（126.98秒）；ECS同一提交61文件/467项通过（743.89秒）。
- 本地和ECS构建、db:check均通过；本地git diff --check通过。
- 本轮新增测试14项，原453项回归保留。
- 代码提交 `4b7d78e34d18a4952dde96a58bb9dcbb0afaef76`，已推送origin/main，并在2026-09-09 00:26（上海）部署确认；服务器current链接已核对。后续文档补充单独提交，不改变线上代码版本。
- 发布前一致性备份 `interview-2026-09-08_16-25-49-335.db` 已生成并通过完整性检查。
- 公网首页、health、catalog/index、新旧搜索、robots、sitemap、manifest、favicon、分享图均正常。`/api/search`返回200 JSON，已不再落到SPA HTML。
- Vercel公开快照通过PowerShell系统网络请求，200 JSON；Node直接连接Vercel超时属于本次验证路径差异，不据此判定备用站故障。

## 新版本真实AI冒烟与账本

只执行一次聊天和一次评分，使用公开题目和合成回答，不创建生产账号或反馈。

| 检查 | 实际结果 |
| --- | --- |
| 聊天 | HTTP200、text/event-stream、收到delta和DONE；游客剩余19次 |
| 评分 | HTTP200、78分（主线完整，有少量遗漏）；游客剩余2次 |
| 聊天账本 | completed；输入7566、输出121、总7687 Token；cost_source=provider |
| 评分账本 | completed；输入5832、输出326、总6158 Token；cost_source=provider |
| 模型 | 服务器确认本次两次调用均为gpt-5.6-luna |
| 费用口径 | 按配置费率估算分别0.007929/0.006810 USD；不是已核对的供应商实际账单 |

可复核请求ID：

- `acceptance-20260909-chat-a4ab2c9c-306b-42d9-9875-d72e0014d8d0`
- `acceptance-20260909-score-b5bd90d1-de17-414f-af9e-04c87c3e3db6`

两条记录均无error_code。游客评分不写入账号练习历史，符合既定产品边界。

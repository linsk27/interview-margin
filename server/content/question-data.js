const rows = (text) => text.trim().split('\n').map((line) => {
  const [title, answer] = line.split('|||')
  if (!title || !answer) throw new Error(`Invalid question row: ${line}`)
  return { title: title.trim(), answer: answer.trim() }
})

export const GENERATED_BANKS = [
  {
    id: 'git-engineering', title: 'Git 与协作工程', shortTitle: 'Git 工程', kicker: 'SOURCE CONTROL',
    category: '前端开发', description: '从对象模型、分支协作到故障恢复与持续集成的 Git 高频面试题。',
    baseTags: ['Git', '协作'], tone: 'green', source: 'public/question-banks/git-engineering.md',
    official: ['Git 官方参考', 'https://git-scm.com/docs'], scenario: '多人协作仓库、线上热修复与发布分支',
    secondary: ['Git 用户手册', 'https://git-scm.com/docs/user-manual'],
    sections: [
      ['Git 对象与工作区', rows(`
Git 的工作区、暂存区和仓库分别是什么？|||工作区是实际文件，暂存区保存下一次提交的快照，仓库以对象和引用保存历史；git add 更新索引，git commit 由索引生成提交。
Git 为什么说是快照模型而不是差异模型？|||每次提交记录一棵完整目录树，并通过内容寻址复用未变化的 blob；展示 diff 是比较两个快照的结果。
blob、tree、commit 和 tag 对象有什么关系？|||blob 保存文件内容，tree 保存目录项，commit 指向根 tree 和父提交，附注 tag 再指向目标对象并保存签名等元数据。
HEAD、branch 和 tag 有什么区别？|||HEAD 表示当前检出的提交或分支，branch 是可移动的提交引用，tag 通常是指向固定版本的发布标记。
git add 到底做了什么？|||它把指定文件内容写成对象并更新 index 中路径对应的 blob、模式和阶段信息，不是简单把文件“放进缓存”。
git status 为什么能判断文件状态？|||Git 比较 HEAD 树、index 与工作区三份状态：HEAD 对 index 得到待提交变化，index 对工作区得到未暂存变化。
git diff、git diff --cached 和 git show 如何区分？|||前者比较工作区与 index，--cached 比较 index 与 HEAD，git show 默认展示指定对象及提交引入的变化。
什么是 detached HEAD？|||HEAD 直接指向提交而非分支引用；此时提交可创建但没有分支名称保护，应及时创建分支避免后续不可达。
.gitignore 为什么对已跟踪文件不生效？|||忽略规则只影响未跟踪路径的发现；已进入 index 的路径仍受版本控制，需要先 git rm --cached 再提交。
Git 的内容寻址和 SHA 哈希解决什么问题？|||对象名由类型、长度和内容计算，可用于去重与完整性校验；哈希相同表示 Git 按对象模型识别为同一内容。
      `)],
      ['分支、合并与历史', rows(`
merge 和 rebase 的核心区别是什么？|||merge 保留分叉并产生合并提交，rebase 复制提交到新基线并重写提交身份；公共历史通常避免随意 rebase。
fast-forward 合并是什么？|||目标分支没有额外提交时，只需把分支引用前移到新提交，不必创建双亲合并提交。
三方合并为什么需要共同祖先？|||合并算法比较共同祖先、ours 和 theirs，只有双方相对基线都改动同一区域时才需要人工解决冲突。
如何安全解决 merge conflict？|||先读取冲突阶段和业务语义，编辑出最终内容，运行测试后 git add 标记已解决，再继续 merge 或 rebase；不要只删除标记。
git rebase --onto 适合什么场景？|||它可把一段提交从旧基线搬到指定新基线，适合拆错分支或只迁移某段连续历史。
cherry-pick 的作用和风险是什么？|||它把指定提交的补丁应用为新提交；会生成新身份，重复挑选或与后续合并叠加可能产生冲突与重复变更。
revert 和 reset 有什么区别？|||revert 新建反向提交，适合共享分支；reset 移动当前分支并可改变 index/工作区，更适合尚未共享的本地历史。
soft、mixed、hard reset 分别改变什么？|||soft 只移动 HEAD，mixed 还重置 index，hard 进一步覆盖工作区；hard 会丢弃未保存修改，使用前必须确认范围。
reflog 能恢复哪些误操作？|||它记录本地引用的移动，可找回误 reset、误 rebase 或删除分支前指向的提交；但有过期清理且不随 push 共享。
什么是 squash，何时不该 squash？|||squash 把多个提交整理为较少提交；需要保留独立回滚点、审计轨迹或有意义的分阶段变更时不应盲目压缩。
      `)],
      ['远程协作与发布', rows(`
fetch、pull 和 push 的边界是什么？|||fetch 更新远程跟踪引用但不整合，pull 等于 fetch 后 merge 或 rebase，push 请求更新远端引用并发送缺失对象。
origin/main 是什么？|||它是本地保存的远程跟踪引用，表示最近一次 fetch 后远端 main 的位置，不会在离线时自动变化。
non-fast-forward push 为什么被拒绝？|||远端引用若不能只前移到本地提交，普通 push 会覆盖别人可达的历史，因此服务器默认拒绝。
force-with-lease 为什么比 force 安全？|||它只在远端引用仍等于本地预期值时强推，能避免无意覆盖他人在最近一次 fetch 后新增的提交。
如何设计 feature branch 与主干协作？|||分支应短生命周期、频繁同步主干、通过 PR 和自动检查合并；功能开关用于把部署与功能发布解耦。
Git Flow、GitHub Flow 和 trunk-based 如何选择？|||多版本发布可用更重的发布分支；持续部署偏向短分支或主干开发，选择依据是发布节奏、合规和团队规模。
如何处理线上 hotfix？|||从线上实际版本创建修复分支，最小改动并回归，发布后把修复合并或挑选回所有仍维护的分支，避免分叉遗漏。
语义化版本与 Git tag 如何配合？|||通过受保护 tag 标记已构建的不可变提交，流水线从 tag 产出制品；版本号表达不兼容、功能和修复级别。
submodule 和 monorepo 的权衡是什么？|||submodule 保持仓库与版本边界但协作复杂；monorepo 统一变更和工具链但需要权限、构建缓存与依赖边界治理。
git worktree 解决什么问题？|||同一仓库可同时拥有多个工作树，各有 HEAD 和 index，适合并行热修复或评审分支而无需反复 stash 和切换。
      `)],
      ['排障与工程质量', rows(`
git bisect 如何定位回归提交？|||给定一个 good 与 bad，Git 二分检出中间提交；每轮运行测试并标记，最终把搜索缩小到首个坏提交。
git blame 应该如何正确使用？|||它用于找到某行最近一次修改，辅助理解上下文；应结合 log、PR 和业务历史，不应当作责任追究工具。
如何从错误提交中只恢复一个文件？|||先用 git restore --source=<commit> -- path 把目标版本写入工作区或 index，检查 diff 后作为新修复提交保存。
大文件为什么会拖慢 Git？|||每个历史版本都进入对象库，克隆和对象遍历成本持续累积；应使用 Git LFS、制品仓库或对象存储管理二进制资产。
什么是 shallow clone 和 partial clone？|||shallow clone 截断提交历史，partial clone 延迟获取部分对象；能降低 CI 初始成本，但某些历史分析命令受限。
Git hooks 能做什么，为什么不能只依赖本地 hook？|||hook 可在提交或接收阶段执行校验；本地 hook 容易被跳过且分发不一致，关键规则还应在 CI 或服务端执行。
CI 中如何保证构建可复现？|||固定依赖锁、运行时与构建参数，从明确提交构建并保存校验和；不要让流水线隐式依赖开发机或浮动版本。
如何避免敏感信息进入 Git 历史？|||提交前用密钥扫描和环境变量；一旦泄露先吊销轮换，再用历史重写清理，并通知协作者重新同步。
如何评审一个大型 PR？|||先核对目标和架构边界，再按提交或模块审查，运行测试并关注失败路径；大型改动应提前拆成可独立验证的小批次。
如何制定提交信息规范？|||标题说明可观察的变更，正文解释原因、边界和迁移；关联任务与破坏性变更，便于生成日志、回滚和追踪决策。
      `)],
    ],
  },
  {
    id: 'vue-core', title: 'Vue 核心与源码', shortTitle: 'Vue 核心', kicker: 'VUE INTERNALS',
    category: '前端开发', description: '覆盖 Vue 3 响应式、渲染机制、组件设计、状态管理和性能排查。',
    baseTags: ['Vue'], tone: 'green', source: 'public/question-banks/vue-core.md',
    official: ['Vue 官方指南', 'https://vuejs.org/guide/essentials/reactivity-fundamentals.html'], scenario: '大型 Vue 业务后台、复杂表单和组件库',
    secondary: ['Vue 深入响应式系统', 'https://vuejs.org/guide/extras/reactivity-in-depth.html'],
    sections: [
      ['响应式系统', rows(`
Vue 3 为什么使用 Proxy？|||Proxy 可拦截对象级读取、写入、删除、in 和遍历等操作，无需预先遍历所有属性，也能处理新增属性和数组索引。
reactive、ref 和 shallowRef 如何选择？|||对象深层响应通常用 reactive，基本类型或可替换值用 ref，大型不可变对象或第三方实例用 shallowRef 避免深层代理成本。
track 和 trigger 分别做什么？|||track 在活跃 effect 读取属性时记录 target-key-effect 关系，trigger 在写入后找到相关 effect 并交给调度器执行。
为什么依赖桶常用 WeakMap 到 Map 到 Set？|||WeakMap 以原对象为键便于垃圾回收，Map 区分属性键，Set 对同一 effect 去重并提供稳定的依赖集合。
effectStack 解决什么问题？|||嵌套副作用执行时需要保存并恢复父 effect，否则内层结束后继续读取的数据会错误收集到内层依赖。
为什么 effect 每次执行前要清理旧依赖？|||分支条件改变后旧字段不再被读取，若不从旧 Set 移除，旧字段变化仍会触发无效执行并造成依赖泄漏。
computed 为什么能缓存？|||computed 内部是 lazy effect，依赖变化时只把 dirty 标为 true；下次读取 value 才重新求值并恢复缓存。
watch 和 watchEffect 有什么区别？|||watch 显式指定来源并能比较新旧值，watchEffect 立即运行并自动收集同步执行阶段读取的依赖，控制精度不同。
为什么不建议 deep watch 大对象？|||深度监听需要递归遍历属性以建立依赖，数据规模和更新频率高时会放大初始化与重复执行成本，应改为监听必要字段。
toRef、toRefs 解决什么问题？|||直接解构 reactive 属性会失去属性访问代理，toRef 把单个属性包装为保持双向联系的 ref，toRefs 批量处理对象属性。
      `)],
      ['渲染与调度', rows(`
虚拟 DOM 的价值是什么？|||它把声明式视图变为可比较的 JavaScript 节点，框架可批量调度并以平台无关方式计算最小必要 DOM 操作，但并非永远快于手写 DOM。
Vue 3 patchFlag 有什么作用？|||编译器标记动态文本、class、props 等变化类型，运行时更新 block 时只检查动态节点和目标字段，减少全量 diff。
key 在列表 diff 中为什么重要？|||稳定且唯一的 key 表示节点身份，使算法能复用和移动正确的 DOM 与组件状态；索引 key 在插入排序时会产生错误复用。
最长递增子序列在 Vue diff 中做什么？|||处理中间未知序列时，映射新旧索引后用最长递增子序列找出可保持位置的节点，其余节点再移动，降低 DOM 移动次数。
nextTick 为什么不是 setTimeout 的同义词？|||Vue 把组件更新放进微任务调度队列去重，nextTick 等待当前刷新 promise 完成；setTimeout 是后续任务，时序更晚。
组件更新为什么会批处理？|||同一同步调用栈内多次状态修改只需在微任务中执行一次去重后的更新，避免每次赋值都触发布局和渲染。
v-if 和 v-show 如何选择？|||v-if 控制挂载销毁，切换成本高但未显示时无组件开销；v-show 保留节点只改 display，适合频繁切换。
KeepAlive 的缓存边界是什么？|||它缓存组件实例与 DOM 子树并触发 activated/deactivated，不等于永久保存业务数据，应设置 include、exclude 或 max 控制资源。
Teleport 解决什么布局问题？|||组件逻辑归属不变，但 DOM 可渲染到 body 等目标，避免弹层受祖先 overflow、transform 和层叠上下文限制。
Suspense 在 Vue 中负责什么？|||它协调异步 setup 或异步依赖的 pending、fallback 与 resolved 展示；仍需自行处理请求失败和超时策略。
      `)],
      ['组件与状态设计', rows(`
props 为什么强调单向数据流？|||父组件拥有状态并向下传值，子组件通过事件请求修改，避免多个组件暗中修改同一来源导致更新路径不可追踪。
组件的 v-model 如何实现？|||默认由 modelValue prop 与 update:modelValue 事件组成，也可通过参数定义多个双向绑定；子组件不应直接改 prop。
provide/inject 适合替代 Pinia 吗？|||它适合组件树范围内的依赖传递和插件能力；跨页面共享、调试、持久化和复杂业务状态通常更适合独立 store。
Composable 应如何设计边界？|||围绕单一能力封装状态和副作用，参数使用 ref 或 getter 保持响应性，暴露最小 API，并在卸载时清理监听器。
Pinia 的 state、getter 和 action 各负责什么？|||state 保存源数据，getter 表达派生值，action 封装同步或异步业务变更；不要把可推导值重复写入 state。
为什么 store 不应保存 DOM 节点和大型第三方实例？|||这类值不适合深层代理和序列化，生命周期也属于组件；需要共享时用 markRaw 或 shallowRef 并明确清理。
动态表单 schema 如何建模？|||schema 描述字段类型、默认值、校验和联动，运行时建立 source 到 target 的依赖索引，只在源字段变化时执行相关规则。
字段联动如何防止循环触发？|||构建依赖图时检测环，执行时记录批次和访问路径，变更前比较新旧值，并把规则限制为纯计算或显式动作。
异步校验如何避免旧请求覆盖新结果？|||为每次校验分配序号或 AbortController，结果返回时只接受最新序号，并区分取消、网络失败和业务不通过。
权限按钮为什么不能只在前端隐藏？|||前端隐藏改善体验但可被绕过，后端必须对每个写接口校验身份、资源范围和操作权限，返回一致的 401/403。
      `)],
      ['性能、工程与安全', rows(`
Vue 页面卡顿如何定位？|||先区分网络、脚本、渲染和内存，再用 Performance、Vue Devtools 与组件更新高亮定位长任务和过大更新范围。
大型列表为什么需要虚拟化？|||只渲染视口附近元素，把 DOM 数量从总数据量降为窗口大小；必须处理动态高度、键值和滚动位置恢复。
如何减少无效组件更新？|||稳定 props 引用、缩小响应式范围、把派生值放 computed、拆分组件并避免父模板中每次创建新对象或函数。
静态字典缓存和请求去重如何配合？|||数据缓存保存已完成结果，pending Map 保存进行中的 Promise；请求完成后清 pending，但按失效策略保留数据缓存。
异步组件和路由懒加载的区别是什么？|||路由懒加载按页面拆包，defineAsyncComponent 可细分非首屏重组件；都要配合 loading、error、超时和预加载策略。
Vue SSR 为什么会发生 hydration mismatch？|||服务端 HTML 与客户端首次渲染不同，例如时间、随机数、浏览器 API 或数据不一致；需保证确定性并把客户端逻辑推迟到挂载后。
v-html 的主要风险是什么？|||它把字符串作为 HTML 注入，不编译 Vue 模板；不可信内容可能造成 XSS，必须在可信边界前使用成熟白名单消毒器。
错误边界如何设计？|||组件层可用 errorCaptured 收敛渲染错误，全局 handler 上报上下文；异步请求和事件回调仍要显式捕获并提供恢复路径。
Vue 应用如何做运行时监控？|||采集 JS 错误、未处理 Promise、接口失败、性能指标和版本信息，使用 source map 还原堆栈并按用户会话关联。
如何证明复杂表单不是普通 CRUD？|||用字段规模、联动图、权限状态、异步校验、草稿恢复和性能指标说明约束，再讲定位过程与可验证结果，而非堆技术名词。
      `)],
      ['源码与边界追问', rows(`
Reflect.get 的 receiver 为什么重要？|||当属性是 getter 或来自原型链时，receiver 决定 getter 内 this；代理中传入 receiver 可让 this 指向代理并继续触发响应式读取。
Proxy set 为什么要区分新增和修改？|||新增属性还会影响 ownKeys、for-in 和数组 length 等依赖，普通修改主要触发该 key，二者需要派发不同依赖集合。
数组 includes 为什么需要对原数组重试？|||代理数组中元素可能是原对象而查询参数是代理对象，第一次查找失败时用原始数组重试可统一两种身份。
数组 push 为什么可能造成依赖递归？|||push 内部会读取并写 length，若 effect 同时因 length 收集又触发写入，可能自触发；实现会暂停特定跟踪或由调度器防重入。
Map 的响应式为什么比对象复杂？|||读取、设置、删除、迭代 key/value 和 size 具有不同依赖，set 还要区分新增与值变化，迭代需专用依赖键。
readonly 是如何实现的？|||读取仍递归包装为只读代理，写入和删除被拦截并警告或拒绝；它是运行时访问约束，不等于深冻结原对象。
markRaw 和 toRaw 的风险是什么？|||markRaw 跳过代理，toRaw 取回原对象；长期混用原对象与代理会产生身份不一致，通常只用于第三方实例或短暂逃生。
effectScope 解决什么生命周期问题？|||它把多个 effect、computed 和 watch 归组，可一次停止，适合 composable、组件外状态或动态模块的集中清理。
Vue 2 数组为什么要改写七个方法？|||Object.defineProperty 无法拦截数组索引和 length，Vue 2 通过原型增强 push、pop、shift、unshift、splice、sort、reverse 并观察新增元素。
从源码学习 Vue 时如何避免背实现细节？|||先画出读取、收集、写入、调度四步契约，再用最小实现验证边界，最后对照当前版本源码；面试中明确简化实现与官方实现差异。
      `)],
    ],
  },
  {
    id: 'react-core', title: 'React 核心与工程实践', shortTitle: 'React 核心', kicker: 'REACT PRACTICE',
    category: '前端开发', description: '覆盖组件模型、Hooks、并发渲染、状态设计、性能和工程测试。',
    baseTags: ['React'], tone: 'rose', source: 'public/question-banks/react-core.md',
    official: ['React 官方文档', 'https://react.dev/reference/react'], scenario: '中大型 React 产品、数据流和交互性能优化',
    secondary: ['React 状态管理指南', 'https://react.dev/learn/managing-state'],
    sections: [
      ['组件与渲染模型', rows(`
React 组件为什么应当是纯函数？|||相同 props、state 和 context 应返回相同 JSX，渲染阶段不产生外部副作用，React 才能安全重试、暂停或丢弃一次渲染。
JSX 最终是什么？|||JSX 经编译变为创建 React element 的调用，element 是描述类型、props 和 key 的不可变普通对象，不是真实 DOM。
state 为什么像一次渲染的快照？|||每次渲染中的事件处理器闭包捕获当次 state 值，setState 请求下一次渲染，不会修改当前闭包里的变量。
React 为什么批处理状态更新？|||同一事件或受支持异步边界内的更新进入队列，事件结束后统一计算和提交，减少中间渲染并保持 UI 一致。
函数式 setState 何时必须使用？|||新状态依赖前一个状态或同一批次连续更新时传 updater，React 会按队列依次计算，避免读取旧快照导致丢更新。
key 如何影响组件状态？|||React 用父级位置、元素类型和 key 判断身份；key 改变会卸载旧实例并创建新实例，可用于有意重置状态。
为什么不能在 render 中直接修改 state？|||会在渲染过程中再次安排渲染，可能形成循环并破坏纯渲染假设；应在事件、effect 或外部订阅回调中更新。
受控组件和非受控组件如何选择？|||受控输入由 React state 驱动，适合联动校验；非受控由 DOM 保存即时值，适合简单表单或文件输入，提交时读取 ref。
Context 的适用边界是什么？|||它适合跨层传递相对稳定的依赖；value 变化会通知所有消费方，高频复杂业务状态需拆分 context 或用外部 store。
Portal 为什么仍能按 React 树冒泡事件？|||Portal 只改变 DOM 放置位置，组件父子关系仍在 React 树中，因此 context 和合成事件沿 React 树传播。
      `)],
      ['Hooks 与副作用', rows(`
Hooks 为什么不能写在条件语句里？|||React 依赖每次渲染相同的 Hook 调用顺序把状态槽与调用对应，条件调用会让后续槽位错位。
useEffect 的职责是什么？|||它用于让组件与网络、订阅、DOM 插件等外部系统同步，不应拿来计算可由 props/state 直接推导的数据。
依赖数组如何判断？|||effect 读取的所有响应式值都应列为依赖；由组件外常量或稳定 Hook 保证不变的值可不列，不能靠手工删依赖压制 lint。
effect cleanup 在何时执行？|||依赖变化重新执行前先清理上一次，组件卸载时也清理；开发 Strict Mode 还会额外执行一次 setup-cleanup 用于暴露缺陷。
useLayoutEffect 和 useEffect 有何区别？|||layout effect 在 DOM 提交后、浏览器绘制前同步执行，适合测量布局；它会阻塞绘制，普通副作用优先 useEffect。
useMemo 和 useCallback 是语义保证吗？|||它们是性能优化缓存，依赖变化会失效，React 也可能因实现需要丢弃缓存；业务正确性不能依赖缓存永久存在。
useRef 为什么修改后不触发渲染？|||ref 是跨渲染保持身份的可变容器，React 不跟踪 current 变化；适合 DOM、定时器和不参与展示的实例数据。
闭包导致 stale state 如何处理？|||使用函数式更新、正确 effect 依赖或 ref 保存最新值；不要通过禁用依赖规则掩盖闭包读取旧快照的问题。
自定义 Hook 的价值是什么？|||复用有状态逻辑而非共享状态实例，每次调用都有独立 state；名称以 use 开头让 lint 能检查 Hook 规则。
useSyncExternalStore 解决什么问题？|||它规定外部 store 的订阅与快照读取契约，使并发渲染下多个组件看到一致快照，并支持 SSR 的服务端快照。
      `)],
      ['状态架构与数据请求', rows(`
如何判断状态应该放在哪里？|||优先放在需要它的最近共同父组件；只有跨远距离、跨页面或服务端缓存才提升到 context、store 或请求缓存层。
为什么不应把可推导值存进 state？|||源数据变化时容易忘记同步形成双源真相；在渲染中计算，昂贵时再用 memo 优化。
useReducer 适合什么场景？|||状态转换由多种事件驱动、字段相关或需要可测试的状态机时，reducer 把事件与纯转换集中起来。
Context 加 reducer 能替代所有状态库吗？|||可覆盖中等规模全局状态，但缺少细粒度订阅、持久化、DevTools 等能力；高频大状态需评估专用 store。
客户端请求为什么要处理竞态？|||参数快速变化时旧请求可能晚返回覆盖新结果；cleanup 中取消或用请求序号只接收当前响应。
服务端状态和客户端状态有什么区别？|||服务端状态有缓存、过期、重试和并发一致性，客户端状态描述本地交互；应避免用普通全局 store 手写完整请求缓存。
乐观更新如何保证失败可恢复？|||提交前保存可回滚快照或逆操作，先更新 UI，服务器失败时回滚并提示；并发更新需使用版本或请求序号。
表单状态如何避免每次输入重渲染整页？|||把状态下沉到字段或表单边界，使用非受控注册、细粒度订阅和 memo，避免顶层对象每次都产生全树新 props。
URL 为什么也可以是状态？|||筛选、分页和当前资源若需分享、刷新恢复或前进后退，应编码在 URL；临时输入和敏感信息不适合放 URL。
状态机比多个 boolean 好在哪里？|||显式枚举合法状态与事件，避免 loading、success、error 等布尔组合出现不可能状态，并便于测试转移路径。
      `)],
      ['并发、性能与 Fiber', rows(`
Fiber 架构解决什么问题？|||它把渲染工作拆成可暂停和恢复的单元，并为更新赋优先级，使 React 能在提交前调度计算；提交阶段仍保持同步一致。
render phase 和 commit phase 有何区别？|||render 计算下一棵树，可被中断或重做；commit 把变更应用到 DOM 并运行布局 effect，不能向用户呈现半成品。
startTransition 适合什么更新？|||把非紧急且可中断的更新标为 transition，例如大列表筛选；输入框自身值等紧急反馈仍应同步更新。
useDeferredValue 与防抖有何区别？|||deferred 保留旧值并让新渲染低优先级推进，不固定等待时间；防抖按时间减少触发次数，网络请求常仍需显式防抖。
React.memo 为什么可能无效？|||默认浅比较 props；父组件每次创建新对象、数组或函数会破坏引用相等，且比较成本可能高于重新渲染。
如何用 Profiler 定位 React 性能问题？|||记录交互，查看提交耗时和哪些组件重复渲染，再检查状态位置、props 身份和昂贵计算，用修复前后数据验证。
大型列表如何优化？|||使用稳定 key、窗口化、分页或增量加载，把行组件隔离，并避免每行订阅整个全局状态。
Suspense 是否等于通用数据请求方案？|||Suspense 协调“等待中的渲染”与 fallback，数据源仍需框架或支持缓存与抛出 promise 的集成，不能直接包任意 useEffect 请求。
错误边界能捕获哪些错误？|||它捕获子树渲染、生命周期和构造错误，不捕获事件处理器、异步回调、服务端渲染或边界自身错误。
Strict Mode 为什么会让 effect 开发环境执行两次？|||开发模式用额外 setup-cleanup 周期检查副作用是否可重入且清理完整，生产环境不会做这次诊断执行。
      `)],
      ['工程测试与安全', rows(`
React 测试为什么更关注用户行为？|||通过角色、文本和交互断言可观察结果，比断言内部 state 或实例方法更抗重构，也更接近可访问性使用方式。
单元、集成和 E2E 测试如何分工？|||纯函数与边界逻辑做单元测试，组件和 API 协作用集成测试，少量关键业务链路用 E2E 覆盖真实浏览器。
SSR hydration mismatch 如何排查？|||比较服务端与客户端首屏输入，排查随机数、时间、浏览器 API、无效 HTML 和数据版本差异，保证首次输出确定。
React 中如何防 XSS？|||普通 JSX 文本默认转义，dangerouslySetInnerHTML 绕过保护；富文本必须在可信边界前消毒并配合 CSP。
代码分割应该以什么边界实施？|||优先路由和低频重功能，用 lazy 与 Suspense 提供加载状态；过度碎片会增加请求与瀑布，需结合打包分析。
如何设计可访问的复合组件？|||先使用语义元素，再实现键盘导航、焦点管理和 ARIA 状态；弹窗打开聚焦、关闭还原焦点并限制背景交互。
React 组件库如何避免样式和状态耦合？|||把无样式行为、设计 token 与视觉组件分层，受控/非受控 API 一致，并为组合、主题和可访问性写契约测试。
前端错误监控应记录哪些 React 上下文？|||错误堆栈、组件栈、路由、版本、用户动作和请求关联 ID；上报前脱敏并使用 source map 还原。
如何渐进迁移旧 React 项目？|||先建立测试与构建基线，按叶子组件或路由迁移，使用适配层保持数据契约，避免一次重写所有业务。
面试时如何解释 Vue 与 React 的差异？|||从更新粒度、编译优化、状态模型和生态约束比较，并回到项目场景；不要简化为“一个双向绑定、一个虚拟 DOM”。
      `)],
    ],
  },
  {
    id: 'frontend-engineering', title: '前端工程化、浏览器与 TypeScript', shortTitle: '前端工程', kicker: 'FRONTEND SYSTEMS',
    category: '前端开发', description: '覆盖浏览器运行机制、网络性能、构建工具、TypeScript、测试和前端安全。',
    baseTags: ['工程化', 'TypeScript'], tone: 'blue', source: 'public/question-banks/frontend-engineering.md',
    official: ['MDN Web 文档', 'https://developer.mozilla.org/en-US/docs/Web'], scenario: '复杂前端应用的构建、性能、兼容性与质量保障',
    secondary: ['TypeScript Handbook', 'https://www.typescriptlang.org/docs/handbook/intro.html'],
    sections: [
      ['浏览器与事件循环', rows(`
浏览器从输入 URL 到页面可交互经历什么？|||依次涉及 URL 解析、DNS、连接与 TLS、HTTP、HTML 流式解析、资源加载、样式布局绘制和脚本执行；可交互还取决于主线程长任务。
浏览器进程和渲染进程如何分工？|||浏览器进程负责导航、网络和窗口，站点渲染进程运行 Blink 与 JavaScript；进程隔离可减少页面崩溃和跨站攻击影响。
宏任务和微任务的执行顺序是什么？|||每个任务执行到调用栈清空后，事件循环会清空微任务队列，再给浏览器渲染机会并进入下一个任务；Promise 回调属于微任务。
requestAnimationFrame 适合做什么？|||它在下一次绘制前调用，适合按刷新节奏更新动画；后台标签可能降频，时间推进应使用回调时间戳而不是假设固定帧率。
什么是长任务，如何定位？|||主线程连续执行超过约 50ms 会阻塞输入和渲染；用 Performance 火焰图定位脚本、样式和布局热点，再拆分或移出主线程。
重排和重绘有什么区别？|||几何或结构变化会触发布局计算并可能随后绘制，纯颜色等变化通常只需重绘；transform 和 opacity 常可交给合成线程。
什么是强制同步布局？|||写入样式后立即读取 offsetWidth 等布局属性会迫使浏览器提前完成布局；循环中读写交错会形成 layout thrashing。
Web Worker 的边界是什么？|||Worker 在独立线程执行脚本，不能直接操作 DOM；通过结构化克隆或 Transferable 消息通信，适合 CPU 密集任务而非所有异步请求。
浏览器存储如何选择？|||Cookie 随请求发送且容量小，localStorage 同步且仅字符串，IndexedDB 异步事务化存储较大结构数据；敏感凭据优先 HttpOnly Cookie。
页面关闭前发送数据为什么常用 sendBeacon？|||它让浏览器在卸载阶段异步排队少量 POST 数据，比普通 fetch 更可能完成；仍有大小和方法头部限制，不能替代可靠业务提交。
      `)],
      ['HTTP、缓存与性能', rows(`
强缓存和协商缓存如何配合？|||Cache-Control max-age 或 immutable 让客户端直接复用；过期后用 ETag/If-None-Match 或 Last-Modified 验证，未变化返回 304。
为什么带内容哈希的静态资源适合长期缓存？|||内容变化会产生新 URL，旧 URL 可设置长期 immutable，HTML 保持短缓存并引用新哈希，从而兼顾命中率和发布更新。
HTTP/2 相比 HTTP/1.1 改进了什么？|||二进制分帧和单连接多路复用减少应用层队头阻塞，并支持头部压缩；TCP 丢包仍会影响同连接多个流。
HTTP/3 为什么基于 QUIC？|||QUIC 在 UDP 上实现可靠流、TLS 1.3 和连接迁移，不同流的丢包彼此隔离，减少传输层队头阻塞和握手成本。
DNS 预解析、preconnect 和 preload 有何区别？|||dns-prefetch 只解析域名，preconnect 提前建立连接和 TLS，preload 以高优先级声明当前页面确定需要的资源。
Core Web Vitals 关注什么？|||LCP 衡量主要内容加载，INP 衡量交互响应，CLS 衡量意外布局偏移；优化应基于真实用户分位数而非只看单次实验室分数。
如何优化 LCP？|||先确认 LCP 元素，缩短 TTFB，避免关键资源发现延迟，压缩并预加载主图或字体，减少阻塞 CSS/JS 和客户端瀑布。
如何降低 CLS？|||为图片、广告和嵌入预留尺寸，字体使用合理回退和加载策略，避免在已有内容上方动态插入未占位元素。
前端请求并发控制如何实现？|||维护最大并发数和等待队列，任务结束后启动下一个；配合 AbortController、超时、重试和幂等性，而不是无上限 Promise.all。
Promise.allSettled 适合什么业务？|||批量任务彼此独立且需要收集每项成功失败时使用；它不会短路，也不会自动重试，关键链路仍需明确整体成功条件。
      `)],
      ['Vite、构建与发布', rows(`
Vite 开发环境为什么启动快？|||它利用浏览器原生 ESM 按需提供源码，依赖预构建减少请求和 CommonJS 兼容成本，不必先打完整 bundle。
生产构建为什么仍需要 bundler？|||部署需做 tree shaking、代码分割、压缩、兼容转换和内容哈希，减少请求与运行开销，因此 Vite 生产阶段使用打包器。
tree shaking 生效需要什么条件？|||依赖静态 ESM 导入导出和可判定副作用；动态访问、CommonJS 或错误 sideEffects 声明会阻碍删除未使用代码。
代码分割过细有什么问题？|||小 chunk 过多增加请求调度、压缩字典损失和加载瀑布；应以路由、低频重功能和共享依赖为边界并结合分析报告。
source map 上线有什么安全与调试权衡？|||它能还原压缩堆栈，可私有上传到监控平台；公开 map 可能暴露源码和路径，但隐藏 map 仍需妥善访问控制。
环境变量为什么不能存前端密钥？|||进入客户端 bundle 的变量任何用户都可查看；前端只放公开配置，私钥和第三方机密必须由服务端保管并代理调用。
如何设计前端发布回滚？|||构建不可变制品并记录提交与配置，静态资源使用哈希，入口版本可快速切换；数据库/API 变更需向前兼容才能独立回滚前端。
CI/CD 中前端质量门禁包括什么？|||锁依赖安装、类型检查、lint、单元与 E2E、构建和包体预算，部署后做健康检查与冒烟，再逐步放量。
Module Federation 适合什么场景？|||多团队需要独立部署前端模块且可接受运行时依赖协调时有价值；会增加版本、共享依赖、故障隔离和本地开发复杂度。
如何分析前端包体？|||用构建产物可视化确认大依赖、重复版本和不可摇树模块，再按加载时机、替代库、动态导入和服务端能力逐项优化。
      `)],
      ['TypeScript 类型系统', rows(`
any、unknown 和 never 的区别是什么？|||any 关闭大部分检查，unknown 使用前必须收窄，never 表示不可能出现的值，常用于穷尽检查和永不返回函数。
interface 和 type 如何选择？|||interface 擅长对象契约与声明合并，type 可表达联合、交叉和映射；项目应按能力和一致性选择，不必争论绝对优劣。
泛型相比 any 的价值是什么？|||泛型在输入和输出之间保留类型关系，调用方仍得到具体类型；any 会丢失约束和自动补全。
extends 泛型约束解决什么问题？|||它限制类型参数必须具备某些结构，使函数体可安全访问对应成员，同时保留调用方更具体的类型信息。
keyof 与 indexed access 如何配合？|||keyof 得到对象键联合，T[K] 取得对应属性类型，可构造安全的 get、表单字段和配置映射 API。
类型收窄有哪些常用方式？|||typeof、instanceof、in、判别字段、相等判断和自定义类型谓词；收窄必须有真实运行时检查支撑。
判别联合为什么适合状态机？|||每个分支有唯一字面量 tag，switch 后字段被精确收窄，default 可用 never 检查遗漏状态。
映射类型和条件类型分别解决什么？|||映射类型遍历键变换属性，条件类型按可赋值关系选择类型；组合可实现库级通用工具但应控制可读性。
satisfies 与类型断言有何区别？|||satisfies 校验表达式符合目标类型同时保留推断的具体字面量，as 直接告诉编译器按目标看待，可能掩盖错误。
为什么运行时仍需要 Zod 等校验？|||TypeScript 类型编译后被擦除，网络、存储和用户输入仍是未知数据；边界处要解析验证后才能进入内部类型系统。
      `)],
      ['架构、测试与可维护性', rows(`
前端分层如何避免业务逻辑散落组件？|||组件负责交互与展示，领域服务表达规则，数据访问封装协议，状态层协调生命周期；依赖方向应清晰且可替换测试。
什么是依赖倒置在前端的应用？|||业务逻辑依赖接口而非 fetch、localStorage 等具体实现，通过参数或 provider 注入，测试可替换为内存适配器。
单例请求缓存如何避免重复请求？|||已完成结果按 key 与过期策略缓存，进行中 Promise 单独去重并在 finally 清除；失败不应永久污染数据缓存。
错误处理为什么要区分可恢复与不可恢复？|||校验失败、超时可提示重试或修正，权限和版本冲突需专门流程，未知程序错误进入边界并上报，不能统一 toast。
如何设计前端日志关联后端请求？|||前端为操作生成或透传 trace/request ID，记录版本、路由和匿名会话，后端日志用同一 ID 串联但避免采集敏感正文。
契约测试解决什么问题？|||它验证前后端对字段、状态码和兼容规则的共同理解，可由 OpenAPI 生成或在提供方/消费方流水线校验。
测试替身中的 stub、spy 和 mock 有何区别？|||stub 提供固定行为，spy 记录调用，mock 同时预设交互期望；应优先测试可观察结果，避免过度绑定内部调用。
E2E 测试为什么会不稳定？|||异步等待、共享数据、时间和外部依赖会导致竞态；使用语义定位、确定数据、条件等待和隔离环境，不靠固定 sleep。
如何做渐进式重构？|||先用测试或监控建立行为基线，抽边界和适配层，按垂直业务切片迁移，每一步可发布回滚。
技术方案评审应写哪些内容？|||明确问题、约束、候选方案、数据流、失败模式、安全、迁移回滚和验证指标，让决策可追溯而非只列技术栈。
      `)],
      ['安全与跨端边界', rows(`
XSS 的主要类型和防护是什么？|||存储型、反射型和 DOM 型都来自不可信数据进入可执行上下文；按上下文转义、避免危险 sink、消毒富文本并配置 CSP。
CSRF 为什么 HttpOnly 不能单独防住？|||浏览器仍会自动携带 Cookie，攻击页不需读取它；应使用 SameSite、Origin/CSRF token 并保证 GET 无副作用。
CORS 是什么安全边界？|||它是浏览器限制脚本读取跨源响应的机制，不阻止服务器、curl 或表单发送请求，也不能替代鉴权。
点击劫持如何防护？|||使用 CSP frame-ancestors 或 X-Frame-Options 限制被第三方 iframe 嵌入，并对敏感操作提供明确确认。
前端如何安全处理 JWT？|||短期访问令牌放内存并配合刷新机制，或使用 HttpOnly Cookie；必须防 XSS、CSRF、过期和撤销，不能只解析 payload 当鉴权。
postMessage 如何避免跨窗口攻击？|||发送时指定精确 targetOrigin，接收时校验 event.origin、source 和消息结构，不使用通配符处理敏感数据。
Service Worker 的缓存风险是什么？|||它可长期控制同源请求，错误缓存会让旧版本滞留；更新需版本化缓存、清理旧项并设计激活与刷新提示。
PWA 离线写入如何保证一致性？|||先写本地 outbox，网络恢复按幂等键重放，服务器确认后删除；冲突需版本或合并策略，不能把缓存当权威数据库。
小程序与 H5 的核心运行差异是什么？|||小程序有宿主提供的逻辑层/渲染层、页面栈和权限 API，H5 依赖浏览器 DOM 与 Web API；跨端需通过适配层和条件编译隔离。
如何评估引入第三方 npm 包的风险？|||检查维护活跃度、许可证、依赖树、包体和安全公告，锁定版本并最小权限；小功能不应无评估引入高风险依赖。
      `)],
    ],
  },
  {
    id: 'backend-fullstack', title: '后端、API、鉴权与 Node/Flask', shortTitle: '后端与 API', kicker: 'BACKEND FOUNDATIONS',
    category: '后端开发', description: '覆盖 REST、Node/Flask 运行模型、认证授权、消息系统和高并发治理。',
    baseTags: ['后端', 'API'], tone: 'rose', source: 'public/question-banks/backend-fullstack.md',
    official: ['Node.js API 文档', 'https://nodejs.org/docs/latest/api/'], scenario: '前后端分离服务、账号系统、流式 AI 与高并发接口',
    secondary: ['Flask 官方文档', 'https://flask.palletsprojects.com/en/stable/'],
    sections: [
      ['API 与协议设计', rows(`
REST 中资源和动作如何建模？|||URL 表达资源名词，HTTP 方法表达读取、创建、替换、局部更新和删除；复杂业务动作可建模为子资源或明确命令端点。
PUT 和 PATCH 有何区别？|||PUT 通常提交资源完整表示并具有幂等语义，PATCH 描述局部修改；服务端必须定义缺失字段和并发版本规则。
什么是接口幂等性？|||同一请求执行一次或多次对服务器最终状态影响相同；支付等创建操作常用客户端幂等键和结果记录实现。
如何设计统一错误响应？|||使用正确 HTTP 状态码、稳定业务错误码、可读消息和 request ID；字段校验返回结构化路径，内部堆栈不暴露给客户端。
分页为何推荐游标而非深 offset？|||offset 越深扫描和丢弃越多，数据变动还会重漏；游标基于稳定排序键继续查询，适合大数据和实时列表。
API 版本如何演进？|||优先做向后兼容的加法变更，破坏性变更通过 URL、Header 或媒体类型分版本，并提供迁移期与弃用监控。
OpenAPI 有什么价值？|||它是机器可读接口契约，可生成文档、客户端和校验，驱动契约测试；仍需评审业务语义和错误边界。
文件上传如何防止内存与安全问题？|||限制请求体和文件大小，流式写入隔离存储，校验真实类型、扩展名与权限，随机命名并做恶意内容扫描。
SSE 的协议原理是什么？|||服务器以 text/event-stream 保持单向 HTTP 连接，按 event、id、data 行发送事件，浏览器可用 Last-Event-ID 自动续连。
SSE 和 WebSocket 如何选择？|||服务端单向文本流、需要自动重连时 SSE 简单；高频双向或二进制通信用 WebSocket，但需自定义心跳、重连和消息协议。
      `)],
      ['Node.js 运行机制', rows(`
Node.js 的事件循环解决什么问题？|||JavaScript 在线程上执行回调，I/O 由系统和 libuv 管理，完成后进入各阶段队列，使少量线程处理大量等待型连接。
Node 单线程为什么仍会有竞态？|||多个异步任务可在 await 边界交错读写共享状态，数据库和外部服务也并发；需要事务、原子操作或版本控制。
process.nextTick 和 Promise 微任务有何风险？|||它们在进入后续事件循环阶段前执行，递归排队可能饿死 I/O；长任务应拆分并让出事件循环。
Stream 的背压是什么？|||写端处理不过来时 write 返回 false，读端应暂停并等待 drain；pipe 会协调背压，避免无上限缓冲占满内存。
Buffer 与字符串有什么区别？|||Buffer 表示原始字节，编码决定字节与字符串转换；网络、文件和加密必须明确编码并防止按字符误切二进制。
何时使用 worker_threads？|||CPU 密集计算会阻塞事件循环，可放到 Worker 线程池；普通网络 I/O 不会因此更快，且线程通信和数据复制有成本。
cluster 与多进程部署如何权衡？|||多进程利用多核并隔离崩溃，但会话和内存状态不能假设共享；通常由进程管理器或容器配合负载均衡。
Node 如何优雅停机？|||收到终止信号后停止接收新连接，等待在途请求和队列，设置最大超时，关闭数据库与消费者后退出。
未处理 Promise rejection 应如何处置？|||记录完整上下文并让进程受控退出，由守护进程重启；继续运行可能处于未知状态，业务层应在边界显式 catch。
Node 高并发接口如何定位瓶颈？|||分别测事件循环延迟、CPU、堆、GC、连接池、数据库和外部依赖，使用压测分位数和火焰图而非只看平均 QPS。
      `)],
      ['Flask 与 Python 服务', rows(`
Flask application factory 有什么价值？|||工厂按配置创建 app 并初始化扩展，避免导入时全局副作用，便于测试使用不同数据库和多实例部署。
Blueprint 解决什么问题？|||它记录一组路由和资源并按前缀注册到 app，适合按领域拆模块；Blueprint 本身不是独立应用进程。
Flask request context 是什么？|||每个请求期间维护 request、session、g 等上下文本地代理，请求结束后清理，不能随意在后台线程直接使用。
WSGI 和 ASGI 有何区别？|||WSGI 是同步请求调用接口，ASGI 支持异步、长连接和多事件类型；框架语法有 async 不代表部署栈自动具备同等并发模型。
Flask 为什么不能用开发服务器上生产？|||开发服务器面向调试，缺少生产级并发、超时、进程管理和安全配置；应使用 Gunicorn 等 WSGI 服务器并置于反向代理后。
SQLAlchemy Session 的职责是什么？|||它维护身份映射、变更跟踪和事务工作单元，不等于单条连接；请求结束必须提交或回滚并释放资源。
Python 后台任务为什么不能直接在请求里长时间执行？|||会占用 Web worker 并导致超时；可靠任务应写入持久队列，由独立 worker 执行并记录重试和幂等状态。
Flask 如何做流式响应？|||生成器逐块 yield，必要时保持请求上下文；代理缓冲、异常中断和客户端取消都要纳入设计。
Flask 文件上传有哪些边界？|||设置 MAX_CONTENT_LENGTH，使用 secure_filename 只是路径处理的一部分，还需随机存储名、类型校验和目录隔离。
如何组织 routes、services 和 repositories？|||route 解析协议与权限，service 执行业务事务，repository 封装持久化；避免 route 直接堆 SQL 和跨领域规则。
      `)],
      ['认证、会话与 RBAC', rows(`
认证和授权有什么区别？|||认证确认“你是谁”，授权判断“你能对什么资源做什么”；登录成功不代表拥有所有接口权限。
服务端 Session 和 JWT 如何选择？|||Session 易撤销和集中控制但需服务端存储，JWT 便于分布式验证但撤销与权限变更更复杂；按威胁模型选择。
密码为什么使用 Argon2id？|||它是内存困难型密码哈希，可配置内存、时间和并行成本并为每个密码加盐，提高离线暴力破解成本。
为什么会话 Cookie 要设置 HttpOnly、Secure 和 SameSite？|||HttpOnly 降低脚本读取风险，Secure 只经 HTTPS 发送，SameSite 限制跨站携带；仍需配合 XSS 和 CSRF 防护。
RBAC 如何建模？|||用户关联角色，角色关联权限，接口检查权限而非硬编码角色名；资源归属和租户范围还需额外 ABAC 条件。
管理员为什么默认不能读取私人批注正文？|||最小权限要求管理能力与内容隐私分离，审计只记录操作元数据；确有合规需求时应走授权和留痕流程。
改密后为什么撤销所有会话？|||已有令牌可能被窃取或仍持旧安全状态，统一撤销迫使所有设备重新认证，缩短风险窗口。
登录限流为什么要同时按 IP 和账号？|||只按 IP 可被分布式攻击绕过，只按账号可被用来锁死用户；组合并配合退避、告警和多因素认证。
一次性密码如何安全交付？|||随机高熵、短期有效、首次登录强制修改且使用后失效；不要在长期日志、邮件主题或公开聊天中留存。
多租户授权最容易漏什么？|||只校验“已登录”或角色却不校验资源 tenant_id，导致水平越权；每次查询和写入都要绑定当前租户范围。
      `)],
      ['可靠性、并发与消息', rows(`
高并发系统为什么需要限流？|||在资源耗尽前按用户、接口或全局预算拒绝或排队，保护核心依赖；限流阈值应基于容量测试和优先级。
令牌桶和漏桶有何区别？|||令牌桶允许在累计令牌范围内突发，漏桶以较稳定速率流出；二者分别适合容忍突发和严格整形。
重试为什么会放大故障？|||大量客户端立即重试会形成重试风暴；应只重试幂等瞬时错误，使用指数退避、抖动、上限和总时限。
熔断器解决什么问题？|||依赖持续失败时快速拒绝，避免线程和连接堆积；半开阶段用少量探测决定恢复，不能替代超时和隔离。
消息队列如何保证“至少一次”消费？|||生产成功持久化，消费者处理后再确认；失败或超时会重投，因此业务必须用幂等键或去重表防重复副作用。
“恰好一次”为什么通常是业务效果？|||网络和崩溃让投递与确认难以原子完成，系统多通过至少一次传输加幂等处理，实现最终业务只生效一次。
MQTT 的发布订阅模型是什么？|||客户端连接 broker，按主题发布或订阅；broker 路由消息并按 QoS、保留消息和会话配置提供不同交付语义。
MQTT QoS 0、1、2 有何区别？|||0 至多一次，1 至少一次可能重复，2 通过四步握手实现协议层恰好一次，可靠性越高开销越大。
数据库连接池为什么不能无限增大？|||数据库并发和锁资源有限，过多连接增加上下文切换和争用；池大小应结合 worker、查询耗时和数据库容量。
缓存穿透、击穿和雪崩如何处理？|||穿透用校验/空值/Bloom，热点击穿用互斥或逻辑过期，雪崩用随机过期、多级缓存和降级；都需防止错误长期缓存。
      `)],
      ['Agent 与 AI 应用后端', rows(`
AI 流式输出为什么常用 SSE？|||模型回复是服务端持续单向文本，SSE 复用 HTTP、事件边界清晰并支持重连；服务端应发送 start、delta、done 和 error。
Tool Calling 的信任边界在哪里？|||模型只提出结构化调用意图，服务端必须校验工具名、参数、用户权限和资源范围，危险操作还要人工确认。
RAG 为什么不能直接把全文塞进 Prompt？|||上下文窗口、成本和噪声有限，检索先选相关片段并保留来源，可提高可解释性和有效信息密度。
Embedding 维度或模型变化如何迁移？|||向量不可直接跨模型比较，应记录模型与维度，后台重建新索引并双读或切换，完成后再回收旧向量。
如何防 Prompt Injection 影响工具？|||把资料当不可信数据，系统策略与工具权限在服务端强制执行，限制可调用工具和参数并记录审计。
      `)],
    ],
  },
  {
    id: 'database-cache', title: 'SQL、MySQL、Redis 与缓存', shortTitle: '数据库缓存', kicker: 'DATA SYSTEMS',
    category: '后端开发', description: '覆盖 SQL 索引设计、事务与锁、查询优化、Redis 数据结构和缓存一致性。',
    baseTags: ['SQL', 'Redis'], tone: 'amber', source: 'public/question-banks/database-cache.md',
    official: ['MySQL 8.4 参考手册', 'https://dev.mysql.com/doc/refman/8.4/en/'], scenario: '订单、库存、题库和高并发缓存的数据设计',
    secondary: ['Redis 官方文档', 'https://redis.io/docs/latest/'],
    sections: [
      ['SQL 与索引设计', rows(`
数据库索引的本质是什么？|||索引是按键组织的辅助数据结构，以额外存储和写放大换取更少的数据页读取；是否有效取决于选择性、查询模式和成本估算。
B+Tree 为什么适合范围查询？|||内部节点保存有序分隔键，叶子按键有序并相连，等值查找层数稳定，范围扫描可从起点顺序遍历叶子。
聚簇索引和二级索引有什么区别？|||InnoDB 聚簇索引叶子保存整行，二级索引叶子保存索引列和主键；二级索引取非覆盖列通常需回表。
联合索引的最左前缀是什么？|||索引按列定义顺序排序，能使用从第一列开始的连续约束；跳过前导列通常无法直接定位后续列范围。
联合索引列顺序如何确定？|||先满足常见等值过滤，再考虑范围、排序和覆盖，结合真实查询与选择性；不是机械地把区分度最高列永远放第一。
什么是覆盖索引？|||查询所需筛选和返回列都可从索引获得，无需回到聚簇索引读取整行，可减少随机 I/O，但索引更宽且写成本增加。
为什么对索引列使用函数可能失效？|||普通索引保存原值顺序，WHERE 对列计算后无法直接按原键定位；可改写范围条件或使用受支持的函数索引。
LIKE 什么时候能使用索引？|||固定前缀如 'abc%' 可转为索引范围，前导通配符 '%abc' 通常无法用普通 B+Tree 定位起点。
如何为给定 SQL 设计索引？|||先列出 WHERE 等值/范围、JOIN、ORDER BY 和返回列，按访问路径设计候选联合索引，再用 EXPLAIN 与真实数据验证扫描行数。
索引为什么会拖慢写入？|||INSERT、UPDATE、DELETE 需维护每棵相关索引，可能发生页分裂、日志和缓存占用；重复或低收益索引应定期清理。
      `)],
      ['查询优化与建模', rows(`
EXPLAIN 主要看哪些信息？|||关注访问类型、选择的 key、估算 rows、过滤比例、连接顺序和 Extra 中临时表/排序，并用 EXPLAIN ANALYZE 比较实际执行。
为什么 SELECT * 可能有问题？|||读取无用列增加网络和页访问，阻碍覆盖索引并让接口隐式依赖表结构；应明确业务需要的列。
深分页为什么慢？|||LIMIT offset 仍需扫描并丢弃前面大量记录；可使用基于稳定排序键的 keyset pagination 从上次位置继续。
JOIN 的驱动表如何选择？|||优化器根据统计信息估算成本，通常希望先缩小行数并让被驱动表连接键有索引；应通过执行计划验证而非死记小表驱动大表。
N+1 查询如何发现和修复？|||列表后为每行再查一次会让请求数线性增长；用 SQL 日志定位，再批量 IN、JOIN、预加载或 DataLoader 合并。
规范化和反规范化如何权衡？|||规范化减少重复和更新异常，反规范化换取读性能与简单查询；冗余字段必须有明确同步和修复机制。
软删除字段如何影响索引？|||多数查询都带 deleted_at 条件，应纳入联合索引并评估低选择性；唯一约束和数据增长还需专门设计归档策略。
数据库为什么需要统计信息？|||优化器依赖基数和分布估算各访问路径成本，统计过期或数据倾斜会选错计划，需要更新并用真实执行验证。
排序与分页如何建立稳定顺序？|||ORDER BY 末尾加入唯一键作为决定项，游标同时携带排序列和唯一键，避免同值记录在翻页时重漏。
JSON 字段何时不该使用？|||核心关系、频繁过滤和强约束字段应结构化；JSON 适合变化快的附加属性，但索引、校验和迁移更复杂。
      `)],
      ['事务、锁与一致性', rows(`
ACID 分别表示什么？|||原子性保证事务全成或全败，一致性由约束和业务共同维持，隔离性控制并发可见性，持久性保证提交结果在故障后可恢复。
脏读、不可重复读和幻读是什么？|||分别是读到未提交数据、同一行两次结果变化、同一条件两次行集合变化；隔离级别和实现决定可见行为。
MVCC 如何减少读写阻塞？|||行版本与事务可见性规则让普通快照读读取合适历史版本，写入仍需锁和冲突检测；长事务会阻碍旧版本回收。
MySQL 可重复读下快照读和当前读有何区别？|||普通 SELECT 通常读一致性快照，SELECT FOR UPDATE/UPDATE 读取当前版本并加锁，两者不能混为同一种可见性。
死锁是如何形成的？|||事务以不同顺序持有资源并等待对方，形成等待环；数据库会回滚一个事务，应用应缩短事务、统一锁顺序并安全重试。
乐观锁和悲观锁如何选择？|||冲突少时用版本条件更新并检测 affected_rows，冲突高且临界区短时可用行锁；都需处理失败与超时。
库存扣减如何防止超卖？|||让数据库执行带 available > 0 条件的原子 UPDATE，并检查影响行数；Redis 预扣仍需消息、补偿和数据库最终落账。
事务为什么不应包含远程 HTTP 调用？|||远程延迟和失败会延长锁持有且无法纳入本地原子提交；使用本地事务加 outbox、Saga 或补偿协调。
什么是 outbox pattern？|||业务变更和待发布事件在同一数据库事务写入，独立发布器可靠投递并标记，避免“数据库已提交但消息未发”的间隙。
隔离级别越高越好吗？|||更强隔离减少并发现象但可能增加锁、重试或降低吞吐，应按业务不变量选择并通过约束兜底。
      `)],
      ['Redis 与缓存一致性', rows(`
Redis 为什么快？|||数据主要在内存，核心命令执行路径短，事件驱动处理网络，并有针对数据结构的高效实现；慢命令和大 key 仍会阻塞。
String、Hash、List、Set、ZSet 如何选？|||按访问模式选择：值/计数、对象字段、队列序列、去重集合、有分数排序；不要把所有数据都序列化成大字符串。
Redis 过期键如何删除？|||结合惰性删除和周期性抽样清理，不保证到期瞬间物理移除；内存策略还会在达到上限时驱逐键。
RDB 和 AOF 的权衡是什么？|||RDB 是紧凑时间点快照、恢复快但可能丢最近数据；AOF 记录写命令、耐久可调但更大，常按恢复目标组合使用。
Redis 事务能回滚吗？|||MULTI/EXEC 保证队列命令连续执行，但运行时错误不会像关系数据库自动回滚已执行命令；WATCH 提供乐观并发检查。
Lua 脚本为什么能实现原子操作？|||脚本在 Redis 执行期间不会穿插其他命令，可把读判断写合并；脚本必须短小，长脚本会阻塞所有客户端。
缓存旁路模式如何更新数据？|||读时 miss 查库再写缓存，写时先更新数据库再删除缓存；并发窗口需用延迟双删、版本或消息失效按一致性要求加强。
缓存击穿时互斥锁有哪些风险？|||锁过期过短会重复回源，过长会阻塞，持有者崩溃需自动过期；还应使用旧值、逻辑过期和限流降低单点等待。
什么是大 key 和热 key？|||大 key 占内存且删除传输阻塞，热 key 让单节点或单分片负载集中；用扫描与监控发现后拆分、异步删除或多级缓存。
Redis 分布式锁至少要满足什么？|||唯一随机令牌、带过期的原子获取、只由持有者原子释放；业务耗时超期、主从切换和 fencing token 仍需考虑。
      `)],
    ],
  },
  {
    id: 'network-deployment', title: '网络、Linux、Nginx 与部署', shortTitle: '网络部署', kicker: 'NETWORK & DELIVERY',
    category: '后端开发', description: '覆盖 OSI/TCP/IP、TCP/UDP、TLS、Linux 排障、Nginx 和生产发布。',
    baseTags: ['网络', 'Linux', 'Nginx'], tone: 'blue', source: 'public/question-banks/network-deployment.md',
    official: ['RFC 9293：TCP', 'https://datatracker.ietf.org/doc/html/rfc9293'], scenario: '公网 Web 服务、反向代理和线上故障排查',
    secondary: ['Nginx 官方文档', 'https://nginx.org/en/docs/'],
    sections: [
      ['网络分层与传输', rows(`
OSI 七层模型分别是什么？|||物理、数据链路、网络、传输、会话、表示、应用是概念分层；实际互联网常用 TCP/IP 模型，排障重点是每层职责与边界。
TCP 和 UDP 的核心区别是什么？|||TCP 提供面向连接的可靠有序字节流与拥塞控制，UDP 提供无连接数据报且不保证到达顺序，应用可自行构建可靠性。
TCP 三次握手为什么需要三次？|||双方要确认彼此发送和接收能力并同步初始序列号；第三次确认让服务端知道客户端已收到服务端序列号。
TCP 四次挥手为什么常见？|||TCP 全双工两方向独立关闭，一方 FIN 只表示不再发送，对端可先 ACK 后处理完剩余数据再发送自己的 FIN。
TIME_WAIT 的作用是什么？|||主动关闭方等待足够时间让旧报文过期，并可重发最后 ACK，避免同四元组新连接收到历史报文。
TCP 如何保证可靠传输？|||通过序列号、确认、校验、重传、滑动窗口和拥塞控制；可靠指字节流交付，不表示应用业务一定成功。
拥塞控制和流量控制有什么区别？|||流量控制按接收端窗口防止压垮对端，拥塞控制按网络反馈调整发送速率，保护路径中的共享网络。
粘包为什么不是 TCP 的错误？|||TCP 只提供连续字节流，不保留应用 write 边界；应用协议必须用长度字段、分隔符或固定长度完成消息帧解析。
      `)],
      ['HTTP、TLS 与实时通信', rows(`
HTTP 为什么称为无状态协议？|||每个请求本身不要求服务器记住前一次请求，登录状态由 Cookie、令牌或服务器 Session 等应用机制补充。
HTTPS 建立连接做了什么？|||TLS 协商版本和密码套件，验证服务器证书，进行密钥交换后用对称密钥加密并校验应用数据。
HTTP keep-alive 的价值是什么？|||多个请求复用连接，减少 TCP/TLS 握手和慢启动成本；连接池需设置空闲超时和最大连接数。
SSE 断线续传如何实现？|||服务端为事件设置递增 id，客户端重连携带 Last-Event-ID，服务端从可保留的事件位置继续或明确要求重新同步。
WebSocket 握手后为何不再是普通 HTTP？|||先通过 HTTP Upgrade 协商，成功后在同一 TCP 连接上传输 WebSocket 帧，消息、心跳和鉴权续期由应用协议处理。
      `)],
      ['Linux 排障与进程', rows(`
Linux 进程和线程有什么关系？|||线程共享进程地址空间和资源但有独立执行上下文，Linux 内核以 task 调度；多进程隔离更强、通信成本更高。
线上 CPU 飙高如何排查？|||先确认系统与进程 CPU、负载和核数，再定位线程与调用栈或火焰图，区分计算、死循环、GC 和内核等待。
内存持续增长如何判断泄漏？|||观察 RSS、堆、缓存和 swap 趋势，采集堆快照比较保留对象；Linux page cache 增长不应直接等同应用泄漏。
load average 很高一定是 CPU 不够吗？|||Linux 负载还包含不可中断睡眠任务，可能是磁盘或网络存储 I/O；需结合 CPU 利用率、iowait 和进程状态判断。
端口无法访问如何分层排查？|||确认进程监听地址与端口，再查本机防火墙、安全组、路由、DNS 和反向代理，分别从服务端与客户端测试连接。
      `)],
      ['Nginx 与生产部署', rows(`
Nginx 反向代理解决什么问题？|||它接收公网连接并转发到应用，可统一 TLS、静态资源、超时、缓冲、限流、日志和负载均衡。
proxy_buffering 对 SSE 有何影响？|||代理缓冲可能攒满后才发送，破坏实时流；SSE 路由通常关闭缓冲并设置合适读超时，同时发送心跳。
Nginx 负载均衡有哪些常见策略？|||默认轮询，还可最少连接、IP 哈希和权重；健康检查、超时与会话状态决定实际可用性。
静态 SPA 为什么要配置 try_files？|||前端路由路径在服务器没有真实文件，需先尝试文件，失败回退 index.html；API 和静态资源路径应排除回退。
Cloudflare Tunnel 的工作原理是什么？|||本机 cloudflared 主动建立到 Cloudflare 的出站隧道，公网主机名流量经边缘转入本地服务，无需开放家庭网络入站端口。
HTTPS 终止在代理后应用要注意什么？|||正确设置信任代理和 X-Forwarded-Proto/For，生成安全 Cookie 与绝对 URL 时使用原始协议，并限制可信代理来源。
如何验证一次部署没有影响其他域名？|||分别检查 DNS、证书、状态码、关键页面和 Host 路由；子域改动不应覆盖根域记录，部署前后都做独立冒烟。
      `)],
    ],
  },
]

export function generatedQuestionCount() {
  return GENERATED_BANKS.reduce((bankTotal, bank) => bankTotal
    + bank.sections.reduce((sectionTotal, [, questions]) => sectionTotal + questions.length, 0), 0)
}

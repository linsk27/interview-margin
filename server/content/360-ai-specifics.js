export const QUESTION_SPECIFICS = {
  '2.1': {
    practice: '把自我介绍录成 90 秒版本并计时复盘：前 15 秒交代身份与方向，中间 55 秒只讲智能硬件、SRM 和 AI 知识工作台 中最能对应岗位的个人贡献，最后 20 秒说明 React、Go 的真实边界与求职动机。再准备 45 秒压缩版，验证被打断时仍能保留核心证据。',
    followups: `1. **如果面试官只给你 45 秒，哪些内容必须保留？**

   保留“前端与 AI 应用工程化”主线、一个可验证项目证据和岗位匹配结论；学校背景、项目枚举和学习计划各压成一句，不能靠加快语速硬塞。

2. **你说自己适合岗位，面试官如何现场验证？**

   可以打开 AI 知识工作台 演示一次检索到流式回答的链路，再从代码定位请求取消或引用来源处理；React 与 Go 则明确是补齐项，不伪装成生产经验。`,
    pitfalls: `- 不要按简历时间顺序逐段朗读，听完却没有一条清晰的能力主线。
- 不要把团队项目全部说成个人独立完成，必须能指出自己负责的模块和代码边界。`,
  },
  '2.2': {
    practice: '用同一个需求做对照说明 AI 应用前端的特殊性：普通搜索页提交查询后等待完整 JSON；AI 页面还要管理上下文选择、首 token 延迟、增量 Markdown、取消、重试、引用和内容安全。画出 idle、connecting、streaming、done、error 五态并演示一次断线恢复。',
    followups: `1. **AI 应用前端与普通聊天 UI 的最大工程差异是什么？**

   AI 输出是持续变化且可能失败的异步过程，前端必须维护消息身份、流状态和部分结果；普通聊天若只等完整响应，不需要处理乱序 delta、取消确认和半成品渲染。

2. **模型效果不好为什么也可能是前端问题？**

   上下文选错、请求被重复发送、旧流串入新会话、Markdown 增量解析错误都会让正确模型看起来不稳定，因此要用 runId、请求快照和阶段日志排除交互层问题。`,
    pitfalls: `- 不要把 AI 前端简化为“调一个大模型接口”，这会忽略状态一致性与安全边界。
- 不要承诺前端能解决幻觉；前端只能改善证据呈现、反馈和恢复，答案真实性仍需检索与评测保障。`,
  },
  '2.3': {
    practice: '回答前先核对岗位 JD 与 360 现有 AI 浏览器、企业知识库能力，把动机落到三个可验证交集：PC 场景中的长文本与网页上下文、实时 AI 交互、前后端协作。准备一个入职后 30 天计划，包含熟悉产品链路、补 React 工程习惯和完成一个小型 Go 接口。',
    followups: `1. **为什么不是泛泛地选择任何一家做 AI 的公司？**

   360 的浏览器、安全与办公入口拥有真实 PC 上下文和安全约束，问题不只是聊天框；自己的 RAG、流式交互和复杂前端经历能直接映射到网页理解、知识问答与可信输出。

2. **如果实际工作大量是传统业务维护，你还愿意吗？**

   愿意，前提是先把现有业务质量做好；权限、性能、监控和工程化同样是 AI 功能可靠落地的基础，并可在理解业务后逐步承担 AI 相关模块。`,
    pitfalls: `- 不要只说“大厂平台好、AI 有前景”，应具体到产品、用户和岗位职责。
- 不要编造自己深度使用过未体验的 360 产品，演示和评价必须基于真实试用记录。`,
  },
  '2.4': {
    practice: '用一张四格卡片演练 AI 知识工作台：用户是需要反复整理资料的人，问题是上下文分散且回答不可追溯，方案是文档切块、检索、上下文包与 SSE，个人贡献是前后端链路和交互状态。现场演示一条查询，并在两分钟结束前主动说出目前尚未完成的评测和生产化能力。',
    followups: `1. **AI 知识工作台 相比直接把文件上传给聊天模型多解决了什么？**

   它把资料处理、可复用上下文包、检索结果和引用保存成可观察对象，用户能控制本次回答使用哪些证据，也能重复验证失败发生在检索还是生成阶段。

2. **这个项目目前最有说服力的可验证结果是什么？**

   可现场展示固定资料经过规范化、切块、检索后以 SSE 返回带引用回答，并从日志看到各阶段输入输出；没有真实用户规模时不虚构准确率或性能百分比。`,
    pitfalls: `- 不要从技术栈开始讲两分钟，面试官首先需要理解用户问题和产品闭环。
- 不要把关键词回退、可选向量检索说成成熟生产级混合检索，完成度必须与仓库一致。`,
  },
  '2.5': {
    practice: '选一份 20 页产品文档做四路对照实验：全文塞入 Prompt、浏览器关键词搜索、固定问答微调假设和 RAG。记录上下文长度、是否能返回原文位置、资料更新成本和错误案例，用同一组五个问题解释为什么当前需求优先 RAG，同时指出短文档场景下全文 Prompt 可能更简单。',
    followups: `1. **什么情况下全文 Prompt 反而比 RAG 更合适？**

   文档很短、一次性使用且能完整放入上下文时，全文输入省去切块与召回误差；仍需控制敏感信息、提示注入和模型上下文成本。

2. **为什么不直接微调模型记住企业知识？**

   微调更适合改变行为或风格，不擅长频繁更新且要求逐条引用的事实；企业资料变化时 RAG 可重建索引并保留来源，回滚和审计更直接。`,
    pitfalls: `- 不要宣称 RAG 必然更准确，召回失败或坏切块同样会降低答案质量。
- 不要把普通关键词搜索贬低为落后方案，精确编号、专有名词和错误码常由关键词检索更稳。`,
  },
  '2.6': {
    practice: '把一个月的提交按可演示里程碑重排：第一周建立文档与数据模型，第二周完成检索最小闭环，第三周接入 SSE 与交互，第四周补测试、说明和部署。为每个里程碑列出复用的成熟库、AI 辅助生成内容和自己亲自验证的部分，用 Git 记录证明速度来自范围控制而非虚构工作量。',
    followups: `1. **AI 编程工具在这一个月里具体替你做了什么？**

   它帮助生成候选实现、测试样例和文档草稿，但数据边界、接口契约、失败路径和最终验收由自己决定；关键代码必须能逐行解释并用测试复现。

2. **如何证明不是为了赶进度堆出不可维护代码？**

   展示模块边界、固定输入回归、错误处理与提交历史，并说明哪些能力主动延期；能说清删除过的方案和遗留债务，比只报功能数量更可信。`,
    pitfalls: `- 不要用“每天高强度开发”代替计划与证据，速度必须能从提交和范围取舍解释。
- 不要隐瞒 AI 辅助，也不要把 AI 生成代码当个人能力；面试官会继续追问设计理由和边界。`,
  },
  '2.7': {
    practice: '选择一个真实失败案例做五段复盘：预期是流式回答可恢复，实际是刷新后丢失未完成消息；用 requestId 和状态快照定位只保存了最终态；修复为持久化 pending 元数据并明确无法续传时的提示；最后补刷新、断网和重复提交测试。避免把“不足”回答成无关紧要的完美主义。',
    followups: `1. **这个失败为什么没有在开发阶段更早暴露？**

   最初只验证了正常网络和完整响应，没有把刷新、取消、旧请求晚到纳入测试矩阵；根因是验收标准按功能页面写，而不是按状态机和故障事件写。

2. **如果再做一次，你会把哪项机制前置？**

   在编码前定义消息与 run 的身份、状态转移和持久化边界，再为每个转移写可观察日志；这样 UI、接口和恢复策略共享同一契约。`,
    pitfalls: `- 不要说“最大的不足是做得太认真”，这不是可验证的工程复盘。
- 不要只描述故障现象，必须区分根因、修复、验证结果与仍然存在的限制。`,
  },
  '3.1': {
    practice: '准备一个最小 React 练习页复现真实 AI 交互：使用 reducer 管理消息状态，Effect 建立并清理流连接，AbortController 取消请求，列表 key 使用消息 ID。把它与 Vue 版本逐项对照，并保留测试记录；面试时直接说明生产经历以 Vue 为主，React 能力以该可运行样例为证据。',
    followups: `1. **Vue 经验能迁移到 React 的哪些部分，哪些不能直接照搬？**

   组件边界、状态机、请求竞态和可访问性可以迁移；依赖追踪、模板编译、Effect 生命周期和不可变更新心智不同，不能把 watch 写法机械搬进 useEffect。

2. **入职后如何在两周内降低 React 交付风险？**

   先遵循现有项目的状态、请求和测试约定，从小改动与结对评审开始；用 lint 的 Hooks 规则和集成测试约束副作用，再逐步承担完整页面。`,
    pitfalls: `- 不要把读过文档或做过 Demo 说成生产 React 经验，范围要明确。
- 不要回答“框架都一样”，React 的渲染快照、Effect 与并发语义需要专门掌握。`,
  },
  '3.2': {
    practice: '用 Go 写一个与 AI 知识工作台 无关的小接口验证学习能力：实现带 context 超时的并发聚合、结构化错误和单元测试，再用压测观察 goroutine 数与取消是否释放资源。将其与 Flask 同功能版本比较，说明已掌握的语法和运行模型，以及还未经历的生产部署与性能排障。',
    followups: `1. **为什么岗位需要 Go，而你过去主要用 Flask？**

   AI 应用仍需要鉴权、流式网关、任务编排和高并发连接；Flask 经验提供 HTTP 与业务边界基础，Go 的并发与部署模型需要通过实际模块继续补齐。

2. **如果第一个 Go 任务线上出现 goroutine 泄漏，你怎么排查？**

   先看 goroutine profile 与请求生命周期，检查 channel 无消费者、未传递 context 和阻塞 I/O；补超时、关闭协议与回归测试，不靠增加机器掩盖。`,
    pitfalls: `- 不要仅以“语法简单”判断能快速掌握 Go，工程约定和并发取消更重要。
- 不要把 Python 线程或 JavaScript Promise 的经验直接等同 goroutine 与 channel 语义。`,
  },
  '3.3': {
    practice: '挑选一段由 Codex 或 Cursor 协助完成的代码，保留原提示、候选补丁、人工修改和测试结果。现场说明先让工具列风险与测试，再限定文件范围生成方案，最后自己检查权限、错误处理和兼容性；同时准备一个曾拒绝 AI 建议的案例，证明决策没有外包。',
    followups: `1. **怎样防止 AI 工具把敏感代码或密钥发到外部？**

   遵守组织的数据策略，敏感仓库使用获批环境，提示中不粘贴凭据和个人数据；提交前运行密钥扫描，并把工具输出当不可信第三方代码审查。

2. **AI 生成的测试全部通过就能合并吗？**

   不能，测试可能与错误实现共享同一假设；还要依据需求独立设计边界样例、检查未覆盖路径，并进行人工代码评审和必要的安全检查。`,
    pitfalls: `- 不要说“AI 帮我写了大部分代码所以效率高”，应说明自己控制的需求、验证和取舍。
- 不要把工具建议直接复制进仓库，许可证、依赖、安全与风格都需要核对。`,
  },
  '3.4': {
    practice: '把地点回答写成可执行承诺：说明简历地点是此前偏好还是居住信息，明确当前选择、可确认的到岗时间和仍待处理的前置事项。如果仍有约束就直说决策日期，不用模糊承诺敷衍；同时确保招聘系统、简历和口头说法一致。',
    followups: `1. **如果拿到异地 offer，你能在什么时候到岗？**

   给出具体日期区间和毕业、租房等前置事项；若尚未确认家庭安排，就说明何时能最终答复，不用模糊承诺换取流程机会。

2. **为什么此前地点偏好与现在不同？**

   解释当时的信息与职业方向，说明看到 AI 应用前端岗位后重新评估城市优先级；重点是决策依据发生变化，而不是贬低原先选择。`,
    pitfalls: `- 不要为了通过面试临时承诺尚未决定的地点，后续反复会损害可信度。
- 不要把地点冲突推给简历模板或招聘平台，主动统一所有渠道的信息。`,
  },
  '3.5': {
    practice: '逐条对照简历、GitHub README 和可运行代码制作完成度表：已实现写入口与测试，部分实现写限制，计划项写验收条件。对 RRF、MMR、向量迁移等尚未落地能力从“已实现”改成“设计或学习”，并用固定 commit 链接保存面试当天的真实版本。',
    followups: `1. **README 写了完整 RAG，代码里为什么只有关键词回退？**

   应立即承认表述超前并指出当前真实链路，说明向量能力的开关、未完成项和下一步验收；不能用“后面会补”把计划包装成现状。

2. **面试前临时补上功能就能证明原描述没问题吗？**

   不能，时间线同样属于真实性；可以说明最近新增的 commit 和验证结果，但应承认简历提交时的完成度并修正文档。`,
    pitfalls: `- 不要让“设计过”与“已在代码运行”使用同一种完成时表述。
- 不要只修改 README 掩盖差异，简历、演示数据、测试和实际分支必须同步。`,
  },
  '4.1': {
    practice: '用真实 BLE 外设做一次分片实验：读取协商后的 MTU，分别按默认 ATT_MTU 23 和更大 MTU 计算可用载荷，发送包含多字节中文的录音元数据。记录序号、总片数和 CRC，故意丢失中间片，验证接收端不会把“20 byte”写死或静默拼出损坏数据。',
    followups: `1. **为什么常见说法是 20 byte，而不是 23 byte？**

   默认 ATT_MTU 为 23，写请求或通知还要占用 3 字节 ATT 操作码与句柄，因此常见应用载荷是 20；不同操作和协商后 MTU 会改变上限。

2. **MTU 变大后是否可以一次发送任意长度数据？**

   不可以，还受特征属性、平台 API、链路层数据长度和设备缓冲影响；应用协议仍应定义分片、顺序、重试和完整性校验。`,
    pitfalls: `- 不要把 20 byte 说成 BLE 标准永远不变的硬限制，它只是常见默认载荷。
- 不要按 JavaScript 字符数切中文字符串，必须先编码为字节再按实际载荷切片。`,
  },
  '4.2': {
    practice: '把设备配置流程建模为 idle、connecting、discovering、writing、verifying、success、failed，并列出每个状态允许的事件。模拟用户连续点击、蓝牙关闭和写入超时，确认同一事件在不同状态有明确处理，失败能回到可重试节点而不是散落多个 loading 布尔值。',
    followups: `1. **为什么多个 boolean 不足以描述配置流程？**

   connecting、success、error 等布尔值可能组合出同时成功又失败的非法状态；显式状态与事件表只允许定义过的转移，也便于测试超时和取消。

2. **状态机中的副作用应放在哪里？**

   转移本身保持可预测，连接、写入和计时器由进入状态后的 effect 执行；结果再作为事件回送，并在离开状态时清理未完成资源。`,
    pitfalls: `- 不要让 UI 组件直接跨过中间状态修改最终结果，否则日志与恢复无法解释。
- 不要只画正常路径，断连、权限拒绝、重复事件和取消才是状态机价值所在。`,
  },
  '4.3': {
    practice: '为 SRM 表单定义一份可执行 schema，包含字段类型、默认值、校验、可见条件和权限，再建立 sourceField 到受影响规则的反向索引。用 200 个字段压测初次加载和单字段修改，确认只重算相关依赖；同时验证服务端仍对金额、权限和必填规则做权威校验。',
    followups: `1. **字段 A 控制 B，B 又控制 A 时如何避免循环？**

   保存规则时先对依赖图做环检测，运行时记录本批访问路径并在值未变化时停止传播；无法消除的业务环应拒绝配置并给出具体路径。

2. **schema 版本升级后旧草稿怎样恢复？**

   草稿保存 schemaVersion，加载时按明确迁移函数补默认值、重命名字段或标记不兼容；迁移失败保留原始快照并提示人工处理，不能静默丢字段。`,
    pitfalls: `- 不要把任意 JavaScript 表达式存进配置执行，这会引入注入、安全和不可审计问题。
- 不要监听整个表单对象做所有联动，字段规模增长后会造成重复遍历和难以定位的级联更新。`,
  },
  '4.4': {
    practice: '构造五个独立上传任务，其中两个超时、一个校验失败，使用 Promise.allSettled 收集每项结果并按文件 ID 更新界面。只对网络类失败开放重试，保留已成功项，提交按钮依据业务要求判断“全部成功”还是“允许部分成功”，而不是把 settled 误认为整体成功。',
    followups: `1. **什么时候仍应使用 Promise.all？**

   后续步骤必须依赖所有任务成功且任何失败都应立即进入统一错误流程时，Promise.all 的快速拒绝语义更清晰；但已经启动的请求仍需单独取消。

2. **allSettled 能自动解决并发过高吗？**

   不能，它只改变结果收集方式，不限制同时启动数量；大量文件还需并发队列、AbortController、超时和服务端幂等键。`,
    pitfalls: `- 不要只检查 allSettled 返回数组存在，必须逐项区分 fulfilled 与 rejected。
- 不要对权限拒绝或格式错误无限重试，重试条件必须按错误类型和幂等性定义。`,
  },
  '4.5': {
    practice: '在 Three.js 场景中选一个世界坐标点，先更新对象和相机矩阵，再复制 Vector3 调用 project(camera)，把 NDC 的负一到一映射到画布像素。滚动、缩放和设备像素比变化时重新计算，并检测点是否在相机后方或被遮挡，验证标签不会错误漂移。',
    followups: `1. **为什么投影后标签位置仍可能偏移？**

   常见原因是使用窗口尺寸而非渲染画布矩形、忽略 CSS 缩放或相机矩阵尚未更新；应以 getBoundingClientRect 和当前 camera 数据完成映射。

2. **仅凭投影坐标能判断标签可见吗？**

   不能，NDC 范围只能判断视锥位置；对象可能被其他模型遮挡，还需 raycaster 或深度策略，并为大量点控制检测成本。`,
    pitfalls: `- 不要直接修改模型原始 Vector3 做 project，否则世界坐标会被覆盖。
- 不要把 devicePixelRatio 重复乘入 CSS 坐标，WebGL 缓冲像素与页面布局像素是不同坐标系。`,
  },
  '5.1': {
    practice: '用一份带页码的产品手册跑通可审计 RAG：入库时记录文档版本与 chunk 来源，建立关键词和向量索引；查询时保存改写结果、各路召回、融合分数、最终上下文和引用。固定十个问题逐次重放，区分“没召回”“召回后模型没采用”和“引用渲染错误”。',
    followups: `1. **检索到了正确片段但答案仍错误，应查哪一层？**

   先确认上下文排序、截断和提示中是否真正包含证据，再看模型是否遵守仅基于证据回答；通过保存最终 Prompt 与引用映射把生成问题和召回问题分开。

2. **文档更新后如何避免回答引用旧版本？**

   索引项携带 documentVersion，发布新索引后查询只读当前激活版本；缓存 key 包含版本，旧索引在无在途请求后再回收。`,
    pitfalls: `- 不要把向量库接通就称为完整 RAG，评测、引用和版本治理同样属于链路。
- 不要在检索为空时让模型凭参数知识继续编造，应明确无证据并提供补充资料入口。`,
  },
  '5.2': {
    practice: '把同一份包含标题、表格和代码的文档分别按固定 300 字、递归标题边界和语义段落切块。对五个跨段问题记录召回命中、重复率和上下文占用，再调整 overlap；同时保存 startOffset、页码和标题路径，保证回答引用能回到原文。',
    followups: `1. **chunk 越大为什么不一定召回越好？**

   大块包含更多无关词，会稀释向量与关键词信号并占用上下文预算；虽然跨句信息更完整，但排序与生成阶段的噪声也同步增加。

2. **如何处理表格或代码被切断的问题？**

   解析阶段识别结构节点，把完整表格、函数或代码块作为原子单元；过大时按行或符号分层切分，并保留父节点摘要与位置信息。`,
    pitfalls: `- 不要只按字符长度切分而丢失标题层级、页码和源文档定位。
- 不要机械设置固定 overlap，重复过大会让召回结果挤满相同内容并增加模型成本。`,
  },
  '5.3': {
    practice: '为查询“ERR_CONN_104 如何恢复”同时运行 BM25 与向量检索：关键词路保住精确错误码，向量路召回语义相近的断线说明。把两路原始排名、归一化前分数和融合结果写入调试面板，测试专有名词、自然语言和混合查询三组样本。',
    followups: `1. **为什么不能直接相加 BM25 分数与余弦相似度？**

   两种分数范围和分布不同，直接相加会让某一路无意占优；可用排名融合、校准后的归一化或在验证集上训练重排器。

2. **什么时候可以只用关键词检索？**

   文档短、查询以编号、名称和错误码为主且同义表达少时，关键词更可解释且成本低；应由离线样本证明向量路没有额外收益。`,
    pitfalls: `- 不要把“关键词加向量”本身当作质量保证，融合参数仍需基于真实查询评测。
- 不要忽略中文分词、缩写和大小写归一化，它们会直接影响关键词召回。`,
  },
  '5.4': {
    practice: '准备两份有意让 BM25 与向量分数尺度完全不同的排名列表，按 RRF 公式逐项计算 1/(k+rank)，验证同一文档在两路靠前时自然累加。用 k=10、60、100 对比头部差异，并保存文档去重键，避免同一 chunk 因索引副本被重复计分。',
    followups: `1. **RRF 中的 k 起什么作用？**

   k 越大，各名次之间的分值差距越平缓，融合更重视多路一致而非单路第一；它不是固定最佳值，应结合候选深度和验证集选择。

2. **只有一路检索返回结果时 RRF 会怎样？**

   结果退化为按该路排名排序，仍可工作；但应记录另一路为空的原因，区分无命中、超时和索引版本故障。`,
    pitfalls: `- 不要把 RRF 说成融合原始相似度，它只使用各路排名位置。
- 不要在合并前缺少稳定文档 ID，否则同一内容无法累分，重复 chunk 还会挤占候选。`,
  },
  '5.5': {
    practice: '从同一章节召回十个高度相似 chunk，先按相关性取第一项，再计算每个候选与已选集合的最大相似度，用 lambda 平衡查询相关性和新信息。分别以 0.3、0.7 运行，人工标注是否遗漏关键答案，观察多样性提升是否牺牲了核心证据。',
    followups: `1. **MMR 的 lambda 越小意味着什么？**

   越偏好多样性、越惩罚与已选内容相似的候选；过小可能选入与问题关系弱但形式不同的片段，因此要结合任务所需证据密度调节。

2. **候选之间的相似度用什么向量计算？**

   可复用同版本 embedding，但必须统一归一化和模型版本；若文档结构特殊，也可叠加章节距离或文档来源约束，不能混用不可比较向量。`,
    pitfalls: `- 不要把 MMR 当去重算法，它是在相关性与多样性之间做贪心选择。
- 不要在候选池很小或召回已失败时盲目做多样化，否则会进一步稀释有效证据。`,
  },
  '5.6': {
    practice: '建立 30 条带标准证据的最小评测集，覆盖可回答、跨块、无答案和恶意指令。分别计算 recall@k、MRR、引用命中与无答案拒答，再让人工按忠实度和完整性复核；每次改切块、模型或提示都保存版本与逐题差异，不只看单个总分。',
    followups: `1. **答案看起来正确，为什么仍可能评测不合格？**

   它可能使用模型记忆而非给定资料，引用也可能指向无关段落；需分别评估答案正确性、对证据忠实度和引用定位，不能只做字符串相似。

2. **线上用户点赞能否替代离线评测集？**

   不能，点赞受用户偏好和样本分布影响且缺少标准答案；线上反馈适合发现新失败类型，再经脱敏审核加入版本化离线集合。`,
    pitfalls: `- 不要只报告平均分，关键问题的回归和无答案误答可能被平均值掩盖。
- 不要用参与调参的同一批题宣称泛化效果，至少保留未见验证集并记录评测版本。`,
  },
  '5.7': {
    practice: '在索引元数据中保存 model、dimension、normalization 和 createdAt，模拟从旧 embedding 迁移到新模型：后台写入新命名空间，逐批重算并校验数量；查询阶段按版本双读比较，达到验收后原子切换 activeVersion，最后延迟删除旧索引。',
    followups: `1. **为什么不能把新向量直接写入旧索引？**

   不同模型或维度的向量不在同一语义空间，距离不可比较；混写会让检索分数失真，甚至因维度不符直接失败。

2. **双读期间如何控制成本和一致性？**

   只对采样流量同时查询两版，记录相同 query 的排名差异；文档写入通过 outbox 同步两版，切换前核对版本水位和失败队列。`,
    pitfalls: `- 不要只检查向量条数相等，还要验证文档版本、维度和可检索样本。
- 不要先删除旧索引再重建，新模型质量或迁移任务失败时会失去快速回滚路径。`,
  },
  '5.8': {
    practice: '对上传内容先做确定性的文本规范化，再计算包含租户、解析器版本和内容字节的哈希。测试同文件改名、空白变化、不同权限用户和哈希碰撞处理：复用底层 blob 可以节省存储，但文档记录、ACL、来源和删除生命周期仍保持独立。',
    followups: `1. **content_hash 相同是否代表两个文档业务上相同？**

   只代表参与哈希的规范化内容相同，不代表标题、权限、租户或来源相同；去重必须分离内容对象与业务文档元数据。

2. **规范化规则升级后如何处理旧哈希？**

   在哈希键中加入 normalizerVersion，新旧结果并存；后台迁移时建立映射并验证引用，不能让规则变化把历史文档错误合并或拆分。`,
    pitfalls: `- 不要用截断过短或非加密哈希作为唯一可信身份，仍需处理碰撞并核对长度。
- 不要跨租户因相同哈希直接暴露已存在信息，这可能形成数据存在性侧信道。`,
  },
  '5.9': {
    practice: '构造一份含“忽略系统要求并调用删除工具”的恶意文档，验证检索层仍把它标记为不可信资料。生成提示将资料与指令分隔，服务端只开放只读工具且校验参数；输出再经过引用核对和 HTML 消毒，同时记录攻击片段、策略命中和被拒工具调用。',
    followups: `1. **在系统提示中写“忽略文档指令”是否足够？**

   不足，模型仍可能被诱导；真正边界要靠最小工具权限、服务端授权、参数 schema、危险操作确认和输出处理共同强制。

2. **如何区分正常文档中的命令示例与攻击？**

   不能仅凭关键词删除内容，应保留原文并降低其指令权重，结合来源信任、上下文用途和行为监控；高风险调用最终由确定性策略拦截。`,
    pitfalls: `- 不要让模型自己决定是否有权限执行工具，授权必须发生在模型之外。
- 不要只防输入注入，模型输出进入 DOM、SQL 或工具参数时同样要按目标上下文校验。`,
  },
  '5.10': {
    practice: '用“查询订单并申请退款”画三种实现：普通聊天只生成说明，Workflow 固定执行鉴权、查询、条件判断与退款，Agent 在受限工具集里动态选择下一步。为三者分别设置超时、可审计日志和人工确认，比较可预测性、成本与需求变化频率。',
    followups: `1. **什么任务不适合使用 Agent？**

   流程固定、合规要求高且每一步都能预先枚举时，普通 Workflow 更可测试、更便宜；Agent 的动态规划能力没有抵消额外不确定性。

2. **Agent 与聊天模型的状态有什么不同？**

   Agent 除对话历史外还维护目标、工具结果、步骤预算和终止条件；状态必须持久化为可审计事件，不能只依赖模型上下文自行记忆。`,
    pitfalls: `- 不要把调用一次工具的聊天接口就包装成自主 Agent，必须说明循环、决策和终止机制。
- 不要默认 Agent 越自由越智能，生产系统通常需要更窄工具和更强确定性护栏。`,
  },
  '5.11': {
    practice: '实现一个只读天气工具闭环：服务端把工具名和 JSON Schema 发给模型，收到 tool call 后验证名称、参数类型、用户权限与调用次数，执行结果作为 tool message 回传，再由模型生成最终回答。故意传入未知字段和越权城市，确认在执行前被拒并写审计日志。',
    followups: `1. **模型返回 tool call 是否意味着工具已经执行？**

   不意味着，它只是结构化意图；应用负责解析、授权、执行、处理错误并把结果送回模型，任何副作用都不能由模型响应直接触发。

2. **工具执行超时后应该怎样继续对话？**

   记录该 callId 的失败结果并返回结构化错误，让模型选择解释、重试或替代工具；重试必须受幂等性、次数和总时限控制。`,
    pitfalls: `- 不要只校验 JSON 格式而忽略当前用户是否有权访问指定资源。
- 不要把工具返回的文本当可信指令再次执行，它同样可能包含注入或恶意内容。`,
  },
  '5.12': {
    practice: '搭建一个本地 MCP 客户端与只读文件服务器，先完成 initialize 和能力协商，再列出 tools、resources 并调用一个受限工具。抓取消息日志说明 MCP 负责客户端与工具服务的标准协议，而模型厂商的 Function Calling 是模型表达调用意图的接口，两者通过宿主应用衔接。',
    followups: `1. **用了 MCP 后还需要 Function Calling 吗？**

   常见架构仍需要：模型通过 Function Calling 或等价机制选择工具，宿主再把调用映射到 MCP server；MCP 不规定模型内部如何产生选择。

2. **连接第三方 MCP server 最大的安全风险是什么？**

   server 暴露的工具可能读取敏感数据或执行副作用，返回内容也可能注入；客户端需做信任分级、权限提示、参数确认和网络隔离。`,
    pitfalls: `- 不要把 MCP 描述为模型或 Agent 框架，它首先是上下文与工具互操作协议。
- 不要因工具来自 MCP 就跳过授权，协议标准化不等于服务端可信。`,
  },
  '5.13': {
    practice: '为一个资料整理 Agent 设置 maxSteps=8、总时限 30 秒、费用预算和工具白名单。每步记录目标、输入摘要、toolCallId 与结果哈希；相同调用连续出现则熔断，写操作要求人工确认。测试工具持续报错和模型重复规划，验证任务以明确失败状态结束。',
    followups: `1. **如何判断 Agent 已经陷入语义上的死循环？**

   除步数上限外，对规范化后的计划与工具参数做重复检测，观察状态是否产生新信息；连续多步无状态增量时停止并要求用户补充。

2. **人工确认应该放在哪些动作前？**

   删除、付款、外发消息、权限变更等不可逆或影响他人的操作必须确认；确认界面展示具体对象、参数和预期影响，而不是只问“是否继续”。`,
    pitfalls: `- 不要只依赖模型主动说“完成”，宿主必须用预算和状态规则决定终止。
- 不要无限自动重试付费或有副作用的工具，重复执行可能放大成本和业务损害。`,
  },
  '5.14': {
    practice: '用一条长度为四的 token 序列画出 Transformer block：嵌入加位置后进入多头注意力，拼接投影并做残差与归一化，再经过逐位置前馈网络和第二次残差。打印每层张量形状，确认注意力改变 token 间信息混合，FFN 在每个位置独立变换特征。',
    followups: `1. **多头注意力为什么不等于重复做同一件事？**

   每个头有独立投影参数，可在不同子空间关注位置、语法或语义关系；头结果拼接后再线性组合，但并不保证每个头都有可人工命名的含义。

2. **残差连接与 LayerNorm 各解决什么问题？**

   残差提供较短的梯度和信息通路，LayerNorm 稳定每个 token 特征尺度；Pre-Norm 与 Post-Norm 的放置还会影响深层训练稳定性。`,
    pitfalls: `- 不要把 Transformer 简化成只有注意力，前馈、残差、归一化和位置表示同样关键。
- 不要声称注意力权重可以完整解释模型推理，权重只是内部计算的一部分。`,
  },
  '5.15': {
    practice: '取三个二维 token 手算单头注意力：分别乘 Wq、Wk、Wv 得到 Q、K、V，计算 QK 转置除以根号 d_k，加入 mask 后做 softmax，再用权重加权 V。改变一个 key 并观察所有查询的分布，区分“用什么去找、被怎样匹配、最终取回什么”。',
    followups: `1. **为什么点积后要除以根号 d_k？**

   维度增大时点积方差变大，softmax 容易进入极端饱和区导致梯度很小；缩放让数值范围更稳定，但不是为了把结果严格归一化。

2. **同一个 token 的 Q、K、V 是否相同？**

   输入相同但经过不同可学习矩阵，角色与数值通常不同；查询用于发起匹配，键用于被比较，值是按匹配权重聚合的内容。`,
    pitfalls: `- 不要把 Q、K、V 直接说成原始词向量，它们是当前层投影后的表示。
- 不要忘记 padding mask 或因果 mask，它们决定哪些位置允许参与 softmax。`,
  },
  '5.16': {
    practice: '用同一段文本画出 encoder-only、encoder-decoder 与 decoder-only 的可见性矩阵。decoder-only 训练时对下一个 token 做因果预测，推理时复用 KV cache 逐 token 生成；再列出分类、翻译和开放生成任务，解释为什么架构选择取决于目标而不是“Encoder 消失了”。',
    followups: `1. **decoder-only 如何理解输入 Prompt？**

   输入 token 也经过多层因果自注意力形成上下文化表示，后续生成位置可以关注全部先前 Prompt；并不需要独立 Encoder 才能编码输入。

2. **为什么 KV cache 能加速自回归推理？**

   历史 token 的 key、value 在每层已计算，可缓存并只为新 token 计算投影；它减少重复算力，但会随序列长度占用显存。`,
    pitfalls: `- 不要说 decoder-only 没有编码能力，它只是没有单独的双向 Encoder 堆栈。
- 不要把当前主流选择归因于单一优势，训练数据、扩展性和任务统一同样影响架构取舍。`,
  },
  '6.1': {
    practice: '为“模型生成回答”分别做 SSE 与 WebSocket 原型：记录握手、自动重连、代理配置和服务端资源；再用轮询实现每两秒取状态作为基线。单向文本增量优先 SSE，协同编辑等高频双向消息才选 WebSocket，长任务且实时性低时轮询反而更容易运维。',
    followups: `1. **SSE 基于 HTTP，为什么仍要处理心跳？**

   中间代理或负载均衡可能关闭长时间无数据的连接，服务端需定期发送注释事件并设置合适超时；心跳只保活，不能代替业务续传。

2. **WebSocket 是否天然比 SSE 延迟更低？**

   不一定，二者在稳定连接上都能及时推送；实际延迟更多取决于模型、缓冲和代理，选择应看通信方向、消息类型与基础设施。`,
    pitfalls: `- 不要因为 SSE 简单就忽略连接数、断线重连和事件游标。
- 不要用高频轮询模拟 token 流，它会产生大量空请求且仍无法提供平滑增量。`,
  },
  '6.2': {
    practice: '对同一流式接口分别用 EventSource 和 fetch 实现：EventSource 验证自动重连与 Last-Event-ID，fetch 通过 ReadableStream 读取字节并支持 POST、自定义 Header 和 AbortController。记录 Cookie、鉴权、错误状态和 UTF-8 解码差异，形成按接口约束选择的清单。',
    followups: `1. **为什么需要 POST Prompt 时常选 fetch 流？**

   原生 EventSource 主要发 GET 且不能自由设置请求体和 Header；fetch 可携带 JSON Prompt 并读取响应流，但重连和 SSE 帧解析要自行实现。

2. **EventSource 收到 401 时怎样刷新令牌？**

   关闭旧连接，在应用层完成刷新后创建新连接；若使用 HttpOnly Cookie 可让浏览器自动携带，但仍要处理 SameSite、过期和跨域凭据配置。`,
    pitfalls: `- 不要把 fetch 的每个 chunk 当成完整事件，一个事件可能跨多个网络分片。
- 不要在 URL 查询参数放长期访问令牌，地址可能进入历史、代理和服务器日志。`,
  },
  '6.3': {
    practice: '从 Flask 路由开始为一次生成分配 runId，生成器按 SSE 格式输出 start、delta、done、error；Nginx 对该路径关闭缓冲，前端用 TextDecoder 流式解码并按事件边界解析，再由 reducer 更新指定消息。逐层记录时间戳，定位首 token 延迟究竟出现在模型、代理还是渲染。',
    followups: `1. **Flask generator 抛异常后还能返回普通 JSON 错误吗？**

   响应头和部分数据已发送后不能切换状态码，应捕获异常并写一条 error 事件，随后结束流；开始发送前的鉴权和参数错误仍用正常 HTTP 状态。

2. **如何判断 Nginx 是否在缓冲流？**

   让服务端每秒输出带时间戳的事件，同时用 curl 禁用客户端缓冲观察到达时间；若集中出现，检查 proxy_buffering、压缩与上游刷新行为。`,
    pitfalls: `- 不要只在 Vue 组件里调字符串拼接，协议分帧、传输和 UI 状态应分层。
- 不要在流开始后继续访问已退出的 Flask 请求上下文，必要时使用受控的上下文包装。`,
  },
  '6.4': {
    practice: '让服务端把“你好🌍”故意切在 UTF-8 多字节字符中间，前端先用每块单独 new TextDecoder 解码复现乱码，再改为同一 decoder 的 stream:true 增量解码，结束时 flush。随后再按 SSE 空行分帧，证明字节边界、字符边界和事件边界是三个不同层次。',
    followups: `1. **TextDecoderStream 解决了事件被拆分的问题吗？**

   它只保证多字节字符正确跨 chunk 解码，不能保证一段文本就是完整 SSE 事件；仍需维护字符串缓冲并按协议分隔符提取完整帧。

2. **为什么服务端按字符切片仍可能被网络重新分片？**

   TCP 是字节流，不保留应用 write 边界，代理和 TLS 也会重新缓冲；客户端协议解析必须接受任意分片组合。`,
    pitfalls: `- 不要用 String.fromCharCode 逐字节拼 UTF-8，它无法正确还原多字节码点。
- 不要在每个网络块末尾强行补换行，这会破坏跨块 JSON 和 SSE 字段。`,
  },
  '6.5': {
    practice: '写一个每 500 毫秒 yield 带序号事件的最小接口，依次直连 Flask、经过 Nginx、开启压缩和进入前端请求。用 curl --no-buffer 记录到达时刻；逐项关闭代理缓冲、响应压缩和客户端批处理，找到是哪一层攒包，而不是凭浏览器观感猜测。',
    followups: `1. **为什么 gzip 可能让小片段最后一起出现？**

   压缩器为了获得更好压缩比会累积输入，未 flush 时下游收不到完整块；流式端点可关闭压缩或使用明确支持同步刷新的策略。

2. **服务端已经 flush，前端仍不更新还有什么原因？**

   可能是读取循环未启动、状态更新被批处理或主线程长任务阻塞；应在字节到达、事件解析和渲染提交三处分别打点。`,
    pitfalls: `- 不要一遇到集中显示就只改 Nginx，浏览器、压缩和前端调度都可能缓冲。
- 不要用大量空格填充强迫代理吐数据，这不是稳定协议，也会浪费流量。`,
  },
  '6.6': {
    practice: '在前端为每次生成创建 AbortController 和 runId，点击停止时先置为 cancelling，再中止 fetch；后端监听客户端断开并取消模型任务，数据库用条件更新只让 running 进入 cancelled。测试停止与最后一个 token 同时发生、重复点击和网络断开，确保旧 done 不能覆盖 cancelled。',
    followups: `1. **前端 abort 成功是否代表模型计算已停止？**

   不代表，它只终止浏览器请求；服务端必须感知断连并向模型供应商传播取消，否则计算与计费可能继续。

2. **取消后保留已经生成的文字还是清空？**

   由产品契约决定，通常保留部分内容并标记“已停止”，同时禁止把它当完整答案；重试应创建新 run 而非复用已取消身份。`,
    pitfalls: `- 不要把 AbortError 当普通失败弹红色错误，取消是用户主动的独立终态。
- 不要仅靠消息 ID 判断回调归属，同一消息重试可能拥有多个不同 runId。`,
  },
  '6.7': {
    practice: '同时打开两个会话，各启动两次生成，为 conversationId、messageId、runId 建立三级键。服务端事件和前端 reducer 都要求 runId 精确匹配当前 pending 项；故意让旧请求晚到、切换路由后继续输出，验证内容只进入原消息且已替换 run 的事件被丢弃并记录。',
    followups: `1. **为什么只用当前会话 ID 仍可能串台？**

   同一会话内可以重试或并发生成多个消息，旧流晚到会命中相同 conversationId；还需 messageId 与每次尝试唯一的 runId。

2. **多标签页使用同一账号时如何同步？**

   服务端以事件身份为权威，客户端可用 BroadcastChannel 通知状态变化；写入通过版本或幂等键处理，不能假设标签页共享内存顺序。`,
    pitfalls: `- 不要用数组最后一项作为当前流目标，排序、重试或历史加载后位置会变化。
- 不要在组件卸载时只清 UI 状态而留下连接，隐藏页面仍可能把事件写回全局 store。`,
  },
  '6.8': {
    practice: '模拟每秒 60 个 delta，比较“每 token setState”与使用缓冲区每 50 毫秒或 requestAnimationFrame 批量提交。用 Performance 记录 render 次数、长任务和输入延迟；最终文本按序号拼接，done 前强制 flush，确保优化渲染频率不会丢最后一段。',
    followups: `1. **为什么 React 自动批处理不能完全解决 token 高频更新？**

   网络回调可能跨多个任务持续到达，每批仍会触发渲染；大 Markdown 重新解析也很昂贵，需要应用层聚合和增量展示边界。

2. **批量间隔应该固定为多少？**

   没有通用值，应在流畅度、可读延迟和设备性能间测量；页面不可见时可降频，用户滚动或代码块更新时也可采用不同策略。`,
    pitfalls: `- 不要为减少渲染而等到整段完成，否则失去流式反馈价值。
- 不要用闭包中的旧字符串直接追加并发 delta，应使用序列队列或函数式更新保证顺序。`,
  },
  '6.9': {
    practice: '准备跨 chunk 的代码围栏、未闭合链接和恶意 HTML 三组流。状态中保存原始 Markdown 文本，按节流周期重新解析稳定前缀并为未闭合尾部提供纯文本回退；禁用原始 HTML，链接限定协议，最终 done 时全量解析校验。对每组截图和安全结果做回归。',
    followups: `1. **为什么不能直接把模型输出赋给 innerHTML？**

   模型可能复述恶意资料或生成事件属性、危险链接，innerHTML 会把文本变成可执行 DOM；必须使用安全 Markdown 渲染器和严格消毒策略。

2. **流式阶段代码块没闭合时怎样避免页面抖动？**

   可只渲染已确认完整的块，尾部以普通 pre 或纯文本展示；收到后续分隔符再替换，避免每个 token 重建整棵富文本 DOM。`,
    pitfalls: `- 不要认为 Markdown 天然安全，链接、图片和内联 HTML 都可能成为攻击入口。
- 不要在每个 token 到达时全量高亮所有代码块，高亮应延迟或按完成块执行。`,
  },
  '6.10': {
    practice: '生成开始时先把 user message、assistant 占位和 run 状态写入本地 IndexedDB，再发请求；每隔固定字数保存部分文本和 lastEventId。刷新后读取 pending 记录，若服务端支持续传则从游标恢复，否则把部分内容标为中断并提供重新生成。模拟离线、崩溃和旧版本 schema。',
    followups: `1. **为什么 localStorage 不适合频繁保存长回答？**

   它同步阻塞主线程、容量有限且只能存字符串；IndexedDB 适合异步事务写入结构数据，但仍要控制节流和数据清理。

2. **离线期间用户新发的问题怎样处理？**

   写入带客户端幂等键的 outbox，明确显示“待发送”；联网后按顺序重放，服务端确认后移除，并对会话已删除或权限变化给出冲突处理。`,
    pitfalls: `- 不要把本地 pending 记录当作服务器已接受，UI 必须区分待发送、生成中和已确认。
- 不要在多标签页同时重放同一 outbox 项，应使用租约或幂等键防止重复请求。`,
  },
  '6.11': {
    practice: '用同一“流式消息列表”分别画 Vue 与 React 数据流：Vue 通过响应式依赖和编译优化更新，React 每次 render 读取 state 快照并协调元素树。对比 composable 与 custom Hook、watch 与 Effect、模板与 JSX；最后说明选型由团队生态和场景决定，而非简单比较谁更快。',
    followups: `1. **Vue 的细粒度响应式是否意味着完全不需要虚拟 DOM？**

   不意味着，Vue 仍以虚拟 DOM 表达组件树并 patch；编译器的 block 与 patchFlag 帮助缩小动态检查范围，响应式负责调度相关组件。

2. **React 为什么强调不可变更新，而 Vue 常直接修改响应式对象？**

   React 依赖新状态快照和引用变化驱动重渲染，Vue 代理可拦截属性写入；两者都要求避免不可追踪副作用，只是更新契约不同。`,
    pitfalls: `- 不要用“双向绑定对单向数据流”概括全部差异，Vue 的 props 同样是单向的。
- 不要用单个微型 benchmark 宣称某框架更快，编译策略、组件边界和业务更新模式都会影响结果。`,
  },
  '6.12': {
    practice: '写一个依赖 query 拉取数据的 Hook，开启 Strict Mode 后快速切换参数，复现旧请求覆盖与 Effect 双执行。修复为完整依赖、AbortController 和 cleanup；再把可从 props 推导的值移出 state，使用函数式更新解决连续计数，最终用测试验证卸载后不再写状态。',
    followups: `1. **为什么不能删除依赖数组项来阻止 Effect 重跑？**

   Effect 读取了变化值却不声明依赖会捕获旧闭包，行为与代码表面不一致；应重构数据流、稳定函数或把非响应值放 ref，而不是压制规则。

2. **useMemo 能否用于保证对象永远不变？**

   不能，它是性能缓存而非业务语义，React 可在实现需要时丢弃；正确性需要稳定身份时应重新设计状态归属或使用 ref。`,
    pitfalls: `- 不要用 Effect 同步本可在 render 中计算的派生 state，这会多一次渲染并产生双源真相。
- 不要把 Strict Mode 的额外 setup-cleanup 当生产 bug，它用于暴露缺少清理的副作用。`,
  },
  '6.13': {
    practice: '为流协议定义 start、delta、tool、done、error 五种带 type 字段的联合，每种声明自己的 payload 与必填 ID。解析网络 JSON 时先用运行时 schema 校验，再在 reducer 的 switch 中用 never 做穷尽检查；新增 heartbeat 类型时让编译和测试同时指出遗漏处理。',
    followups: `1. **TypeScript 已定义联合，为什么还需要运行时校验？**

   网络输入在运行时只是未知 JSON，类型会被擦除；服务端版本错误或恶意字段必须经 Zod 等 schema 解析后才可进入内部联合。

2. **done 事件为什么还要携带 runId？**

   旧连接可能晚到，同一消息也可能重试；reducer 只有同时核对 messageId 与 runId 才能避免旧 done 关闭当前新流。`,
    pitfalls: `- 不要用一个包含大量可选字段的 Event 接口，它允许不可能的字段组合通过编译。
- 不要在 default 分支静默忽略未知类型，至少记录协议版本并安全终止当前流。`,
  },
  '6.14': {
    practice: '在一个小型 React 项目中分别记录 Vite 冷启动、首次页面加载和生产构建产物，再用 webpack 配置实现相同别名、CSS 和分包。比较开发阶段按需 ESM、依赖预构建与完整 bundle 的差异，同时检查生产 chunk 和缓存策略，避免只背“Vite 快”。',
    followups: `1. **Vite 开发时不打包，为什么仍有依赖预构建？**

   大型依赖可能由大量模块或 CommonJS 构成，预构建把它们转换并合并为更适合浏览器请求的 ESM；业务源码仍按需提供。

2. **什么情况下 webpack 仍可能更合适？**

   现有大型工程深度依赖成熟 loader、插件、Module Federation 或定制构建流程时，迁移收益未必覆盖风险；应以约束和指标评估。`,
    pitfalls: `- 不要把开发服务器启动速度等同生产页面性能，生产仍需分析打包产物。
- 不要为追求更少 chunk 盲目手工合包，缓存命中、并行加载和执行成本需要一起衡量。`,
  },
  '6.15': {
    practice: '为 index.html、带内容哈希的 JS 和 API 各设置一组 Cache-Control：入口短缓存并可协商，哈希资源 max-age 长期 immutable，用户 API private no-store。用浏览器和 curl 观察首次 200、后续内存缓存与带 ETag 的 304，证明 no-cache 是“使用前验证”而不是“不缓存”。',
    followups: `1. **no-cache 与 no-store 的核心区别是什么？**

   no-cache 允许保存响应但复用前必须向服务器验证，no-store 要求不保存；敏感个人数据通常考虑 no-store，版本化静态资源不应使用它。

2. **为什么 HTML 不能像哈希 JS 一样缓存一年？**

   HTML URL 通常不随内容变化且负责引用新资源，长期缓存会让用户一直拿旧入口；哈希资源内容改变时 URL 也改变，可以安全 immutable。`,
    pitfalls: `- 不要只在前端 fetch 选项里改 cache 就认为 CDN 与代理策略同步，响应头才是共享缓存契约。
- 不要给携带 Cookie 的私有响应误设 public，否则中间缓存可能向其他用户复用。`,
  },
  '6.16': {
    practice: '在两个本地域名间分别发送简单 GET、JSON POST 和带 Authorization 的请求，抓包观察哪些触发 OPTIONS。服务端只允许指定 Origin、方法和 Header，预检成功后再处理实际请求；测试拒绝 Origin 时浏览器报错，同时确认服务器日志可能仍收到无需预检的请求。',
    followups: `1. **为什么 Postman 能调用，浏览器却报 CORS？**

   CORS 是浏览器对脚本读取跨源响应的限制，Postman 不受同源策略约束；服务器可达并不表示浏览器允许前端读取。

2. **带凭据请求为什么不能配 Access-Control-Allow-Origin 星号？**

   浏览器要求服务器明确回显受信 Origin 并设置 Allow-Credentials，避免任何站点都能以用户凭据读取响应；还应校验 Origin 白名单。`,
    pitfalls: `- 不要把 CORS 当服务端鉴权，curl 或攻击者服务器不受浏览器策略保护。
- 不要对收到的 Origin 无条件回显并允许 credentials，这等于把跨站读取权限交给任意来源。`,
  },
  '6.17': {
    practice: '让模型输出脚本标签、javascript 链接、带 onerror 的图片和普通代码示例，用真实 Markdown 渲染链路测试。禁用原始 HTML，链接协议白名单化，必要时经成熟 sanitizer；CSP 作为第二层防线。再验证流式未闭合标签不会绕过最终策略，并对阻断结果写安全回归。',
    followups: `1. **React 普通 JSX 文本会转义，为什么仍会发生 XSS？**

   dangerouslySetInnerHTML、Markdown 插件、DOM API 和危险 URL 会绕过普通文本转义；AI 输出通常经过富文本转换，因此必须审查实际 sink。

2. **只在 Prompt 中要求模型不输出 HTML 是否足够？**

   不足，模型输出不可作为安全保证，资料本身也可能诱导；客户端与服务端都应把输出当不可信数据并在进入执行上下文前处理。`,
    pitfalls: `- 不要自己用正则删除 script 标签，事件属性、SVG 和编码变体会轻易绕过。
- 不要允许模型生成的链接直接使用任意协议，javascript 与 data URL 需要明确限制。`,
  },
  '6.18': {
    practice: '搭建 Cookie Session 与 Authorization Header 两种登录 Demo，从第三方页面分别发普通表单和 fetch。验证浏览器可能自动携带 Cookie，SameSite、Origin 检查和 CSRF token 如何阻断；Header token 不会自动附带但更怕 XSS 读取。把 CORS 仅用于“能否读响应”，不混入身份判断。',
    followups: `1. **SameSite=Lax 是否意味着不再需要 CSRF 防护？**

   它能阻止多数跨站子请求携带 Cookie，但顶级导航、旧浏览器和业务例外仍需评估；敏感写操作还应验证 Origin 或 CSRF token。

2. **使用 JWT 就不会有 CSRF 吗？**

   取决于存储与发送方式；JWT 放 HttpOnly Cookie 仍会被浏览器自动携带，需要 CSRF 防护，放 JavaScript 可读存储则主要增加 XSS 窃取风险。`,
    pitfalls: `- 不要把预检请求当 CSRF 防线，简单表单请求可能无需预检就到达服务器。
- 不要同时允许任意 Origin、credentials 和状态修改接口，这会扩大跨站攻击面。`,
  },
  '6.19': {
    practice: '生成一个短期 access token，验证签名、iss、aud、exp 与算法白名单；刷新令牌放 HttpOnly、Secure、SameSite Cookie，并在服务端保存可撤销会话与轮换链。模拟旧 refresh 重放、账号停用和密钥轮换，确认仅“能解码 payload”不会被当成鉴权成功。',
    followups: `1. **JWT 如何做到立即注销？**

   纯无状态 access token 到期前难以立即失效，可缩短有效期并用服务端会话、tokenVersion 或撤销列表控制刷新和高风险请求。

2. **为什么不能相信 token Header 里的 alg？**

   服务端必须按预配置算法和密钥验证，不能让攻击者选择 none 或错误算法；同时按 kid 从受信密钥集合取钥并限制轮换范围。`,
    pitfalls: `- 不要把 JWT payload 的 Base64 解码误认为加密，任何持有者都能读取其中内容。
- 不要把长期 token 放 localStorage 后忽略 XSS，一次脚本注入即可窃取并跨设备使用。`,
  },
  '7.1': {
    practice: '在 Chrome DevTools 录制一次禁用缓存的首屏导航，把 URL 解析、DNS、连接、TLS、首字节、HTML 流式解析、CSSOM、脚本、布局、绘制和合成逐段标注。再注入一个 200 毫秒长任务，观察资源已到达但页面仍不可交互，说明网络完成不等于渲染完成。',
    followups: `1. **HTML 解析遇到普通 script 为什么可能暂停？**

   脚本可能调用 document.write 或修改当前 DOM，解析器需等待脚本下载执行；defer 通常在解析后按序执行，async 则下载完即执行且顺序不保证。

2. **页面已经绘制为什么点击仍然卡顿？**

   主线程可能正执行长 JavaScript、样式计算或同步布局，事件只能排队；应结合 INP、Long Task 和火焰图定位，而非只优化图片下载。`,
    pitfalls: `- 不要背成绝对串行步骤，预加载扫描、网络请求和解析会并行交错。
- 不要把 DOMContentLoaded、load 与“可交互”当成同一个时刻，它们衡量的条件不同。`,
  },
  '7.2': {
    practice: '用 nslookup 或 dig 对一个未缓存域名查询，记录浏览器/系统缓存、hosts、递归解析器、根、TLD 与权威服务器的角色。再查 CNAME 和 A/AAAA，修改一个低 TTL 测试记录，观察不同递归节点传播时间；明确客户端通常向递归解析器发查询，不会自己走遍所有层。',
    followups: `1. **为什么修改 DNS 后不同用户看到结果时间不同？**

   各级缓存按旧记录 TTL 独立过期，运营商和本地缓存时间点不同；负缓存、CNAME 链与应用自身 DNS 缓存也会造成差异。

2. **DNS 查询一定使用 UDP 53 吗？**

   不是，响应截断、区域传送等会使用 TCP，现代客户端还可能通过 DoH 或 DoT 加密查询；具体路径由系统与网络配置决定。`,
    pitfalls: `- 不要把递归解析和迭代查询混为一谈，客户端与递归服务器承担的工作不同。
- 不要通过把 TTL 永久设得极低解决发布，查询负载、成本和缓存收益也需要权衡。`,
  },
  '7.3': {
    practice: '用 Wireshark 抓取一次 TCP 连接建立和主动关闭，标出双方初始序列号、ACK、FIN 与 TIME_WAIT。再制造丢包观察重传，说明三次握手确认双向收发与序列空间，四次挥手来自全双工方向独立关闭；最后用端口复用实验解释旧报文风险。',
    followups: `1. **为什么不能两次握手就安全建立连接？**

   服务端发出 SYN+ACK 后需要知道客户端确实收到自己的初始序列号，第三个 ACK 才完成双向确认，也能避免旧 SYN 让服务端长期保留半开状态。

2. **TIME_WAIT 为什么通常在主动关闭方？**

   主动关闭方发送最终 ACK，需要保留状态以便对端重发 FIN 时再次确认，并等待旧分节从网络消失；时长与实现的最大报文生存假设有关。`,
    pitfalls: `- 不要把四次挥手说成永远四个独立包，ACK 与 FIN 在合适时机可以合并。
- 不要看到大量 TIME_WAIT 就立即调小内核参数，应先确认连接复用、关闭方向和真实端口耗尽。`,
  },
  '7.4': {
    practice: '用 curl -v 请求同一路径的 HTTP 与 HTTPS，观察 HTTP 语义如何承载在 TCP/TLS 或其他传输上，HTTPS 额外完成证书验证和密钥协商。为 API 构造 200、201、204、304、400、401、403、404、409、429、500、503，逐个写清客户端是否重试及用户提示。',
    followups: `1. **401 与 403 应如何区分？**

   401 表示缺少或无效认证，通常可重新登录；403 表示身份已确认但无权限。隐藏敏感资源存在性时服务端也可按策略统一返回 404。

2. **HTTPS 能否阻止恶意服务端读取请求？**

   不能，TLS 保护客户端到所验证服务端之间的传输和完整性，终止 TLS 的服务仍能看到明文；端点可信、应用鉴权和数据最小化仍必要。`,
    pitfalls: `- 不要认为状态码 200 就代表业务一定成功，接口契约仍需检查响应结构。
- 不要把 500 系列全部自动重试，非幂等请求和持续故障会因重试放大副作用。`,
  },
  '7.5': {
    practice: '用一个 CPU 计算和一个 I/O 等待任务分别比较单进程、多线程、async 协程与 worker 进程。记录地址空间、调度单位、上下文切换和崩溃隔离；在 Python 示例中说明协程只在 await 处让出，并不会让 CPU 密集代码自动并行。',
    followups: `1. **线程共享内存为什么既快又危险？**

   同进程线程可直接访问堆，通信成本低，但并发读写会产生竞态和可见性问题；需要锁、原子操作或所有权设计，并控制临界区。

2. **协程数量可以无限增长吗？**

   不可以，每个协程仍占栈、任务对象和外部连接，阻塞操作还会卡住事件循环；应设置并发上限、超时和背压。`,
    pitfalls: `- 不要把“线程由系统调度、协程由用户态调度”当成所有语言实现的完整答案。
- 不要用协程处理未拆分的 CPU 长任务，它仍会阻塞所在执行线程。`,
  },
  '7.6': {
    practice: '在 Linux 进程中申请一大段虚拟内存但只访问部分页面，观察 VIRT、RSS 与缺页变化；再读取一个地址的页号概念，画出虚拟页经页表和 TLB 映射到物理页框。触发 swap 或文件映射实验，说明“从硬件读取内存”还涉及缓存层级与操作系统权限。',
    followups: `1. **为什么分配 1GB 虚拟内存后 RSS 可能很小？**

   操作系统可延迟到首次访问才提交物理页，未触碰的地址只占虚拟地址范围；过量承诺是否允许取决于系统 overcommit 策略。

2. **TLB miss 与 page fault 是同一件事吗？**

   不是，TLB miss 可能通过已有页表快速补充；page fault 表示页表权限或驻留不满足，需要内核处理，可能分配页或从磁盘读入。`,
    pitfalls: `- 不要把虚拟地址直接说成磁盘地址，绝大多数已驻留页面映射到物理内存。
- 不要只看进程 VIRT 判断内存泄漏，共享映射、保留地址和真实 RSS 含义不同。`,
  },
  '7.7': {
    practice: '为三类需求选结构并实测：随机访问百万项用数组，频繁在已知节点后插入用链表，撤销历史用栈，任务先来先服务用队列。分别记录访问、插入、删除复杂度与实际缓存局部性；再说明 JavaScript Array 的动态实现不等于教科书固定数组。',
    followups: `1. **链表插入是 O(1)，为什么实际应用仍常用数组？**

   O(1) 前提是已拿到插入节点，寻找位置仍可能 O(n)；数组连续内存缓存友好、实现简单，移动少量元素时往往更快。

2. **实现队列为什么不建议反复 Array.shift？**

   shift 可能移动后续元素形成 O(n) 成本；可维护 head 索引或使用环形缓冲区，并在合适时机压缩已消费空间。`,
    pitfalls: `- 不要只背大 O 而忽略常数、缓存局部性和数据规模。
- 不要用链表解决需要频繁按索引读取的问题，它必须从头或已知节点顺序遍历。`,
  },
  '8.1': {
    practice: '实现 Floyd 快慢指针并用四组用例走读：空链表、单节点无环、单节点自环、长链中部入环。相遇后让一个指针回到头部，两者同速前进求入口；打印移动步数并解释距离等式，同时给出 Set 方案作为易懂但 O(n) 空间的对照。',
    followups: `1. **快慢指针第一次相遇点为什么通常不是入口？**

   快指针在环内可能多走若干圈，相遇只证明存在环；由头到入口距离与相遇点到入口的特定模环关系，重置一个指针后才会在入口相遇。

2. **如果还要计算环长度怎样做？**

   第一次相遇后固定一个指针，另一个沿 next 走到再次相遇并计数；必须先确认有环，避免在 null 上继续访问。`,
    pitfalls: `- 不要忘记检查 fast 与 fast.next，偶数长度无环链表会访问空指针。
- 不要为了找入口修改节点标记，题目可能要求保持输入结构且节点值也可能重复。`,
  },
  '8.2': {
    practice: '实现逆序数字链表相加，循环条件写成 l1、l2 或 carry 任一存在；每轮取缺失节点为零，计算 sum、digit 和新 carry。用 342+465、999+1、0+0、长度不等四组测试，再说明若输入为正序链表需用栈或先反转，不能沿用同一遍历方向。',
    followups: `1. **为什么循环结束后还要处理 carry？**

   最高位相加可能产生新进位，例如 999+1 得到额外节点 1；把 carry 放进循环条件可统一处理，不需结束后特殊分支。

2. **能否复用较长输入链表减少空间？**

   可以原地修改，但会破坏输入并使共享节点场景危险；面试前先确认约束，默认新建结果链表通常更清晰，额外空间是结果本身所需。`,
    pitfalls: `- 不要用 JavaScript Number 先还原超长整数，超过安全整数范围会丢精度。
- 不要忘记题目数字存储方向，正序与逆序决定遍历和进位传播方案。`,
  },
  '8.3': {
    practice: '对包含 NaN、-0、对象引用和重复字符串的数组分别用 Set、filter+indexOf、排序双指针和按 key 的 Map 去重。记录是否保持原顺序、比较语义、时间与额外空间；对象按业务 id 去重时明确保留第一项还是最后一项，并测试 id 缺失。',
    followups: `1. **Set 对 NaN 和 -0 的比较语义是什么？**

   Set 使用 SameValueZero，NaN 与自身视为相等，+0 与 -0 也视为相等；这与 indexOf 对 NaN 的行为不同，需要按题目要求说明。

2. **对象内容相同为什么 Set 仍会保留两项？**

   对象按引用身份比较，不做深度结构比较；若业务按 id 或组合字段去重，应显式构造稳定 key，并处理冲突和缺失值。`,
    pitfalls: `- 不要默认排序后去重仍保持输入顺序，排序会改变元素位置且有 O(n log n) 成本。
- 不要用 JSON.stringify 做通用对象去重，键顺序、循环引用和不可序列化值都会出问题。`,
  },
  '8.4': {
    practice: '迭代反转时在每轮写清不变量：prev 已是反转完成的前缀，current 指向未处理首节点；先保存 next，再改 current.next，最后推进两指针。用空、单节点、两节点和长链测试；再写递归版比较 O(n) 调用栈与尾节点处理。',
    followups: `1. **为什么必须先保存 next 再改指针？**

   current.next 被改为 prev 后，原来的后续链路会丢失；保存 next 才能继续遍历未处理部分，这是原地算法的关键顺序。

2. **如何只反转区间 m 到 n？**

   先找到区间前驱，按相同不变量反转指定长度，再把前驱接新头、旧区间头接后继；需要特别处理 m=1 时没有前驱。`,
    pitfalls: `- 不要在循环结束时返回 current，它已经是 null，正确新头是 prev。
- 不要忽略输入可能包含环，普通反转会无限循环；若题目不保证无环需先检测。`,
  },
  '8.5': {
    practice: '实现一个接收任务函数数组和 limit 的调度器，只在 running 小于上限时启动；每项完成或失败都在 finally 释放槽位并启动下一项，结果按原索引保存。用并发计数器断言从未超过 3，并测试同步抛错、异步拒绝、空队列和取消等待任务。',
    followups: `1. **为什么队列里应保存函数而不是已经创建的 Promise？**

   Promise 创建时任务通常已开始，放入队列后无法限制并发；保存惰性函数，调度器取得槽位时才调用，才能真正控流。

2. **某个任务失败后其余任务是否继续？**

   由契约决定，可仿 allSettled 收集全部结果，也可停止启动新任务并取消在途项；无论哪种都要在失败路径释放槽位，避免队列死锁。`,
    pitfalls: `- 不要只在成功 then 中 running--，拒绝任务会永久占住并发名额。
- 不要把返回结果按完成顺序直接 push，如果调用方要求输入顺序应按原索引写入。`,
  },
  '8.6': {
    practice: '为搜索框实现 trailing debounce：连续输入只在停顿 300 毫秒后请求，并取消旧计时器；为滚动统计实现每 200 毫秒最多一次的 throttle。用假定时器验证 leading、trailing、cancel、flush 与 this/参数透传，比较短时间内实际调用时间线。',
    followups: `1. **防抖为什么适合搜索建议，而节流适合滚动？**

   搜索通常只关心用户停顿后的最终值，防抖可消除中间请求；滚动需要持续反馈但限制频率，节流保证时间窗口内仍有更新。

2. **组件卸载时需要做什么？**

   清理计时器并取消未发送调用，若请求已启动还需 AbortController；否则回调会捕获旧组件状态或在新页面产生副作用。`,
    pitfalls: `- 不要把每次触发都重新延后的实现称为节流，那实际上是防抖。
- 不要忽略 leading 与 trailing 同时开启时的重复边界，单次触发是否执行两次要由契约明确。`,
  },
  '8.7': {
    practice: '用 Map 实现容量为 3 的 LRU：get 命中后 delete 再 set 使键移动到末尾，put 超容量时删除迭代器首键。按 A、B、C、访问 A、加入 D 走读淘汰 B，并测试更新已有键、容量为零和存储 undefined；再说明严格 O(1) 的经典哈希表加双向链表结构。',
    followups: `1. **为什么普通对象加时间戳不一定是 O(1) 淘汰？**

   找最小时间戳需要扫描全部键形成 O(n)，时间相同还需稳定规则；双向链表可在 O(1) 移动节点并从尾部淘汰。

2. **LRU 为什么不一定是最佳缓存策略？**

   顺序扫描会把一次性数据留在缓存并驱逐热点，且未考虑对象大小和过期时间；生产系统可能组合 TTL、LFU、权重与容量字节。`,
    pitfalls: `- 不要只在 put 时更新最近使用顺序，get 命中同样必须提升。
- 不要忘记更新已有键不应增加 size，否则会错误提前淘汰其他项。`,
  },
  '9.1': {
    practice: '运行一段同时包含同步日志、queueMicrotask、Promise.then、setTimeout 和 requestAnimationFrame 的代码，先手写顺序再用 Performance 验证。加入递归微任务观察定时器与渲染被推迟，说明一次 task 结束后清空微任务队列，浏览器才获得渲染机会。',
    followups: `1. **Promise.then 与 setTimeout 谁一定先执行？**

   在同一任务中都被安排且调用栈正常结束时，then 微任务会在下一个 timer 任务前执行；但若它们来自不同时间和任务源，不能脱离上下文背固定顺序。

2. **微任务为什么可能造成页面卡死？**

   每次微任务又排入新微任务时，检查点迟迟无法清空，浏览器不能进入渲染和后续任务；长工作应分片到任务或 worker，并设置终止条件。`,
    pitfalls: `- 不要说“宏任务队列只有一个且严格按所有来源全局先进先出”，浏览器有多个任务源与调度策略。
- 不要把 async 函数整体视为微任务，只有 await 之后的续体按 Promise 反应调度。`,
  },
  '9.2': {
    practice: '定义 Person 构造函数和 alice 实例，用 Object.getPrototypeOf 逐层打印 alice、Person.prototype、Object.prototype；再手写简化 instanceof，沿左侧对象原型链寻找右侧函数的 prototype。修改 prototype 后创建新实例，观察旧实例链不会自动指向新对象。',
    followups: `1. **Person.prototype 与 Person 的原型是同一个对象吗？**

   不是，前者是构造实例时使用的普通属性，后者可由 Object.getPrototypeOf(Person) 取得，通常指向 Function.prototype，二者角色不同。

2. **为什么跨 iframe 使用 instanceof 可能失败？**

   每个 realm 有独立的内建构造函数和 prototype，对方数组的原型链不包含当前窗口的 Array.prototype；判断数组应使用 Array.isArray。`,
    pitfalls: `- 不要在业务代码依赖非标准的 __proto__ 修改原型，优先使用标准 API 并避免运行时变更。
- 不要把实例的 constructor 属性当不可伪造类型标签，它来自原型且可被覆盖。`,
  },
  '9.3': {
    practice: '设计一个解析 API 结果的函数：入口类型用 unknown，经运行时校验后变成判别联合；泛型保留请求与响应关系，switch default 用 never 检查遗漏。再分别用 interface 扩展对象契约、type 表达联合，演示 any 如何让错误字段一路逃过检查。',
    followups: `1. **unknown 相比 any 的实际收益是什么？**

   unknown 接受任意输入但使用前必须收窄，迫使边界写真实检查；any 会同时跳过读取、调用和赋值检查，错误会扩散到下游。

2. **什么时候会自然推断出 never？**

   判别联合所有分支都已处理后的变量、永不返回函数或互斥类型交集会得到 never；可利用它让新增协议类型在编译期暴露遗漏。`,
    pitfalls: `- 不要争论 interface 与 type 谁全面更好，应按声明合并、联合表达和团队一致性选择。
- 不要用 as 强制断言替代解析外部数据，断言不会生成任何运行时保护。`,
  },
  '9.4': {
    practice: '为登录会话、主题设置和离线题库分别选存储：会话用 HttpOnly Cookie，主题可用 localStorage，结构化题库与 outbox 用 IndexedDB。用 DevTools 观察容量、同步阻塞、事务和请求是否自动携带；再清除站点数据，验证应用对缺失或旧 schema 能恢复。',
    followups: `1. **为什么不把 access token 一律放 localStorage？**

   任意同源 XSS 都可读取并外传，且 token 生命周期难由服务端控制；HttpOnly Cookie 不可被脚本读取，但要配合 SameSite 与 CSRF 防护。

2. **IndexedDB 的事务什么时候可能失效？**

   事务只在活跃事件循环阶段保持，跨不相关 await 可能自动提交；应在事务回调中及时发起请求，并处理版本升级被其他标签页阻塞。`,
    pitfalls: `- 不要在 localStorage 保存大对象并高频 stringify，它是同步 API，会阻塞主线程。
- 不要把 Cookie 当普通前端数据库，容量很小且会随匹配请求发送，增加带宽与泄露面。`,
  },
  '9.5': {
    practice: '建立包含联合索引 (tenant_id, status, created_at, id) 的订单表，用 EXPLAIN ANALYZE 比较等值租户加状态范围、只查 created_at 和深 offset 三类查询。观察 B+Tree 从根到叶定位、叶子顺序扫描和二级索引回表，再改成覆盖索引与游标分页验证扫描行数。',
    followups: `1. **联合索引为什么强调最左前缀？**

   键按定义列依次排序，跳过前导列时后续列在整棵树中不连续，通常无法直接定位范围；优化器是否使用仍取决于统计和查询成本。

2. **B+Tree 为什么把数据集中在叶子层？**

   内部节点只存导航键可提高扇出、降低树高，叶子有序相连便于范围扫描；具体聚簇与二级索引叶子内容由存储引擎决定。`,
    pitfalls: `- 不要机械地把区分度最高列放联合索引第一位，等值、范围、排序和实际查询组合都要考虑。
- 不要只看 EXPLAIN 选择了索引就判定优化成功，还要比较实际扫描行数、回表和耗时。`,
  },
  '9.6': {
    practice: '用两个数据库会话复现并发异常：一个事务更新未提交，另一个读取验证脏读；同一事务两次读同一行验证不可重复读；同一范围再次查询验证幻读。记录 MySQL 当前隔离级别、快照读与 FOR UPDATE 的差异，并为库存扣减写带条件的原子 UPDATE。',
    followups: `1. **可重复读是否意味着所有查询结果永远不变？**

   快照读通常保持一致视图，但当前读会读取最新可锁版本，自己事务的写入也可见；具体幻读防护依赖 MVCC 与锁实现，不能只背标准名称。

2. **隔离级别越高为什么不一定越好？**

   更强隔离可能增加锁等待、冲突重试或序列化失败，吞吐下降；应根据业务不变量选择，并用唯一约束和条件更新在数据库层兜底。`,
    pitfalls: `- 不要在事务中等待远程 HTTP，长时间持锁会放大死锁与连接池占用。
- 不要捕获死锁后无条件无限重试，只有幂等事务才能在退避和次数上限内安全重放。`,
  },
  '9.7': {
    practice: '设计订单 API：PUT /orders/{id} 表达完整替换，PATCH 只接受白名单字段，创建用 Idempotency-Key 防重复。列表按 createdAt、id 做稳定游标，错误返回 code、message、fieldErrors、requestId。用同一请求重放、版本冲突和深分页数据变动验证契约。',
    followups: `1. **PATCH 请求天然幂等吗？**

   不天然，设置字段为固定值可幂等，而“余额加一”重复执行会累加；接口必须定义操作语义并用幂等键或版本条件控制重放。

2. **为什么游标要同时包含唯一键？**

   createdAt 可能相同，只用它无法确定严格下一位置，会重复或漏记录；追加 id 形成全序，查询条件与 ORDER BY 必须一致。`,
    pitfalls: `- 不要所有业务失败都返回 200 再塞 error 字段，HTTP 状态与稳定业务码应共同表达结果。
- 不要把内部异常堆栈和 SQL 直接返回客户端，保留 requestId 供服务端关联日志。`,
  },
  '9.8': {
    practice: '在 Flask application factory 中注册一个流式 Blueprint，请求进入前记录 requestId，路由读取 request 与 g，返回 generator。先复现 generator 执行时上下文已退出，再使用 stream_with_context 或预先复制所需值修复；最后验证 teardown 无论成功异常都会释放数据库 session。',
    followups: `1. **request 为什么看似全局却能区分并发请求？**

   它是上下文本地代理，在当前 worker 的 request context 中解析到对应对象；脱离上下文或把代理传到后台线程都会失败或读错生命周期。

2. **流式响应期间数据库事务应一直保持吗？**

   通常不应，先完成必要查询并提交或释放，再开始长流；否则慢客户端会长期占用连接和锁，需把后续状态写入拆成短事务。`,
    pitfalls: `- 不要用 Flask 开发服务器作为生产并发方案，它缺少正式 worker、超时与进程治理。
- 不要在 import 时创建全局 app 和数据库连接，工厂模式更利于测试配置与多实例。`,
  },
  '9.9': {
    practice: '写一个并发查询三个服务的 Go handler：errgroup 派生 context，任一关键请求失败就取消其余 goroutine；结果通过明确所有权写入，设置总超时并在返回前等待回收。再实现一个有缓冲 channel 的 worker pool，压测慢消费者时观察背压和 goroutine 数。',
    followups: `1. **channel 应该由发送方还是接收方关闭？**

   通常由能确定不会再发送的一方关闭，接收方随意关闭会让后续发送 panic；channel 不必总关闭，只有接收方需要结束信号时才必要。

2. **context.Value 适合传业务参数吗？**

   不适合，它用于请求范围的元数据，如 traceId、认证主体；必需业务参数应显式传递，避免隐藏依赖和键冲突。`,
    pitfalls: `- 不要启动 goroutine 后忘记退出条件，阻塞的发送、接收和 I/O 都可能泄漏。
- 不要复制含 mutex 的结构或并发无保护地写 map，竞态检测应进入测试流程。`,
  },
  '9.10': {
    practice: '为文章站分别制作 CSR、SSR 和构建时 SSG 路由，比较首屏 HTML、数据新鲜度、缓存与部署成本。SSR 示例中故意使用 Date.now、随机数和 window 条件造成 hydration mismatch，再把首屏输入序列化为确定数据、浏览器逻辑移到 Effect，验证告警消失。',
    followups: `1. **SSG 页面数据更新后必须全站重建吗？**

   不一定，可按路由增量构建、使用按需再生成或让部分数据在客户端刷新；选择取决于发布平台、实时性和一致性要求。

2. **SSR 是否一定改善所有性能指标？**

   它可能更早输出内容，但服务器计算、数据瀑布和 hydration JavaScript 仍有成本；需要测 TTFB、LCP、INP 与缓存命中，而非只看有 HTML。`,
    pitfalls: `- 不要在服务端与客户端首帧使用不同随机值、时区或浏览器 API，这会造成 hydration 不一致。
- 不要把 SEO 当选择 SSR 的唯一理由，分享预览、弱设备体验、缓存与运维复杂度也要评估。`,
  },
  '9.11': {
    practice: '为一次 AI 生成贯穿 requestId、conversationId、messageId、runId，服务端记录排队、检索、模型首 token、总生成和错误阶段，前端记录点击、首字节、首个可见字符、长任务与取消结果。建立一张按版本和阶段聚合的仪表盘，并对日志中的 Prompt、文档和令牌做脱敏。',
    followups: `1. **TTFT 很高时如何定位是模型还是前端问题？**

   比较服务端 model_first_token_at、边缘首字节和前端 first_delta_at；若模型早已产出但客户端晚到，检查代理缓冲与网络，若 delta 到达却未显示则查解析和渲染。

2. **为什么只看平均延迟会误导？**

   少量极慢请求和不同文档规模会被平均掩盖，应看 P50、P95、P99 并按阶段、模型、版本和错误类型切分，同时保留样本量。`,
    pitfalls: `- 不要把完整 Prompt、模型输出、JWT 或用户文档直接写日志，可观测性不能越过隐私边界。
- 不要只有指标没有 trace 关联字段，否则前端慢、后端慢和模型慢无法还原为同一次请求。`,
  },
}
